# =============================================================
# Saigon Rider - Dev Environment Config (EXAMPLE)
# Copy this file to .env and fill in your values
# =============================================================

# Network
NETWORK_BRIDGE=saigon-net

# Ports
NGINX_PORT=18090
BIZ_LANDING_PORT=18092
FRONTEND_PORT=5174
BACKEND_PORT=8082
DB_PORT=5435

# Database (PostgreSQL + PostGIS)
DB_NAME=saigon_rider
DB_USER=saigon
DB_PASSWORD=change_me

# Frontend → API base URL
VITE_USE_MOCK=true
VITE_API_BASE=http://localhost:18090/api

# ── BFF ────────────────────────────────────────────────────
# BFF 공개 URL (절대경로 redirect, mock-img endpoint 등에 사용)
BFF_PUBLIC_URL=http://localhost:18090
CORS_ALLOWED_ORIGINS=http://localhost:18090,http://localhost:5173,http://localhost:5174,https://saigon.doil.me,https://localhost,https://business.saigon-rider.com
# 광고주 tier 계약(웹 게이트) 링크가 가리키는 비즈니스 포털 도메인 (앱 밖 계약/결제안내 — Apple 3.1.3(g) 회피)
BIZ_PORTAL_BASE_URL=https://business.saigon-rider.com
# BFF 타임존 (DAILY/WEEKLY 퀘스트 일자 경계, 월별 통계 등)
APP_TIMEZONE=Asia/Ho_Chi_Minh

# Imgproxy
# KEY/SALT 미설정 시 /insecure/ 경로 사용 가능 (개발 전용)
# 운영 시: openssl rand -hex 32
IMGPROXY_KEY=
IMGPROXY_SALT=
# 클라이언트에 반환할 imgproxy 공개 URL
IMGPROXY_BASE_URL=http://localhost:18090/img

# ── Redis (Message Queue) ─────────────────────────────────
# Docker 내부 네트워크 전용 (포트 미노출)
REDIS_URL=redis://redis:6379/0
REDIS_FUEL_PRICE_PREFIX=saigon:fuel:
# 다중 워커 실시간 브로드캐스트(위치채널/워키토키 공유) — redis|inprocess, 기본 inprocess(단일 워커)
REALTIME_BROADCAST=inprocess

# ── Engine 서비스 ──────────────────────────────────────────
ENGINE_PORT=8090
# BFF→Engine 서비스 인증 키 (운영 시: openssl rand -hex 32)
ENGINE_SERVICE_KEY=change_me_engine_service_key
ENGINE_ADMIN_JWT_SECRET=change_me_engine_admin_jwt_secret
# Engine 워커가 BFF 내부 API 콜백 시 사용 (Docker 내부 호스트)
BFF_INTERNAL_URL=http://bff:8080

# ── Plane (프로젝트 관리) ──────────────────────────────────
PLANE_API_KEY=
PLANE_URL=https://plane.doil.me
PLANE_WORKSPACE=doil
PLANE_PROJECT_ID=

# ── MCP Dev Server (legacy — Plane으로 대체됨) ─────────
MCP_DEV_PORT=8095

# ── Admin Console (Backend 어드민) ─────────────────────────
# 운영 시: ADMIN_PASS_HASH 는 passlib bcrypt 해시, JWT_SECRET 은 openssl rand -hex 32
# ADMIN_USER / ADMIN_PASS_HASH 는 root 관리자 (비밀번호 분실 방지용 정적 계정).
# 일반 관리자 계정은 admin_accounts 테이블에서 root 가 등록.
ADMIN_USER=change_me_admin_user
ADMIN_PASS_HASH=change_me_admin_pass_hash
ADMIN_JWT_SECRET=change_me_admin_jwt_secret

# Wiki 링크 (admin 콘솔 → 위키 메뉴 / login 화면 하단). nginx /wiki/ 프록시 경로.
WIKI_BASE_PATH=/wiki/

# ── Wiki Basic Auth ────────────────────────────────────────
# /wiki/docs/private/ 접근 시 Nginx Basic Auth에 사용
WIKI_AUTH_USER=admin
WIKI_AUTH_PASS=changeme

# ── Info Modules (날씨·침수·주유소·정비소) ──────────────────────
# OpenWeather API (https://openweathermap.org/api) — Free tier: 60 calls/min
OPENWEATHER_API_KEY=
# RainViewer API (https://www.rainviewer.com/api.html)
RAINVIEWER_API_KEY=
# OSM Overpass endpoint (공개 인스턴스 그대로 사용 가능)
OSM_OVERPASS_ENDPOINT=https://overpass-api.de/api/interpreter
# Google Maps Directions API (주유소/정비소 경로 미리보기 — SGR-269)
# 비우면 [경로] 진입 시 "준비 중" 안내(폴백). 키 입력 시 즉시 실경로 활성화.
GOOGLE_MAPS_API_KEY=
# 자체호스팅 라우팅 엔진(Valhalla, docker compose --profile backend). 비우면 위 Google
# Routes API 경로 그대로 동작(롤백 스위치) — BFF 내부 호출 전용, nginx 미노출.
ROUTING_ENGINE_URL=
# 실시간 번역 (SGR-287). 비우면 stub(원문 반환). 키 입력 시 즉시 실번역.
# TRANSLATE_PROVIDER=google → Google Cloud Translation v2 (translation.googleapis.com)
TRANSLATE_API_KEY=
TRANSLATE_PROVIDER=google
# 캐시 TTL (초)
WEATHER_CACHE_TTL_CURRENT=600
WEATHER_CACHE_TTL_FORECAST_1H=1800
WEATHER_CACHE_TTL_FORECAST_24H=3600

# ── SRE 비즈니스 룰 (docker-compose에서 직접 주입되므로 참고용) ──
# SRE_TIMEZONE=Asia/Ho_Chi_Minh
# SRE_XP_EXPIRY_MONTHS=3
# SRE_DAILY_CAP_STANDARD=250
# SRE_DAILY_CAP_DRIVER=2000
# SRE_NEW_ACCOUNT_PENALTY_DAYS=3
# SRE_NEW_ACCOUNT_MULTIPLIER=0.5
# SRE_IDEMPOTENCY_TTL_DAYS=7
# SRE_LOG_LEVEL=INFO

# ── Firebase Cloud Messaging (FCM) ──────────────────────────────
# 서비스 계정 JSON 파일 경로 (Docker 내부 경로)
# engine/ 디렉토리 내에 서비스 계정 JSON 파일 배치 (./engine:/app 마운트로 자동 접근)
FIREBASE_CREDENTIALS_JSON=/app/firebase-credentials.json
# APNs 직접 전송 — iOS Live Activity 원격 갱신 (engine). 키 파일은 engine/apns-key.p8 (커밋 금지)
APNS_KEY_ID=
APNS_TEAM_ID=
APNS_KEY_PATH=/app/apns-key.p8
APNS_USE_SANDBOX=false
# 푸시 발송 이력 Redis TTL (일)
FCM_PUSH_HISTORY_TTL_DAYS=7

# ── Google AI Studio (Imagen 이미지 생성용) ────────────────────────
# scripts/generate_quest_card_images.py 등 이미지 생성 스크립트에서 사용
# AI Studio (https://aistudio.google.com/) 에서 발급한 API 키 (AIza...)
GOOGLE_API_KEY=

# ── SMS OTP (판매자 휴대폰 인증) ────────────────────────────────
# VN 로컬 게이트웨이(SpeedSMS/eSMS 계열) API 키. 비우면 __DEV 스텁(서버 로그로 코드 출력).
SMS_PROVIDER_API_KEY=
# 실행 환경 (development|production) — SMS __DEV 스텁·dev-login 노출 제어. 운영은 docker-compose.prod.yml 이 production 강제.
APP_ENV=development
# 개발 전용 — 운영 금지. APP_ENV 가 dev 화이트리스트일 때만 유효한 이중게이트 — OTP 검증 시 임의 6자리 코드 허용(해시 비교 생략). 기본 비활성.
OTP_DEV_BYPASS=
# 개발 전용 — 운영 금지. /auth/dev-login-as(지정 계정 OAuth 우회 로그인)를 허용할 Host 헤더 값.
# APP_ENV 화이트리스트와 별개의 이중게이트 — 이 값과 요청 Host 가 정확히 일치해야 우회가 열린다.
DEV_HOST=

# ── 오프사이트 암호화 백업 (게이트9 B-5) ───────────────────────────
# app/jobs/backup_db.py 가 로컬 pg_dump 산출물을 openssl 로 대칭키 암호화한 뒤
# S3 호환 버킷(AWS S3 / Cloudflare R2 / MinIO 등 — endpoint_url 오버라이드만 다름, 코드는 동일)에
# 업로드한다. 아래 6개 키 중 BACKUP_S3_BUCKET·BACKUP_ENCRYPTION_KEY 가 비어있으면 업로드 단계를
# 완전히 건너뛴다(fail-open) — 로컬 백업 자체는 이 설정과 무관하게 항상 수행된다.
#
# ⚠️ BACKUP_ENCRYPTION_KEY 를 분실하면 그 키로 암호화된 모든 오프사이트 백업은 영구 복구 불가
#    (대칭키 1개로 전체 암호화 — 키가 곧 백업의 존재 가치). 이 파일에만 두지 말고 별도
#    비밀관리 수단(비밀번호 관리자·대표 개인 보관)에 반드시 이중 백업해둘 것.
BACKUP_ENCRYPTION_KEY=
BACKUP_S3_BUCKET=
# AWS S3 사용 시 비워둠(기본 엔드포인트). Cloudflare R2/MinIO 등은 해당 서비스의 S3 호환
# 엔드포인트 URL을 지정 (예: https://<account>.r2.cloudflarestorage.com).
BACKUP_S3_ENDPOINT_URL=
BACKUP_S3_REGION=
BACKUP_S3_ACCESS_KEY_ID=
BACKUP_S3_SECRET_ACCESS_KEY=

# ── Zalo API 프록시 (Zalo OAuth 로그인) ────────────────────────
# 한국 호스팅에서 Zalo Graph API 호출 시 IP 제한(-501) 우회용 베트남 IP 프록시. 예: http://user:pass@vn-host:port
# (socks5:// 를 쓰려면 httpx[socks]/socksio 의존성 추가 필요 — 현재 미설치, HTTP 프록시만 지원)
# 비우면 프록시 없이 직접 호출(기존 동작과 동일).
ZALO_API_PROXY=

# ── 운영자 알림 웹훅 (F-17 신고 접수 / F-18 미처리 예외) ──────────
# Slack Incoming Webhook 등 {"text": "..."} POST 형태를 받는 URL. 비우면 로그만 남기고 무동작.
OPS_ALERT_WEBHOOK_URL=

# ── 탈퇴회원 식별자 해시 아카이브 pepper (withdrawn_member_archive) ──
# 탈퇴 시 전화번호·OAuth 식별자를 HMAC-SHA256 으로 해시해 1년 보관할 때 쓰는 서버 비밀키.
# 임의의 64자 hex 권장 (openssl rand -hex 32). 비우면 아카이브 기록을 건너뛰고 운영자 경보만
# 남긴다(탈퇴 자체는 정상 진행 — fail-open).
# ⚠️ 이 값을 바꾸면 기존에 저장된 해시 전부가 대조 불가능해진다(사실상 아카이브 리셋).
WITHDRAWN_HASH_PEPPER=

# ── 광고 익명 노출 reach 중복제거 키 pepper (ad_events.anon_key) ──
# 익명(비로그인) 광고 노출을 IP+User-Agent 해시(HMAC-SHA256, 일 단위 회전)로 중복제거해
# reach 를 근사 집계할 때 쓰는 서버 비밀키. 임의의 64자 hex 권장 (openssl rand -hex 32).
# 비우면 익명 reach 는 0 으로 집계된다(fail-open — 노출/클릭 수집 자체는 그대로 동작).
AD_ANON_KEY_PEPPER=

# uvicorn 핫리로드 (빈 값이면 --reload 없이 구동). WSL2 /mnt/c(drvfs) 가 os error 5 를 내며
# watchfiles 워처가 죽어 BFF 가 크래시 루프에 빠질 때 비운다. 코드 반영은 docker restart saigon_bff.
UVICORN_RELOAD=1
