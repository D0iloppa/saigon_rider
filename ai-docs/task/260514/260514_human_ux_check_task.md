# 260514 · 휴먼 UX 점검 Task (화면 직접 확인)

> 작성일: 2026-05-14  
> 목적: 자동화로 검증 불가한 UI 렌더링·인터랙션·상태분기를 인간이 브라우저에서 직접 점검한다.  
> 환경: `http://localhost:18090`  
> 보고 방법: 각 항목 점검 후 결과를 "✅ 정상" / "❌ 문제: [증상]" / "⛔ N/A" 형태로 보고

---

## 준비사항

```
1. 브라우저 DevTools 열기 (F12) → Console 탭 + Network 탭 열어둘 것
2. 시크릿 창으로 http://localhost:18090 접속
3. 각 화면 진입 전 localStorage 상태 메모 (로그인 여부)
```

---

## 📋 화면별 점검 항목

---

### [ONB-001] 스플래시 (`/splash`)

> 로그인 **안 된** 상태로 접속

| # | 점검 항목 | 점검 방법 | 결과 |
|---|---|---|---|
| O-1 | 배경 이미지 + 브랜드 그라디언트 표시 | 사이공 야경 사진 위에 그라디언트 오버레이 노출 여부 | ✅ 정상 |
| O-2 | "SAIGON RIDER" 텍스트 + 서브타이틀 표시 | 중앙 워드마크 정상 노출 | ✅ 정상 |
| O-3 | 언어 선택 칩 (상단 우측) | VI/EN/KO 중 하나 노출, 클릭 시 드롭다운 열림 | ✅ 정상 |
| O-4 | 언어 변경 시 텍스트 즉시 변경 | KO 선택 → 버튼 텍스트가 한국어로 전환 | ✅ 정상 |
| O-5 | "시작하기" 버튼 → `/auth/phone` 이동 | 클릭 시 번호 입력 화면으로 이동 | ✅ 정상 |
| O-6 | "로그인" 버튼 → `/auth/phone` 이동 | 클릭 시 동일하게 번호 입력 화면 이동 | ✅ 정상 |
| O-7 | 로그인 상태에서 `/splash` 접속 | localStorage에 user 있을 때 → `/home` 자동 이동 | ✅ 정상 |

---

### [AUTH-001] 번호 입력 (`/auth/phone`)

> 로그인 **안 된** 상태

| # | 점검 항목 | 점검 방법 | 결과 |
|---|---|---|---|
| A1-1 | 국가코드 선택 드롭다운 (기본값 VN +84) | 좌측 국기 + +84 노출, 클릭 시 국가 목록 | ✅ 정상 |
| A1-2 | 숫자 외 입력 차단 | 영문/특수문자 입력 시 무시 여부 | ✅ 정상 (에러 메시지 대신 버튼 비활성화로 처리 — 명세와 UX 패턴 다르지만 동작은 안전) |
| A1-3 | 너무 짧은 번호 제출 시 에러 | 3자리 입력 후 버튼 클릭 → 에러 메시지 노출 | ✅ 정상 (에러 메시지 대신 버튼 비활성화로 처리 — 제출 자체가 불가함) |
| A1-4 | 정상 번호 입력 후 제출 | `901234567` 입력 → Network탭에 `POST /api/bff/auth/register` 200 응답 확인 | ✅ 정상 |
| A1-5 | 제출 후 화면 이동 | 신규 번호: `/auth/profile-setup` / 기존 번호: `/home` 이동 여부 | ✅ 정상 |

---

### [AUTH-002] OTP/Passcode 입력 (`/auth/otp`)

> **현재 실제 흐름과 다름**: PhoneInput에서 register 완료 시 자동 로그인됨. OTP 화면은 레거시 상태.

| # | 점검 항목 | 점검 방법 | 결과 |
|---|---|---|---|
| A2-1 | `/auth/otp` 직접 접속 시 화면 렌더 | URL 직접 입력 → 6칸 박스 노출 여부 | ⛔ N/A: 실제 인증 흐름에서 미사용(레거시) — 생략 |
| A2-2 | 숫자 입력 시 다음 칸 자동 이동 | 1칸 입력 → 자동으로 다음 박스 포커스 | ⛔ N/A: 생략 |
| A2-3 | 타이머 카운트다운 표시 (03:00 → 감소) | 진입 후 mm:ss 감소 확인 | ⛔ N/A: 생략 |
| A2-4 | 6자리 미만 제출 → 에러 | 3칸만 입력 후 확인 버튼 → 에러 표시 | ⛔ N/A: 생략 |

---

### [PROFILE-SETUP] 라이더 프로필 설정 (`/auth/profile-setup`)

> ⚠️ **알려진 결함**: 저장 버튼이 BFF API 미호출 (Zustand만 업데이트). DB에 저장 안 됨.

| # | 점검 항목 | 점검 방법 | 결과 |
|---|---|---|---|
| P-1 | 화면 진입 시 닉네임 입력 + 라이더 타입 카드 노출 | 3종 카드 (COMMUTER/CAFÉ HUNTER/NIGHT RIDER) | ✅ 정상 (카드 3종 노출) |
| P-1-b | 라이더 타입 카드 아이콘 표시 | 출퇴근러·카페헌터·나이트라이더 아이콘 | ✅ 수정 확인: 1f3cd(🏍️)·1f31f(🌟) 교체 후 정상 표시 |
| P-2 | 닉네임 2자 미만 제출 차단 | 1자 입력 후 시작 버튼 비활성 확인 | 🟡 미결: 최초 보고 시 1자로 활성화 현상. 리빌드 후 재확인 보류 — 한국어 IME 이슈 조사 필요 |
| P-3 | 라이더 타입 선택 시 하이라이트 | 카드 클릭 → 선택 강조 표시 | ✅ 정상 |
| P-4 | 저장 버튼 클릭 → `/home` 이동 | Network탭: `PUT /api/bff/profile` 호출 여부 확인 (현재 미호출 예상) | 🟡 `/home` 이동은 정상. 단 Network탭 PUT 미호출은 알려진 결함 (Zustand만 업데이트) |
| P-5 | Dot Indicator 2/3 단계 표시 | 상단 점 3개 중 2개 활성 | ✅ 정상 |

---

### [HOME-001] 월드맵 홈 (`/home`)

> 로그인 **된** 상태 필요. ⚠️ 방금 `/api/quests` transformer 버그 수정 완료 — 이전 크래시 해결됐는지 확인 필요.

| # | 점검 항목 | 점검 방법 | 결과 |
|---|---|---|---|
| H-1 | 화면 진입 시 에러 없음 | DevTools Console에 빨간 에러 없어야 함 | ✅ 정상 |
| H-2 | 상단 헤더: 아바타 + 닉네임 + 레벨 표시 | 로그인 유저 정보 노출 | ✅ 정상 |
| H-3 | 레벨 Progress Bar 표시 | 상단 헤더 하단 진행도 바 | ✅ 정상 |
| H-4 | XP / GOLD / SKILL PT 카드 3종 | 숫자 카드 3개 노출, 값이 0이어도 표시 | ✅ 정상 |
| H-5 | SVG 월드맵 렌더 | 강·구역 폴리곤 표시, 핀 3개 표시 | ✅ 정상 |
| H-6 | 추천 퀘스트 카드 노출 (Tonight's Pick) | BFF에 퀘스트 데이터 있으면 카드 표시 | ✅ 정상 |
| H-7 | 추천 퀘스트 카드 → 상세 이동 | "퀘스트 시작" 버튼 클릭 → `/quests/{id}` | ✅ 정상 |
| H-8 | 하단 탭바 5개 탭 표시 | 지구본/퀘스트/FAB/피드/사람 아이콘 | ✅ 정상 |
| H-9 | 탭바 탭 클릭 시 화면 전환 | 각 탭 클릭 → 해당 화면으로 이동 | ✅ 정상 |
| H-10 | 설정 아이콘 → `/settings` | 우상단 설정 버튼 클릭 | ✅ 정상 |

---

### [QUEST-LIST] 퀘스트 목록 (`/quests`)

| # | 점검 항목 | 점검 방법 | 결과 |
|---|---|---|---|
| Q1-1 | 퀘스트 카드 목록 로드 | Network탭 `GET /api/bff/quests` 200, 카드 렌더 | ✅ 정상 |
| Q1-2 | 세그먼트 탭 (오늘/주간/이벤트) | 3개 탭 노출, 클릭 시 active 전환 | ✅ 정상 |
| Q1-3 | 필터 Chip (구역/타입) | Chip 선택 시 강조 표시 | ❌ 문제: Chip 강조는 되나 실제 필터링 미동작 (목록 변화 없음) |
| Q1-4 | 퀘스트 카드 클릭 → 상세 | 카드 클릭 → `/quests/{id}` 이동 | ✅ 정상 |
| Q1-5 | 빈 상태 UI | 필터 결과 0건 시 안내 메시지 노출 여부 | ⛔ N/A: 필터링 미동작으로 0건 상태 재현 불가 |

---

### [QUEST-DETAIL] 퀘스트 상세 (`/quests/:id`)

> 위 목록에서 카드 클릭하여 진입

| # | 점검 항목 | 점검 방법 | 결과 |
|---|---|---|---|
| Q2-1 | 상세 정보 로드 | 히어로 이미지·제목·설명·조건·보상 표시 | |
| Q2-2 | "퀘스트 시작" 버튼 클릭 | Network탭 `POST /api/bff/quests/{id}/accept` 호출 여부 + `/ride/active` 이동 | |
| Q2-3 | 북마크 버튼 토글 | 북마크 아이콘 클릭 → 상태 반전 확인 | |

---

### [RIDE-ACTIVE] 주행 HUD (`/ride/active`)

> GPS 권한 필요

| # | 점검 항목 | 점검 방법 | 결과 |
|---|---|---|---|
| R1-1 | 화면 진입 시 GPS 권한 요청 팝업 | 브라우저 위치 권한 요청 노출 | |
| R1-2 | 주행 시간 카운트업 (mm:ss) | 진입 후 1초마다 증가 | |
| R1-3 | SVG 링 게이지 표시 | 원형 진행 게이지 노출 | |
| R1-4 | 일시정지 버튼 → BottomSheet | 정지 버튼 → 미니맵·통계 시트 오픈 | |
| R1-5 | 종료 버튼 → 결과 화면 | 종료 → `/ride/result/success` or `/fail` 이동 | |

---

### [RIDE-RESULT-S] 라이딩 결과 성공 (`/ride/result/success`)

| # | 점검 항목 | 점검 방법 | 결과 |
|---|---|---|---|
| RS-1 | Confetti 애니메이션 | 화면 진입 시 색종이 폭죽 | |
| RS-2 | 보상 정보 표시 (EXP/GOLD) | 획득 보상 숫자 표시 | |
| RS-3 | Network탭 `POST /api/bff/ride/submit` 호출 | 결과 화면 진입 시 자동 호출 여부 | |
| RS-4 | "피드에 공유" 버튼 | 버튼 클릭 → Network탭 `POST /api/bff/feed` 확인 | |

---

### [FEED-001] 소셜 피드 (`/feed`)

| # | 점검 항목 | 점검 방법 | 결과 |
|---|---|---|---|
| F-1 | 피드 카드 목록 로드 | DB 실 데이터 노출 (mock 아닌지 확인: "SaigonRider" 게시물) | ✅ 정상 |
| F-2 | 필터 Chip (all/neighborhood/friends/hot) | 클릭 시 active + 재조회 | 🟡 파라미터 전달 ✅, 하이라이트 ✅. 단 BFF neighborhood/friends WHERE 미구현으로 전체 노출 — 운영자 게시글이 모든 필터에 노출되는 것은 BFF 미구현 상태로 인한 현상 |
| F-3 | 응원(좋아요) 버튼 토글 | 하트 클릭 → Network탭 `POST /api/bff/feed/{id}/like` | ✅ 정상 |
| F-4 | 댓글 버튼 → BottomSheet | 댓글 아이콘 클릭 → 시트 오픈 | ✅ 수정 완료: 시트 오픈 정상, `postComment()` BFF 연결 완료 |
| F-4-a | 댓글 목록 닉네임 표시 | 댓글 작성자 닉네임이 노출되어야 함 | ✅ 수정 완료: BFF `get_comments()` JOIN User 추가, `CommentOut`에 `user_nickname` 포함 |
| F-4-b | 댓글 좋아요 기능 | 댓글 ♥ 버튼 클릭 시 BFF 호출 및 카운트 반영 | ✅ 수정 완료: DB `post_comment_likes` 테이블 신설, BFF `toggle_comment_like()` 엔드포인트 추가, 프론트 핸들러 연결 |
| F-4-c | 댓글 프로필 사진 | 댓글 아바타가 피드 카드 아바타와 동일 방식으로 표시되어야 함 | ✅ 수정 완료: BFF `_enrich_comment()`에서 `user_avatar_url` 포함 + `default_avatar_url()` fallback |
| F-9 | 피드 이미지 스켈레톤 & 라이트박스 | 이미지 로딩 중 스켈레톤 표시, 클릭 시 전체화면 뷰어 | ✅ 260515 신규 구현: `PostImage`(shimmer 스켈레톤 + fade-in), `ImageViewer`(핀치줌/더블탭/팬/스와이프닫기) |
| F-5 | 빈 상태 (게시물 0건) | 확인 어려우면 ⛔ N/A | ⛔ N/A |
| F-6 | 이미지 표시 | 이미지 있는 게시물 이미지 노출 여부 | ❌ 미표시: DB image_url = `http://` 절대경로 → https 페이지 mixed content 차단 추정. imgproxy 파일 존재 여부도 미확인 — 조사 필요 |
| F-7 | 스토리 strip | 상단 스토리 아바타 실 데이터 노출 | ❌ 수정 완료: 하드코딩(@minh 등) → `fetchStories()` 실 API 연결. 현재 DB에 is_story=true 게시물 없어 빈 상태 (스토리 등록 기능 미구현 — 별도 태스크 필요) |
| F-8 | 피드 게시물 등록 | 사용자가 직접 사진+캡션으로 피드 게시물을 작성할 수 있는지 | ⛔ 미구현: 현재 라이딩 결과 화면에서 "피드에 공유"로만 게시 가능. 독립 게시 UI 없음 — 별도 태스크 필요 |

> **⚠️ 미구현 기능 메모 (후속 태스크)**
> - **스토리 등록**: 카메라/갤러리 촬영 → `POST /api/bff/contents/upload` → `POST /api/bff/feed` (`is_story=true`) → 스토리 strip 즉시 반영. 진입점: TopBar 📷 버튼 또는 스토리 strip의 "내 스토리" 터치.
> - **피드 게시물 등록**: 사진 선택 + 캡션 + 해시태그 입력 → `POST /api/bff/contents/upload` + `POST /api/bff/feed`. 진입점: TopBar 📷 버튼 또는 하단 FAB. BFF `POST /feed` 엔드포인트는 이미 구현되어 있어 프론트 UI만 필요.

---

### [PROFILE-001] 프로필 (`/profile`)

| # | 점검 항목 | 점검 방법 | 결과 |
|---|---|---|---|
| PR-1 | 닉네임/레벨/아바타 표시 | 로그인 유저 정보 노출 | |
| PR-2 | RP 잔액 표시 | Network탭 `GET /api/bff/profile/{id}/rp-balance` 호출 여부 | |
| PR-3 | 이번 달 통계 카드 | Network탭 `GET /api/bff/users/me/stats` 호출 여부 | |
| PR-4 | 탭 전환 (기록/배지/장비) | 탭 클릭 시 active 전환 | |
| PR-5 | 배지 클릭 → 상세 모달 | 배지 아이콘 클릭 → 모달 오픈 | |

---

### [SETTINGS] 설정 메인 (`/settings`)

| # | 점검 항목 | 점검 방법 | 결과 |
|---|---|---|---|
| S-1 | 설정 항목 목록 표시 | 알림/언어/계정 등 메뉴 노출 | |
| S-2 | 다크모드 토글 | 토글 클릭 → 테마 즉시 전환 + 새로고침 후 유지 | |
| S-3 | 로그아웃 | 로그아웃 클릭 → `/splash` 이동 + 재접속 시 로그인 안 된 상태 | |
| S-4 | 알림 설정 메뉴 → `/settings/notifications` | |
| S-5 | 언어 설정 메뉴 → `/settings/language` | |
| S-6 | 계정 관리 메뉴 → `/settings/account` | |

---

### [SET-ACCOUNT] 계정 관리 (`/settings/account`)

| # | 점검 항목 | 점검 방법 | 결과 |
|---|---|---|---|
| SA-1 | 계정 정보 표시 (전화번호/가입일) | 정보 노출 확인 | |
| SA-2 | 계정 탈퇴 버튼 | 클릭 → 위험 확인 모달 노출 여부 (실제 탈퇴는 하지 말 것) | |

---

## 🐛 보고 방법

점검 후 아래 형식으로 보고해주세요:

```
[화면ID] 항목번호: ✅ 정상
[화면ID] 항목번호: ❌ 문제: (증상 설명)
[화면ID] 항목번호: ⛔ N/A: (이유)
```

예시:
```
[HOME-001] H-1: ✅ 정상
[HOME-001] H-6: ❌ 문제: 추천 퀘스트 카드가 안 보임
[RIDE-ACTIVE] R1-1: ⛔ N/A: 데스크탑 브라우저라 GPS 없음
```

---

## 📝 발견된 사전 이슈 (점검 전 알려진 결함)

| 이슈 | 화면 | 상태 |
|---|---|---|
| `/home` TypeError (rewardItems.length) — `quests.ts` transformer 추가로 수정 완료 | HOME-001 | 🔧 수정됨 — H-1로 확인 필요 |
| OtpInput BFF login 미호출 (더미 검증) | AUTH-002 | ❌ 미수정 |
| ProfileSetup BFF PUT /profile 미호출 + rider_type 대소문자 | PROFILE-SETUP | ❌ 미수정 |
| ProfileSetup 닉네임 중복확인 API 미연동 | PROFILE-SETUP | ❌ 미수정 |
| 전체 mock → 실 API 전환 (VITE_USE_MOCK=false 리빌드) | 전체 | 🔧 수정됨 |
