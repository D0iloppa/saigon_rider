/** 지도 초기 중심(GPS 미확보 시 표시용) — 호치민 시내(1군) 근사값. 좌표 "저장" 용도로 쓰지 말 것. */
export const HCMC_DISPLAY_CENTER = { lat: 10.7769, lng: 106.7009 };

/** 서비스 지역 밖 위치 요청에 사용하는 앱 기본 동네(Bến Thành) 좌표. */
export const BEN_THANH_FALLBACK = { lat: 10.7716, lng: 106.6980 };
