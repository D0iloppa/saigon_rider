# 사이공라이더 서비스 플로우 검토 킷 — v2 스토리보드판 (2026-09-19)

> **이 문서는 확정된 설계가 아니라 검토·승인을 요청하는 제안서다.** 개발자가 전체 플로우를 스토리보드로 짜 왔지만, 이 설계가 틀렸을 수 있다 — 그래서 프레임 하나하나에 대표의 승인/수정/보류 판정이 필요하다. §B의 "제안"·"현재"·"당위" 같은 서술은 코드에서 실제로 확인한 사실이므로 그대로 두되(§7 참조), 그 사실 위에 올린 개선안 자체는 **전부 판정 대기(미승인) 상태**다. 아래 사용법을 그대로 따르면 대표가 이 파일 하나로 검토를 시작할 수 있다.
>
> **이 파일 하나가 검토의 전체 입력이다.** 어느 AI에 이 파일을 통째로 넘기고 §1 프롬프트를 붙이면, AI가 (1) 프레임 단위 단독 분석 → (2) 판정자 인터뷰 → (3) 판정문(§C) 작성까지 수행한다. 작성자는 판정문을 받아 §D 규칙대로 §B 스토리보드를 갱신하고 버전을 올린다. **승인된 프레임만 구현한다.**
>
> **v2에서 바뀐 것.** 판정자(대표)의 실제 피드백 3건(§B0-3)이 모두 "한 화면 안에서 요소가 어디에, 어떤 무게로, 떠 있는가 고정돼 있는가"에 대한 것이었다. 흐름 단위 카드로는 이 수준의 판정을 받을 수 없어, 모든 플로우를 **화면 × 상태 = 프레임** 단위 스토리보드로 다시 썼다. 프레임마다 영역별 요소 표와 각 요소가 그 자리에 있어야 하는 이유(당위성)를 적었다. 이유가 없는 요소는 "이유 없음"으로 정직하게 적었다.
>
> **09-20 추가 — 화면 샘플이 생겼다.** 이 킷의 프레임 66개(v3 기준)를 파싱해 렌더한 HTML 스토리보드가 `ai-docs/review/storyboard/index.html`에 있다. 그중 **59개 프레임에는 제안이 반영된 손그림 수준의 화면 샘플**(`ai-docs/review/storyboard/screens/<F-ID>__<FR-n>.html`, 390×844)이 함께 임베드돼 있어, 대표는 글이 아니라 이 화면을 보고 판정할 수 있다. 나머지 중 **6개 프레임은 화면 샘플도 자동 목업도 없다**(요소 표가 비어 있어서다) — `F-S3-03 FR-4`, `F-S5-01 FR-4`, `F-S6-01 FR-5`, `F-X-01 FR-2`, `F-X-02 FR-1`, `F-X-02 FR-2`. 취소·교착·신고·차단(X 레인) 전체가 여기 해당하며, 이 6개는 **판정 대상이 아니라 "아직 정의되지 않았다"는 정의 요청 대상**이다. 나머지 1개(`F-P-02 FR-5`)에는 요소 표에서 자동 생성된 폰 목업이 붙는다. 자세한 여는 법·활용법은 §0.
>
> **v3에서 들어온 것(2026-09-21).** v2는 중고거래축만 다루고 나머지를 OUT으로 선언했는데, 상용 서비스 플로우로서 6개 영역이 빠질 수 없다는 것이 확정됐다. 그래서 **9개 F-ID · 22개 프레임을 신설**했다 — A등급(거래 여정 직결, 깊게): 상대방 프로필 `F-P-01`(4) · 개인 프로필 허브 `F-P-02`(5) · 고객센터 `F-CS-01`(4)·`F-CS-02`(1). B등급(간접, 프레임·요소표까지): 커뮤니티 피드 `F-CM-01`(2) · 커뮤니티 그룹 `F-CM-02`(2) · 동네지도 `F-MP-01`(2) · 업체 공개상세 `F-BZ-01`(1) · 비즈니스 라운지 `F-BZ-02`(1). **기존 44개 프레임은 한 글자도 바뀌지 않았다**(화면 샘플 38개가 그대로 붙어 있다). 신설 22개에도 이후 손그림 화면 샘플을 붙였다(2026-09-21). 현재 66개 중 59개가 화면 샘플, 1개가 자동 목업, 6개가 정의 요청 대상이다.
>
> 근거 원문(작성자용, 검토 AI는 접근 불필요): `260918_service_flow_A_trade.md`, `260918_flow_inventory_{A_trade,B_comms,D_trust}.md`, `spec/service-concept-260726.md`. 코드 확인분은 각 프레임에 `파일:라인`으로 표기.

| 항목 | 값 |
|---|---|
| 킷 버전 | v4 |
| 범위 | 중고거래축 S0~S7 + X(종료) + N(전역 오버레이 정책) + **P(프로필 — 개인·상대) · CS(고객센터) · D(발견 업체축: 동네지도→업체 상세) · PT(파트너 라운지) · CM(커뮤니티 — 독립 여정)**. 홈·알림 인박스·채널 게시판 본문·그룹 채팅 관리·게임화 잔재 일괄 삭제·홈화면 위젯은 여전히 OUT(§B3) |
| 판정자(기본) | 대표. 작성자 대리 시 "가판정" 표기, 대표 확정 전 구현 금지 |
| 판정 단위 | **프레임**(F-ID 아래 FR-n). 플로우 전체 승인은 그 아래 프레임 전부 승인과 같다 |
| 라운드 | r0(이 파일) → r1(09-22 대면 또는 AI 인터뷰) → r2 … |
| 종료 조건 | 모든 프레임 상태가 `승인` 또는 `보류` |

---

## 0. 사용법 (사람용)

**역할.** 작성자(개발자)는 스토리보드를 정의·갱신하고 판정하지 않는다. 판정자(대표)는 프레임마다 `승인 / 수정(어떻게) / 보류(이유) / 모르겠다(나중에)`만 말하고 설계하지 않는다. 검토 AI는 분석·질문·기록만 하고 결정하지 않는다.

**대표가 시작할 때 그대로 쓰는 한 문장.**
```
이 킷(260919_service_flow_review_kit.md)을 검토해줘. §1 프롬프트대로 진행하고,
화면 샘플은 ai-docs/review/storyboard/index.html을 브라우저로 열어서 같이 보면서 판정할게.
```
이 한 문장 + 이 파일 하나면 AI가 §1 프롬프트를 시작할 수 있다. 별도로 코드나 다른 문서를 첨부할 필요 없다.

**화면 샘플(먼저 열어 볼 것).** `ai-docs/review/storyboard/index.html`을 브라우저로 열면 프레임 66개가 scene 단위로 나열되고, 그중 59개에는 제안이 반영된 화면 샘플(390×844)이, 1개(`F-P-02 FR-5`)에는 요소 표에서 자동 생성된 폰 목업이 바로 붙어 있다 — 프레임 ID(`F-S3-01 FR-2`처럼)로 페이지 안에서 찾으면 된다. 문서 서두에 적힌 **화면 샘플이 없는 6개**(F-S3-03 FR-4·F-S5-01 FR-4·F-S6-01 FR-5·F-X-01 FR-2·F-X-02 FR-1·F-X-02 FR-2)는 아직 요소 표 자체가 비어 있는 "정의 안 됨" 상태이므로, 이 6개는 승인/수정/보류 판정 없이 **"먼저 정의부터 해달라"고만 말하면 된다.** (스토리보드 재생성이 필요하면 `node ai-docs/review/storyboard/build.mjs` 한 줄.)

**한 라운드.**
```
킷 vN ─▶ 검토 AI: ① 프레임별 단독 분석(§A1·A2) ② 인터뷰(§A3) ③ 판정문(§C) ─▶ flow_review/verdict_rN.md
                                                                                  │
킷 vN+1 ◀─ 작성자: §D 규칙으로 §B 프레임 갱신 + 변경 로그 ◀────────────────────────┘
```

**절차.** ① §1 프롬프트를 복사해 AI에 붙이고 이 파일 전체를 첨부한다(화면 샘플은 대표가 직접 `storyboard/index.html`을 열어서 옆에 두고 본다 — AI에게 별도로 첨부할 필요 없다). ② AI의 질문에 판정자가 답한다(한 번에 한 질문). ③ AI가 §C 형식으로 출력한 판정문을 그대로 `ai-docs/internal/flow_review/verdict_r<N>.md` 파일로 저장한다. **대표는 이 파일 경로 하나만 개발자에게 전달하면 된다** — 내용을 따로 옮기거나 요약할 필요 없이, §C 표 자체가 "프레임 ID · 판정 · 수정 내용 · 근거"를 갖춘 작업 지시서다. 작성자는 이 파일을 §D 규칙대로 §B에 반영한다.

**왜 프레임 단위인가.** "불편하다"는 흐름이 아니라 **특정 화면의 특정 순간**에 대한 말이다. 프레임이 없으면 그 말이 어디에 걸리는지 기록할 수 없고, 다음 라운드에 같은 말을 다시 듣는다. 프레임이 있으면 "FR-3, 더보기 시트, 신고 아래 위치공유"처럼 좌표가 생긴다.

**66개를 한 번에 다 보지 않아도 된다 — 우선순위.** 판정자 기존 피드백(V1~V3)에 직결된 프레임과 대표 결정이 이미 필요하다고 표시된 프레임을 먼저 본다. 나머지는 나중 라운드로 미뤄도 된다.
- **먼저(1순위)**: F-N-01(전역 오버레이 정책, V2·V3), F-S3-01(V1), F-S3-03(V2), F-S5-01(V3), F-S6-01(대표 결정 C2), F-X-01(대표 결정 C1) — 화면 샘플 있는 것 기준 판정 우선.
- **나중(2순위)**: 나머지 F-S0/S1/S2/S4/S7 계열 — 개별 개선 제안 위주, 급하지 않음.
- **v3 신설(3순위, 이번에 처음 올라온 것)**: A등급 `F-P-01`·`F-P-02`·`F-CS-01`·`F-CS-02`(거래 여정 직결 — 프로필 허브·상대 신뢰 확인·사고 후 안전판) → B등급 `F-CM-01`·`F-CM-02`·`F-MP-01`·`F-BZ-01`·`F-BZ-02`. 이 22개는 손그림 샘플 없이 자동 목업으로 본다.
- **존폐 판정(배치 판정 아님)**: `F-CM-02 FR-1`(그룹 탐색이 앱 UI에서 도달 불가) — 요소를 어디 둘지가 아니라 기능을 살릴지부터 묻는다.
- **정의 요청(판정 아님)**: 화면 샘플 없는 6개(F-S3-03 FR-4 등, 위 목록).

**"모르겠다 / 나중에"도 정당한 답이다.** 판정자가 그 자리에서 결정하기 어려우면 그냥 "모르겠다" 또는 "나중에"라고 말하면 된다. 검토 AI는 이를 `보류(사유: 판단 보류)`로 기록하고 다음 프레임으로 넘어간다 — 억지로 셋 중 하나를 강요하지 않는다.

**대표가 설계를 대신 할 필요는 없다.** 판정자가 할 일은 승인/수정(어떻게)/보류(이유)/모르겠다 중 하나를 말하는 것뿐이다. "이렇게 고쳐라"의 구체적 실행(레이아웃 세부·문구·픽셀)은 작성자 몫이다. 판정자가 새 요소나 흐름을 언급하면 AI가 NEW-nn으로만 받아 적고 설계는 완성하지 않는다(§1 절차 참조).

---

## 1. 검토 AI 실행 프롬프트 (이 블록을 그대로 복사해 AI에 붙인다)

```text
당신은 "사이공라이더 서비스 플로우 검토 AI"다. 첨부된 킷(260919_service_flow_review_kit.md)이 유일한 텍스트 입력이다. 코드 저장소에는 접근하지 않으며 파일:라인 인용은 작성자용 근거이므로 검증을 요구하지 않는다.

[화면 샘플에 대한 이해 — 당신이 직접 열 필요는 없다]
- 이 킷의 프레임 66개 중 59개는 `ai-docs/review/storyboard/index.html`에 화면 샘플(390×844 HTML)이, 1개(`F-P-02 FR-5`)에는 요소 표에서 자동 생성된 폰 목업이 붙어 있다. 판정자는 텍스트가 아니라 그 화면을 보면서 판정한다 — 당신은 요소 표를 낭독해 판정자가 화면과 맞춰볼 수 있게 돕는다.
- 화면 샘플이 **없는 6개**는 `F-S3-03 FR-4`, `F-S5-01 FR-4`, `F-S6-01 FR-5`, `F-X-01 FR-2`, `F-X-02 FR-1`, `F-X-02 FR-2`다. 이 6개는 요소 표 자체가 비어 있어 아직 정의되지 않았다 — **승인/수정/보류 판정을 요구하지 않는다.** 이 6개에서는 "이 프레임을 어떻게 정의하고 싶은지"만 받아 적고 (필요하면 NEW 항목으로) 다음으로 넘어간다.

[역할 경계]
- 분석하고, 묻고, 기록한다. 결정하지 않는다. 판정(승인/수정/보류)은 판정자가 말한 것만 기록한다.
- 판정자 = 대표. 대리 판정자가 답하면 판정문 헤더에 "가판정(작성자 대리)"이라고 쓴다.
- 판정 단위는 프레임(FR-n)이다. 플로우(F-ID) 단위로 "다 승인"이라고 하면 아래 프레임 전부에 같은 판정을 복사하되 "일괄"이라고 표기한다.
- 범위는 킷 헤더의 "범위" 행을 따른다. 범위 밖 언급은 OUT으로 기록만 한다.

[절차 — 순서대로, 건너뛰지 않는다]
0. 준비: §B2의 모든 F-ID와 그 아래 프레임 목록을 상태와 함께 출력한다. 이미 `승인`이고 변경 로그에 이후 변경이 없는 프레임은 재검토하지 않는다(판정자가 요청하면 예외). §0의 우선순위 그룹(1순위/2순위/정의 요청)을 그대로 따르되, 판정자가 순서를 바꾸고 싶다고 하면 그에 맞춘다. §B0-3의 피드백 샘플 3건을 읽고, 각 피드백이 어느 프레임에 대응하는지 표로 확인한다 — 인터뷰에서 이 3건은 반드시 해당 프레임에서 "이 제안이 그때 짚었던 문제를 푸는가"를 묻는다.
1. 프레임별 단독 분석: 각 프레임에 §A1 공통 질문을 적용해 표로 답한다. 요소 표의 "왜 여기" 열이 "이유 없음"인 요소는 전부 골라 목록으로 낸다. 당신의 의견(동의/이견/대안)을 적되, 대안은 반드시 (a) 이 앱에 이미 있는 패턴(§B0-4 목록) 또는 (b) §B0-2 레퍼런스의 관행 중 하나에 연결한다. 연결 못 하는 대안은 내지 않는다. 이 단계는 판정자에게 보여주기 전에 완성한다. **화면 샘플이 없는 6개 프레임(§0 참조)은 요소 표가 비어 있으므로 이 분석을 건너뛰고 "정의 필요" 목록에만 올린다.**
2. 인터뷰: 1순위 그룹 → 2순위 그룹 → 정의 요청 6개 순서로 진행한다(§A0은 맨 처음 한 번). 한 번에 한 질문만. 질문은 §A3에서 고른다. **화면 샘플이 있는 프레임은 판정자가 `storyboard/index.html`의 해당 화면을 보고 있다고 가정하고 진행한다.** 판정자가 그 프레임에 `승인/수정/보류/모르겠다(나중에)` 중 하나를 말하기 전에는 다음 프레임으로 넘어가지 않는다. 정의 요청 6개는 승인/수정/보류 대신 "정의 요지"만 받아 적고 다음으로 넘어간다.
   - "불편하다/이상하다/복잡하다/난잡하다"처럼 좌표 없는 답이 나오면 즉시 §A3의 분해 질문 3개를 순서대로 묻는다: 이 프레임의 어느 영역(헤더/컨텍스트/본문/하단/떠 있는 것)에서 / 사용자가 무엇을 기대하는데 / 실제로 무엇이 보이거나 일어나서.
   - 판정자가 새 요소·새 흐름을 말하면 NEW-nn 임시 ID를 붙이고 목표·트리거·종료·어느 영역인지만 받아 적는다. 설계를 대신 완성하지 않는다.
   - 판정자가 "모르겠다" 또는 "나중에"라고 답하면 즉시 `보류(사유: 판단 보류)`로 기록하고 다음 프레임으로 넘어간다. 재질문하지 않는다.
   - 답이 모호하면 재질문은 1회. 그래도 모호하면 `보류(사유: 답 불명)`.
   - 이유 없이 판정하면 "이유는 선택입니다. 없으면 '직관'으로 적겟습니다"라고 알리고 근거 유형을 `판정자 직관`으로 기록한다. 이유를 만들어 넣지 않는다.
3. 판정문 작성: §C 스키마의 제목·표 열을 그대로 쓴다. 열을 빼거나 합치지 않는다. 판정자 답변은 뜻이 바뀌지 않게 원문에 가깝게 쓴다.
4. §C-8 자체 점검을 채운 뒤에만 판정문을 출력한다.

[금지]
- 코드·구현 방법을 논하지 않는다(설계 수준에서만).
- 판정자 대신 판정하거나, 판정자의 '수정'에 당신의 대안을 섞어 쓰지 않는다. 이견은 별도 열에만.
- 여러 질문을 한 번에 묻지 않는다.
- 인터뷰 중 판정자를 설득하지 않는다. 단독 분석 의견은 판정자가 물을 때만 말한다.

[시작]
"준비" 출력 → "피드백 샘플 3건 ↔ 프레임 대응표" → "프레임별 단독 분석" 표(화면 샘플 없는 6개는 "정의 필요" 목록으로 별도 표기) → "지금부터 인터뷰를 시작합니다. 먼저 서비스 전체에 대한 질문 하나입니다."로 §A0 첫 질문.
```

---

## A. 검토 질문

### A0. 서비스 전체 (라운드마다 한 번)

| # | 질문 | 목적 |
|---|---|---|
| A0-1 | 이 앱이 하는 일을 한 문장으로 말하면? §B0-1 정의 문장과 같은가, 다르면 어디가 다른가? | 척추 확인 |
| A0-2 | 거래 1건이 앱 안에서 끝나는 데 §B1 단계 중 없어도 되는 단계, 빠진 단계가 있는가? | 단계 구조 승인 |
| A0-3 | S6 내부 순서 "만나서 확인 → 결제 → 전달"을 서비스 원칙으로 확정하는가?(대표 결정 C2) | 최상위 규칙 확정 |
| A0-4 | 당근마켓과 같아야 하는 지점과 달라야 하는 지점을 각 하나씩. | 레퍼런스 대비 위치 |
| **A0-5** | **화면 위에 떠서 움직이는 요소(무전기 캡슐·위치공유 버블)를 서비스가 허용하는가?** 허용한다면 어떤 조건에서, 허용하지 않는다면 그 기능은 어디에 고정되어야 하는가? | §B0-3 피드백 2·3의 원칙화. F-N-01 판정 기준 |

### A1. 모든 프레임 공통 (프레임마다)

| # | 질문 | 판정에 쓰는 방식 |
|---|---|---|
| Q1 | 이 프레임에서 사용자가 해야 할 **주행동은 하나**인가? 둘 이상이면 어느 것이 진짜인가? | 둘 이상이면 위계 문제 |
| Q2 | 이 프레임을 마친 사용자가 **다음에 무엇을 해야 하는지** 화면만 보고 아는가? | 모르면 진입로 문제 |
| Q3 | 되돌릴 수 없거나 상대에게 즉시 영향이 가는 요소에 **확인 단계**가 있는가? 같은 행동이 다른 프레임에서 다른 마찰을 갖는가? | 안전 문제 |
| Q4 | **같은 영역·같은 모양으로 묶인 요소들이 같은 성격**인가?(진행 vs 종료, 안전 vs 즉시효과, 가역 vs 비가역) | 섞여 있으면 §B0-3 피드백 1과 같은 오해 발생 |
| Q5 | **떠 있는 요소**가 있다면, 고정된 자리로 옮겼을 때 잃는 것이 있는가? 없다면 왜 떠 있는가? | §B0-3 피드백 2·3 |
| Q6 | 화면이 제시하는 순서가 **실제 대면 거래의 순서**와 같은가? | S6 원칙 |
| Q7 | 이 요소가 없어도 거래가 끝나는가? 끝난다면 왜 있는가? | 존재 이유 |
| Q8 | 당근마켓·Chợ Tốt과 다르게 만든 지점이 있다면 **왜**, 그 이유가 §B0-1 전제에서 나오는가? | 근거 없는 차이는 수정 후보 |
| Q9 | **판정자가 사용자라면** 이 프레임에서 제안된 행동을 하겟는가? 아니면 무엇을 먼저 찾겟는가? | 사용자 시점 진술 |
| Q10 | 이 프레임이 잘못 작동하면 사용자에게 **실제로 무슨 일**이 일어나는가(돈·시간·관계)? | 우선순위 |

### A2. 플로우별 개별 질문

| F-ID | 질문 |
|---|---|
| F-N-01 | 무전기 캡슐과 위치공유 버블이 **떠다니고, 드래그되고, 서로 겹칠 수 있는** 현재를 받아들이는가? "하단 고정"이라면 채팅방 안에서는 입력창 위, 다른 화면에서는 탭바 위인가? 둘이 동시에 활성일 때 어떻게 보여야 하는가? |
| F-S0-01 | 거래 희망 장소가 폼 최하단인 것이 맞는가? 필수 게이트가 맞는가? |
| F-S0-02 | 철회(비가역)가 끌올·가격수정과 같은 줄·모양이어도 되는가? 끌올이 접힌 곳에 있어도 되는가? |
| F-S1-01 | 구매자 카드 탭과 판매자 글쓰기 FAB가 동시에 주행동인 것을 받아들이는가? 키워드 알림이 헤더 상시 슬롯을 차지할 빈도인가? |
| F-S1-02 | 찜 목록에서 해제가 가능해야 하는가? |
| F-S2-01 | 판매자 신뢰 블록 옆 게임 레벨 배지를 없애는가? 팔로우가 프로필 링크와 같은 무게여야 하는가? |
| F-S3-01 | 헤더 [뒤로][프로필][무전기][더보기]가 같은 무게인 것이 맞는가? **더보기에 [신고][위치공유][나가기]가 같이 있어도 되는가?**(피드백 1) |
| F-S3-02 | 가격 제안이 채팅보다 약한 무게인 것이 맞는가? |
| F-S3-03 | **무전기는 언제 보이고, 누가 시작하고, 어떻게 닫히길 기대하는가? 떠다니는 캡슐이 맞는가, 하단 고정이 맞는가?**(피드백 2) 홈·채팅 목록 진입 버튼이 필요한가? 아이콘은 하나로? |
| F-S4-01 | 약속 잡기가 "+" 메뉴 안에 이모티콘과 나란히 있어도 되는가? 약속이 열렸다는 것을 사용자가 알아야 하는가? |
| F-S5-01 | 확정 약속을 한 탭으로 취소해도 되는가? 상대가 취소했을 때 푸시 없이 되는가? **위치공유가 떠다니는 버블이어야 하는가?**(피드백 3) |
| F-S6-01 | **만나기 전에 "송금했어요"를 누를 수 있어야 하는가?** 타임라인 순서를 받아들이는가? 판매자 "입금 확인"에 확인 단계가 없어도 되는가? QR 등록의 무게는? |
| F-S7-01 | 판매자 "거래 완료 처리"를 한 탭으로 해도 되는가? |
| F-S7-02 | 거래 이력에서 행을 누르면 어디로 가야 하는가? |
| F-X-01 | 송금 신고 뒤 아무도 되돌릴 수 없는 상태를 받아들이는가? 운영자 되돌림 권한을 주는가?(C1) |
| F-X-02 | 신고 진입점이 화면마다 달라도 되는가? 매물 없는 상대를 차단할 수 없어도 되는가? (이번 라운드 기록만) |

### A3. 판정자 인터뷰 질문 (검토 AI가 실제로 묻는 문장)

**프레임마다 이 순서로, 한 번에 하나.**
1. "F-〈ID〉 FR-〈n〉 〈이름〉입니다. 지금 화면에 이런 요소들이 이 자리에 있습니다(요소 표 낭독). 이 순간 사용자가 해야 할 행동이 하나라면 무엇이라고 보십니까?"
2. "대표님이 사용자로서 이 화면을 보면 어디를 먼저 누르시겟습니까? 제안된 주행동과 같습니까?"
3. (§B0-3 피드백에 대응하는 프레임이면) "이 프레임의 제안이 그때 짚었던 '〈피드백 요지〉' 문제를 푼다고 보십니까?"
4. (§A2 개별 질문 1~2개)
5. "판정을 주십시오. 승인 / 수정(어떻게) / 보류(이유)."

**좌표 없는 답("불편·이상·복잡·난잡")이 나오면 즉시, 순서대로:**
- ① "이 프레임의 어느 영역입니까 — 헤더, 컨텍스트, 본문, 하단, 떠 있는 것 중에서?"
- ② "그 자리에서 사용자가 무엇을 기대한다고 보십니까?"
- ③ "실제로는 무엇이 보이거나 일어나서 그 기대와 다릅니까?"
- → "그러면 수정은 '〈영역〉에서 〈기대〉가 되도록 〈요소〉를 〈어떻게〉'로 적겟습니다. 맞습니까?"

**새 요소·흐름:** "목표, 시작되는 순간, 끝나는 순간, 어느 영역에 있어야 하는지만 말씀해 주십시오. NEW-〈nn〉으로 적고 설계는 작성자가 합니다."

**마무리:** "오늘 판정 중 파일럿 숫자를 보고 다시 보고 싶은 프레임이 있습니까?" → `보류(파일럿 후)` 또는 `승인(재검토 조건)`.

---

## B. 스토리보드

### B0. 전제·어휘·피드백 샘플

#### B0-1. 서비스 정의와 확정 전제
**한 문장.** 호치민 라이더가 오토바이·용품을 **동네에서 대면으로** 사고팔 수 있게, 발견에서 만남·결제·완료까지를 한 앱에서 중개한다.

**확정 전제(판정 기준. 바꾸려면 대표 결정).** ① 대면 동시교환 — 택배·원격 경로 없음, 물건 확인 후 결제. ② 플랫폼은 결제를 검증하지 않는다 — ZaloPay QR은 현금 대체, 판매자가 자기 앱에서 입금 확인(에스크로 아님). ③ 개인은 휴대폰 인증, 업체는 계정 승인 후 출품. ④ 게임화는 의도적 폐기 — 거래축 화면의 잔재는 제거 대상.

#### B0-2. 레퍼런스
- **당근마켓**: 직거래+채팅+약속 모델의 기준. 채팅방 헤더는 [뒤로][상대][더보기] 셋. 결제 미검증. 진행 중 상태는 채팅방 **상단 고정 배너**로 표시.
- **Chợ Tốt**: 베트남 리스팅 중심, 만남 중개 약함 — 우리가 채우는 빈칸(합의~이동).
- **모바일 OS 관행(떠 있는 요소의 기준)**: iOS 통화 중·내비 중 표시는 **상단 고정 바**(움직이지 않음). Android 진행 중 세션은 **고정 알림/하단 시트**. 드래그되는 버블은 Messenger 채팅헤드처럼 **앱 밖(다른 앱 위)**에서 쓰는 패턴이고, 앱 안에서 자기 화면 위에 띄우는 예는 드물다.
- **우리만의 조건**: 오토바이는 고가·이동수단 → **S5 이동이 길고 손이 바쁘다**(운전 중). 음성·위치·길안내는 이 단계의 도구다. 손이 바쁠수록 **예측 가능한 고정 위치**가 유리하다.

#### B0-3. 피드백 샘플 — 원문 요지와 프레임 대응
이 세 피드백이 "스토리보드 수준"의 기준점이다. 검토 AI는 해당 프레임에서 반드시 되묻는다.

| # | 피드백이 가리키는 원칙 | 현재 사실(코드 확인) | 답하는 프레임 |
|---|---|---|---|
| **V1** | **같은 메뉴에는 같은 성격만.** 종료 행위(신고·나가기)와 진행 행위(위치공유)를 섞지 않는다 | 더보기 시트 3항목 [신고하기][위치 공유하기][나가기] 동일 클래스 텍스트 행, 나가기만 위험 색(`DmDetail.tsx:2281-2313`) | **F-S3-01 FR-2** |
| **V2** | **진행 중 도구는 고정 위치.** 떠다니는 것은 예측 불가 + 가림 + 오탭 | 캡슐은 `position: fixed` + 자유 드래그(스냅 없음), 앱 전역 마운트(`App.tsx:574`), 모든 화면 위에 존재(`WalkieTalkieFloatingButton.module.css:3-9`) | **F-N-01 FR-1·2**, **F-S3-03 FR-2·5** |
| **V3** | 위와 동일 + **떠 있는 것이 둘이면 겹친다** | 위치공유 버블도 `position: fixed` + 자유 드래그, 전역 마운트(`App.tsx:576`), 무전기와 같은 z-index 60, "마지막에 터치한 쪽이 위"(`.module.css:9` 주석) | **F-N-01 FR-1·2**, **F-S5-01 FR-3·4** |

**피드백에서 도출한 원칙 두 개(스토리보드 전체에 적용).**
- **P-1 같은 묶음 = 같은 성격.** 한 메뉴·한 줄·한 영역에 놓인 요소들은 진행/종료, 안전/즉시효과, 가역/비가역 중 같은 쪽이어야 한다.
- **P-2 떠 있는 요소 0을 기본값으로.** 진행 중 세션(무전기·위치공유)은 고정된 영역(채팅방: 입력창 위 / 그 외: 탭바 위)에 **고정 바**로 표시한다. 떠 있어야 할 이유가 증명될 때만 예외.

#### B0-4. 표기 규약

**영역(Zone).** `H` 헤더 · `C` 컨텍스트(헤더 아래 고정: 매물 카드·진행 배너·후기 배너) · `B` 본문(스크롤) · `K` 하단(입력창·하단 바·FAB) · `O` 오버레이(떠 있는 것·시트·모달·다이얼로그).

**무게.** `주행동`(화면당 하나, 채움) · `보조`(아웃라인·칩·헤더 아이콘) · `숨김`(더보기·+메뉴·롱프레스·스와이프) · `파괴적`(위험 톤 + 확인 1회 필수) · `표시`(액션 아님).

**성격 태그.** `진행`(거래를 앞으로) · `종료`(취소·신고·차단·나가기) · `이동`(화면 전환만, 안전) · `즉시`(탭 즉시 상대에게 신호가 감) · `가역`/`비가역`.

**F-ID 접두사 체계(v3에서 확정).** 거래 여정 축은 단계 기호를, 그 밖의 축은 영역 기호를 쓴다. 두 체계는 겹치지 않는다.

| 접두사 | 축 | 뜻 | 해당 F-ID |
|---|---|---|---|
| `F-S<n>-` | 거래 여정 | S0~S7 단계 화면 | `F-S0-01` ~ `F-S7-02` |
| `F-X-` | 거래 여정 | 종료(취소·교착·신고·차단) | `F-X-01`, `F-X-02` |
| `F-N-` | 횡단 | 셸·전역 오버레이 정책 | `F-N-01` |
| `F-P-` | 프로필 | 상대방 프로필 / 개인 프로필 허브 | `F-P-01`, `F-P-02` |
| `F-CS-` | 고객센터 | 문의·신고 접수 이후(조회·취소·추적) | `F-CS-01`, `F-CS-02` |
| `F-MP-` | 발견(업체축) | 동네지도·지도 검색 | `F-MP-01` |
| `F-BZ-` | 업체·파트너 | 업체 공개상세 / 비즈니스 라운지 | `F-BZ-01`, `F-BZ-02` |
| `F-CM-` | 커뮤니티(독립 여정) | 피드 / 그룹 | `F-CM-01`, `F-CM-02` |

**프레임 표기.** `F-<접두사>-<nn> FR-<n> <이름>` — 조건(어느 사용자·어느 상태) → 와이어(영역별 한 줄) → 요소 표 [영역 | 요소 | 무게 | 성격 | 탭하면 | 왜 여기(당위성)] → 이 프레임의 주행동 → 제안(현재와의 차이) → 실패 시나리오 → 근거 → 판정 ☐.

**"왜 여기" 열의 정직성 규칙.** 이유가 여정·전제·레퍼런스에서 나오지 않으면 **"이유 없음"** 또는 **"관성(이전 배치 유지)"**이라고 쓴다. 검토 AI는 이 값들을 전부 골라낸다.

**앱 안에 이미 있는 패턴(대안을 낼 때 여기 연결).** 상단 고정 진행 배너(거래 배너) · 확인 다이얼로그(`useConfirmStore`, 3곳 사용) · 카드 안 채움 버튼(약속·제안 카드) · 하단 바(매물 상세 [찜][제안][채팅]) · 접힘 액션 열(판매자 상세) · 위험 톤 메뉴 행(나가기) · 세그먼트 칩(판매중/예약중) · 빈 상태 CTA 3종.

**상태값.** `제안` · `승인(rN)` · `수정반영(rN, 재판정)` · `보류(사유)` · `OUT`. 구현 착수는 `승인`만.

### B1. 여정 지도

| 단계 | 사용자 목표 | 지배적 불안 | 성공 신호 | 이 단계가 표면화되는 영역 |
|---|---|---|---|---|
| **S0 출품** | (판매자) 빨리 빠짐없이 올리고 노출 유지 | "사진이 안 올라가면", "장소를 어디로" | 등록 완료 → 상세 | 등록 폼, 판매자 상세 |
| **S1 발견** | (구매자) 동네·예산 안의 물건을 훑는다 | 낮음 | 카드 탭 | 마켓 첫 화면 B, 검색 |
| **S2 판단** | 물건·가격·**사람**을 보고 연락할지 정한다 | "이 사람 믿어도 되나" | 채팅/제안/찜 | 매물 상세 B·K |
| **S3 문의** | 묻고 가격을 조율한다 | "답 올까" / "진짜 살 사람인가" | 첫 왕복, 제안 수락 | DM B·K |
| **S4 합의** | 언제·어디서 만날지 확정 | "실제로 나올까", "장소가 안전한가" | 약속 확정(매물 예약중) | DM C(매물 카드)·B(약속 카드) |
| **S5 이동** | 약속 장소로 간다, 상대가 오는지 안다 | "늦나", "어디쯤", **손이 바쁨** | 도착 | DM C(진행 배너)·B(약속 카드)·**고정 도구 바** |
| **S6 만남·교환** | **확인 → 결제 → 전달** | "물건이 다르면 돈만 날린다" / "입금 알림이 가짜면" | 수령 확인·완료 | 거래 화면 |
| **S7 완료·사후** | 후기·이력 | 안도 또는 분쟁 잔여감 | 후기 제출 | DM C(후기 배너), 이력 |
| **X 종료** | 어느 단계에서든 빠져나온다 | "빨리 끊고 싶다" | 2탭 이내 + 확인 1회 | 더보기 시트, 위험 톤 버튼 |
| **N 셸·오버레이** | 뒤로·정렬·필터, 진행 중 세션 표시 | 없음 / "어디 갔지" | 항상 같은 자리 | H, 고정 도구 바 |
| **P 프로필(상시 허브·횡단)** | (내) 내 매물·거래이력·파트너로 갈라진다 / (상대) 이 사람을 판단한다 | "내가 뭘 해야 하지" / "이 사람 믿어도 되나" | 거래 축으로 분기 / DM 시작 | `/profile` 시트, `/profile/:userId` B |
| **CS 고객센터(안전판·횡단)** | 사고·분쟁을 사람에게 넘긴다 | "돈만 날렸다", "접수가 된 건가" | 티켓 접수·운영자 답변 확인 | `/settings/support` C·B, 문의 상세 |
| **D 발견(업체축)** | 동네 업체를 지도로 찾는다 | 낮음 | 업체 상세 진입 | `/map` C·B, `/biz/:id` B·K |
| **PT 파트너(업체 운영축)** | 업체 운영 작업을 처리한다 | "반려됐나", "미답변이 몇 건" | 우선조치 해소 | `/biz/manage` C·B |

**P·CS·D·PT가 거래 여정에 붙는 자리(직결 3개 + 업체축).**
- **P 상대방 프로필(`F-P-01`)** = **S2(판단)의 신뢰 확인 경로**다. 킷은 `F-S2-01`("이 사람 믿어도 되나의 답")과 `F-S3-01`("대화 중 이 사람 누구지 재판단")에서 이 화면을 필요하다고 적어놓고 "이동 대상"으로만 취급했다. v3에서 S2의 하위 화면으로 정식 편입한다. S3에서도 같은 화면으로 돌아온다(재판단).
- **P 개인 프로필(`F-P-02`)** = **거래 여정의 상시 허브**다. S0(내 매물 관리 → `F-S0-02`)·S7(거래이력 → `F-S7-02`, 후기 제출)·파트너 라운지(`F-BZ-02`) 진입이 전부 이 한 화면에서 갈라진다. 단계가 아니라 **모든 단계에서 되돌아오는 자리**이므로 횡단 축으로 둔다.
- **CS 고객센터(`F-CS-01`·`F-CS-02`)** = **S6(만남·교환) 실패·사기 지점과 X(취소·교착)의 안전판**이다. 거래가 정상 종료하지 못했을 때 사용자가 마지막으로 가는 곳. X 레인의 신고(`F-X-02`)는 **접수 지점**을, CS는 **접수 이후**(조회·취소·추적·문의)를 맡는다 — 겹치지 않는다.
- **D 발견 업체축(`F-MP-01`→`F-BZ-01`)** = **S1(매물 발견)과 나란한 두 번째 발견축**이다. 종착지가 매물이 아니라 업체이고 성공 신호가 업체 문의(채팅/전화)다. 업체 상세의 매물 카드를 누르는 순간 거래축 S2로 합류한다 — 그 지점이 두 축의 유일한 합류점이다.
- **PT 파트너(`F-BZ-02`)** = 개인 거래 여정과 단계를 공유하지 않는 **역할 축**이다. 진입은 P(개인 프로필)에서만 갈라진다.

**CM 커뮤니티 — 별도 여정(거래 여정에 붙이지 않는다).** 거래 화면(S0~S7·X) 어디에서도 피드·그룹을 참조하지 않는다. 연결점은 **단 하나, 타인 프로필(`F-P-01 FR-1`)의 게시물 레일**뿐이다. 따라서 S0~S7 어디에도 끼워넣지 않고 독립 축으로 세운다.

| 단계 | 사용자 목표 | 지배적 불안 | 성공 신호 | 이 단계가 표면화되는 영역 |
|---|---|---|---|---|
| **CM1 둘러보기** | 동네·친구·핫 글을 훑는다 | 없음 | 카드 탭·응원 | `/feed` C(필터)·B |
| **CM2 반응·대화** | 글에 댓글·응원으로 붙는다 | "아무도 안 보나" | 댓글 등록 | `/feed/post/:id` B·K |
| **CM3 생산** | 내 글을 올리고 고친다 | "지우고 싶은데" | 게시 완료 | `/feed/new`, 수정은 `/profile` 내 피드 탭 |
| **CM4 모임** | 그룹에 들어가 게시판·채팅을 쓴다 | — | 가입·글쓰기 | `/group/:slug` C·B·O — **단, 그룹 탐색 진입점 자체가 없다**(`F-CM-02 FR-1`) |

**하나의 원칙.** 헤더에는 단계 불변·안전(이동)한 것만. 단계 도구는 그 단계가 표면화되는 자리에. 진행과 종료를 한 묶음에 섞지 않는다(P-1). 진행 중 세션은 고정 영역에(P-2).

---

### B2. 플로우별 스토리보드

---

#### F-N-01 전역 오버레이(떠 있는 것) 정책 — V2·V3 직결
- **단계** N(횡단) · **상태** 제안 · **적용 대상** 무전기 캡슐, 실시간 위치공유 버블

##### FR-1 현재: 두 개의 드래그 버블
- **조건** 무전기 채널 활성 또는 위치공유 채널 활성. 어느 화면이든.
- **와이어** `O`: 화면 임의 위치에 캡슐(무전기, 접힘 시 pill / 펼침 시 상태·파형·닫기) + 원형 버블(위치공유, 참여자 수 배지). 둘 다 손가락으로 아무 데나 끌 수 있음. 겹치면 마지막에 만진 것이 위.
- **요소 표**

| 영역 | 요소 | 무게 | 성격 | 탭하면 | 왜 여기 |
|---|---|---|---|---|---|
| O | 무전기 캡슐(접힘) | 보조 | 진행·즉시(탭=녹음 시작/정지·전송) | 녹음 시작 / 녹음 중이면 정지·전송 | B0-2 OS 관행상 드래그되는 버블은 Messenger 채팅헤드처럼 **앱 밖**에서 쓰는 패턴이고 앱 안에서 자기 화면 위에 띄우는 예는 드묾. B1 S5는 "손이 바쁠수록 예측 가능한 고정 위치가 유리"하다고 명시 — "이동 중 한 손으로 즉시"라는 요구는 정당하나 그것이 "떠다녀야 한다"를 도출하지는 않음 |
| O | 캡슐 더블탭 | 숨김 | 표시 토글 | 펼침/접힘 | 이유 없음 — 온보딩 없이 발견 불가한 숨겨진 제스처이며, B0-4 "앱 안에 이미 있는 패턴" 목록에 이에 대응하는 사례가 없음 |
| O | 캡슐 롱프레스 메뉴 [대화방으로 이동][채널 변경][초대장 다시 보내기][홈화면 고정(Android)] | 숨김 | 이동·진행 | 각 항목 | 메뉴 자체는 적정. "홈화면 고정"은 앱 밖 위젯 기능 — OUT |
| O | 캡슐 닫기(X) | 보조 | 종료 | 캡슐 닫힘(채널 이탈 여부 `[확인]`) | **펼친 상태에서만 보임** — 닫는 법을 알려면 더블탭을 알아야 함. 이유 없음 |
| O | 위치공유 버블 | 보조 | 진행 | 위치공유 모달 열기 | 무전기 캡슐과 같은 이유로 근거 없음 — 전역 마운트·자유 드래그·같은 z-index를 공유해 겹침이 발생(V3), B0-2 OS 관행(드래그 버블=앱 밖 패턴)과 P-2(진행 중 세션은 고정 영역) 모두에 위배 |
| O | 드래그(둘 다) | — | — | 위치 이동(스냅 없음) | 이유 없음 — 가리는 것을 피하려면 끌라는 발상인데, B1 S5는 "손이 바쁠수록 예측 가능한 고정 위치가 유리"하다고 명시해 정반대 방향. 스냅 없는 자유 드래그를 정당화할 근거가 없음(P-2 위반) |
- **주행동** 없음(이 요소들은 다른 화면의 주행동 위에 얹힘). 그것이 문제.
- **실패 시나리오** 매물 상세 하단 바 [채팅하기] 위에 캡슐이 내려와 있어 못 누름 → 끌어야 함. 무전기와 위치공유가 동시에 켜지면 하나가 하나를 가림. 운전 중 캡슐을 찾으려고 화면을 훑음.
- **근거** `WalkieTalkieFloatingButton.module.css:3-9`, `LiveLocationFloatingButton.tsx:44-90`, `App.tsx:574-576`.
- **판정(r1)** ☐

##### FR-2 제안: 하단 고정 "진행 중 바"
- **조건** 무전기 또는 위치공유 채널 활성. 둘 다면 한 바에 두 칸.
- **와이어** `K` 바로 위(채팅방: 입력창 위 / 그 외 화면: 탭바 위)에 높이 고정 바 1줄: `[● 무전기 · 채널명 · (탭=말하기) ] [📍 위치공유 · 3명 · (탭=지도) ] [닫기]`. 드래그 없음. 화면 전환해도 같은 자리. 다른 화면의 주행동을 가리지 않도록 본문 하단 여백을 바 높이만큼 확보.
- **요소 표**

| 영역 | 요소 | 무게 | 성격 | 탭하면 | 왜 여기 |
|---|---|---|---|---|---|
| K(고정 바) | 무전기 칸: 상태 점 + 채널명 + 말하기 버튼(누르는 동안 녹음 = PTT) | 보조 | 진행·즉시 | 누르고 있는 동안 녹음, 떼면 전송 | 손이 바쁜 사용자에게 **항상 같은 자리**가 "한 손 즉시"의 진짜 조건. iOS 통화 바·내비 바와 같은 관행(B0-2) |
| K(고정 바) | 위치공유 칸: 아이콘 + 참여자 수 | 보조 | 진행 | 위치공유 모달 | 같은 성격(진행 중 세션)끼리 한 묶음(P-1). 겹침 문제 소멸 |
| K(고정 바) | 닫기(각 칸 롱프레스 또는 우측 X → 확인 1회) | 파괴적(세션 종료) | 종료 | "무전기 채널에서 나갈까요?" → 나감 | 종료는 항상 보이되 확인을 거친다. 더블탭 뒤에 숨기지 않는다 |
| K(고정 바) | 바 본체 탭(빈 곳) | 이동 | 이동 | 그 채널의 대화방으로 | 롱프레스 메뉴 "대화방으로 이동"의 승격. 가장 잦은 후속 행동 |
| — | 채널 변경 / 초대장 다시 보내기 | 숨김 | 진행 | 바 롱프레스 메뉴 | 빈도 낮음. 숨김 자리 유지 |
- **주행동** 이 바가 있는 화면의 주행동은 그대로(가리지 않으므로).
- **제안 요약** 캡슐·버블 → 고정 바 1개. 드래그 제거. 더블탭 제거. 닫기 상시 노출+확인. 채팅방에서는 입력창 위, 나머지는 탭바 위. **판정자가 "하단 고정"이라 말한 것을 그대로 구조화한 안이며, 다른 위치(상단 배너)도 대안으로 열어 둠** — 상단은 이미 거래 진행 배너가 쓰고 있어 하단을 권고.
- **잃는 것(정직하게)** 펼침(peek) 시 보이던 파형·"○○님이 말하는 중" 표시 면적이 줄어듦 → 바 안에 한 줄 텍스트로 축약. 채팅방에서 입력창 위 한 줄만큼 메시지 영역이 줄어듦(활성일 때만).
- **실패 시나리오(제안 적용 시)** 없음이 목표. 남는 위험: 바가 두 칸일 때 좁은 화면에서 라벨 잘림 → 아이콘+숫자만.
- **근거** V2·V3, B0-2 OS 관행, 앱 내 패턴 "상단 고정 진행 배너"의 하단판.
- **판정(r1)** ☐

---

#### F-S0-01 매물 등록
##### FR-1 등록 폼(게이트 통과 판매자)
- **조건** 휴대폰 인증된 개인 또는 승인된 업체. 마켓 FAB 또는 내 매물 FAB로 진입.
- **와이어** `H` [뒤로] 제목 [**완료**] / `B` 사진 슬롯(첫 칸 +) → 카테고리 → 가격 + "가격 제안 받기" 토글 → 설명 → (오토바이) 서류 상태·번호판 지역 → **거래 희망 장소(필수)** / `K` 없음.
- **요소 표**

| 영역 | 요소 | 무게 | 성격 | 탭하면 | 왜 여기 |
|---|---|---|---|---|---|
| H | 완료(등록) | **주행동** | 진행 | 검증 → 등록 → 상세 | 폼의 목적. 헤더 채움은 앱 표준 |
| B | 사진 추가/재시도/삭제 | 보조 | 진행·가역 | 갤러리 / 재업로드 / 제거 | 필수 입력이 첫 자리. 실패 지점에 복구가 붙어 있음 |
| B | 가격 제안 받기 토글 | 보조 | 진행 | ON/OFF | S3의 문법을 판매자가 여기서 정함 → 가격 바로 아래 |
| B | 거래 희망 장소 | 보조(필수 게이트) | 진행 | 장소 선택 | S4 합의의 재료. **폼 최하단은 "가장 중요한 차별점 입력이 가장 아래"라는 순서 문제 — 관성** |
- **주행동** 완료. 당위: 화면 목적.
- **제안** 순서만 판정 요청: 거래 희망 장소를 가격 바로 아래로 올릴 것인가(무게는 그대로).
- **실패 시나리오** 장소를 대충 적으면 S4에서 채팅으로 다시 정함 → 차별점 소실.
- **근거** MC-1~11.
- **판정(r1)** ☐

##### FR-2 게이트 미충족(미인증 개인 / 미승인 업체)
- **조건** 인증·승인 전 사용자가 등록 진입.
- **와이어** `B` 상단에 사유 블록("휴대폰 인증 후 등록할 수 있어요" + [인증하기]) / 나머지 폼은 비활성 또는 미표시 `[현재 방식 확인]`.
- **요소 표**

| 영역 | 요소 | 무게 | 성격 | 탭하면 | 왜 여기 |
|---|---|---|---|---|---|
| B | 사유 문구 + 해결 CTA | **주행동** | 진행 | 인증 화면 | "제출 불가 사유 실시간 안내" 원칙. 막힌 이유와 푸는 길이 같은 자리 |
- **제안** 없음(원칙 확인용).
- **근거** MC-1 게이트 `postBlockReason`.
- **판정(r1)** ☐

---

#### F-S0-02 내 매물 관리(판매자 상세)
##### FR-1 판매중/예약중 판매자 뷰
- **조건** 내 매물, 상태 ON_SALE 또는 RESERVED.
- **와이어** `H` [뒤로] / `C` (결과 핑 있으면 배너, FR-3) / `B` 사진·정보 → 상태 세그먼트 [판매중|예약중] → 액션 열(접힘 기본, 쉐브론) → 펼치면 [끌어올리기][가격 수정][매물 철회] → 매물 수정 / `K` 찜 카운트·상태 바.
- **요소 표**

| 영역 | 요소 | 무게 | 성격 | 탭하면 | 왜 여기 |
|---|---|---|---|---|---|
| B | 상태 세그먼트 판매중↔예약중 | 보조 | 진행·가역 | 상태 전환 | 앱 밖 합의의 수동 예약. 왕복 가능하니 세그먼트가 맞음 |
| B | 끌어올리기 | 보조 | 진행·가역(쿨다운) | 노출 갱신 | **이유 없음 — 빈도 1위 행동(재방문 사유 1순위)이 접힘 뒤에 있음. 제안 ⓑ에서 격상** |
| B | 가격 수정 | 보조 | 진행·가역 | 시트 | 빈도 낮음. 접힘 뒤 적정 |
| B | 매물 철회 | **파괴적** | 종료·비가역 | 확인 시트 → 종료 | **끌올·가격수정과 같은 줄·같은 모양·위험 톤 없음 — P-1 위반.** 문구는 "되돌릴 수 없다" |
| B | 매물 수정 | 보조 | 진행 | 수정 폼 | 유지보수 |
- **주행동** 없음(관리 화면) 또는 끌어올리기. 판정 요청.
- **제안** ⓐ 매물 철회를 접힘 액션 열에서 분리해 열 최하단에 위험 톤(기존 "위험 톤 메뉴 행(나가기)" 패턴과 동일 톤)으로 배치, 확인 시트는 기존 `useConfirmStore` 재사용해 "되돌릴 수 없다" 확인 1회 필수화. ⓑ 끌어올리기를 접힘 액션 열에서 꺼내 `K` 하단 바(매물 상세 [찜][제안][채팅]과 동일한 상시 노출 하단 바 패턴)에 상시 노출 버튼으로 배치, 가격 수정만 접힘 액션 열에 남긴다.
- **실패 시나리오** 끌올 누르려다 철회 → 확인 시트를 관성으로 넘기면 매물 종료.
- **근거** MD-9~15, 불일치 #6 #8.
- **판정(r1)** ☐

##### FR-2 철회/만료 판매자 뷰
- **조건** WITHDRAWN 또는 EXPIRED.
- **와이어** `B` 상태 칩형 [**다시 올리기**] + 매물 수정.
- **요소 표**

| 영역 | 요소 | 무게 | 성격 | 탭하면 | 왜 여기 |
|---|---|---|---|---|---|
| B | 다시 올리기 | **주행동** | 진행 | 재게시 | 닫힌 매물을 보는 판매자의 유일 목적. 채움과 동급으로 읽히는지 실기기 확인 `[확인]` |
- **제안** 없음.
- **근거** MD-9b, MD-10.
- **판정(r1)** ☐

##### FR-3 거래 결과 확인 핑
- **조건** 약속 이후 결과 미확정, 판매자.
- **와이어** `C` 배너 "이 거래는 어떻게 됐나요?" 4버튼 [이 분께 판매][다른 곳에 판매][판매 포기][계속 판매].
- **요소 표**

| 영역 | 요소 | 무게 | 성격 | 탭하면 | 왜 여기 |
|---|---|---|---|---|---|
| C | 4지선다 | **주행동**(핑 있을 때) | 진행 / 그중 포기·타처는 비가역 | 결과 기록 | 핑이 떠 있는 동안 판매자가 온 이유는 이 질문. 배너 상단 정당. **비가역 두 개에 확인 유무 `[확인]`** |
- **제안** 비가역 선택지 2개에 확인 1회(확인 결과 없으면).
- **근거** MD-5, 4.1 보류.
- **판정(r1)** ☐

---

#### F-S1-01 발견(마켓 첫 화면·검색)
##### FR-1 리스트
- **조건** 탭바 마켓 진입, 결과 있음.
- **와이어** `H` [지역명▾] ······ [검색][키워드 알림][찜] / `B` 카테고리 칩 행 · 정렬 칩 → 카드 스크롤 / `K` 우하단 FAB(+) · 하단 중앙 [지도 보기] 필.
- **요소 표**

| 영역 | 요소 | 무게 | 성격 | 탭하면 | 왜 여기 |
|---|---|---|---|---|---|
| H | 지역 변경 | 보조 | 이동 | 지역 시트 | 동네 앱의 "어디"는 탐색 전제 |
| H | 검색 | 보조 | 이동 | 검색 화면 | 발견의 둘째 경로 |
| H | 키워드 알림 | 보조 | 이동 | 알림 설정 | **빈 결과 CTA가 같은 순간을 이미 제공. 상시 슬롯 빈도 아님 — 관성(낮음)** |
| H | 찜 목록 | 보조 | 이동 | 찜 목록 | 재방문 사용자의 잦은 행동 |
| B | 카드 | **주행동(구매자)** | 이동 | 상세 | 발견의 목적 |
| K | FAB 글쓰기 | **주행동(판매자)** | 진행 | 등록 | 예외 허용: 다른 역할, 스크롤 밖 고정이라 경쟁 없음 |
| K | 지도 보기 필 | 보조 | 이동 | 지도뷰 | "오토바이로 N분" 감각의 입구 |
- **제안** 키워드 알림 슬롯 정리 여부(낮음, 유지 무방).
- **근거** MM-1~20, 불일치 #7.
- **판정(r1)** ☐

##### FR-2 지도뷰
- **조건** 지도 보기 필 탭.
- **와이어** `H` 검색바(읽기 전용) · 카테고리 칩 / `B` 지도 + 핀 + 줌 힌트 필 / `K` 하단 캐러셀 카드 · [리스트로] 아이콘.
- **요소 표**

| 영역 | 요소 | 무게 | 성격 | 탭하면 | 왜 여기 |
|---|---|---|---|---|---|
| B | 핀/말풍선 | **주행동** | 이동 | 카드 포커스/상세 | 지도뷰의 콘텐츠 |
| K | 캐러셀 카드 | 주행동(동일 대상) | 이동 | 상세 | 핀과 같은 대상의 두 표현 — 경쟁 아님 |
| K | 리스트 복귀 | 보조 | 이동 | 리스트 | 되돌리기 |
- **제안** 없음.
- **근거** MM-7~13.
- **판정(r1)** ☐

##### FR-3 빈 결과
- **조건** 필터·지역 조건에 결과 0.
- **와이어** `B` 빈 상태 블록 + [전체 지역 보기][키워드 알림 등록][전체 카테고리].
- **요소 표**

| 영역 | 요소 | 무게 | 성격 | 탭하면 | 왜 여기 |
|---|---|---|---|---|---|
| B | 탈출구 3종 | 보조 | 진행 | 각 필터 해제 / 알림 등록 | 빈 결과의 후속 행동이 그 자리에. **키워드 알림의 본래 자리** |
- **제안** 없음.
- **근거** MM-16~18.
- **판정(r1)** ☐

##### FR-4 검색
- **조건** 검색 아이콘 탭.
- **와이어** `H` [뒤로] [키워드 입력 ×] / `B` 카테고리·가격대·정렬 칩 → 결과 카드.
- **요소 표**

| 영역 | 요소 | 무게 | 성격 | 탭하면 | 왜 여기 |
|---|---|---|---|---|---|
| H | 키워드 입력 | **주행동(입력 전)** | 진행 | 검색 | 화면 목적 |
| B | 가격대 칩 | 보조 | 진행 | 범위 시트 | 예산 제약 페르소나의 핵심 필터 |
| B | 결과 카드 | **주행동(입력 후)** | 이동 | 상세 | 시간축상 입력과 경쟁 안 함 |
- **제안** 없음.
- **근거** MS-1~8.
- **판정(r1)** ☐

---

#### F-S1-02 찜·키워드 알림
##### FR-1 찜 목록
- **조건** 찜 1건 이상.
- **와이어** `H` [뒤로] 찜한 매물 / `B` 카드(하트 표시 포함).
- **요소 표**

| 영역 | 요소 | 무게 | 성격 | 탭하면 | 왜 여기 |
|---|---|---|---|---|---|
| B | 카드 | **주행동** | 이동 | 상세 | 유예한 판단 재개 |
| B | 카드 안 하트 | 표시(현재) → 보조(제안) | 진행·가역 | **현재 아무 일 없음** → 제안: 해제 | 유예의 정리(해제)는 목록에서 훑으며 하는 행동. **표시 전용은 결함** |
- **제안** 하트 토글로 해제.
- **실패 시나리오** 지우려면 상세까지 들어가야 함 → 목록이 쌓임.
- **근거** MW-1, MW-(부재), `ListingCard.tsx:42-56`.
- **판정(r1)** ☐

##### FR-2 키워드 알림
- **조건** 키워드 알림 화면.
- **와이어** `B` 입력 + [추가] → 키워드 행(연필·X).
- **요소 표**

| 영역 | 요소 | 무게 | 성격 | 탭하면 | 왜 여기 |
|---|---|---|---|---|---|
| B | 추가 | **주행동** | 진행 | 등록 | 화면 목적 |
| B | 삭제 X | 보조 | 비가역(재입력 수준) | 즉시 삭제 | 손실이 "다시 타이핑" → 확인 불필요 적정 |
- **제안** 없음.
- **근거** MK-1~4.
- **판정(r1)** ☐

---

#### F-S2-01 판단(매물 상세, 구매자)
##### FR-1 구매자 뷰
- **조건** 타인 매물, ON_SALE/RESERVED.
- **와이어** `H` [뒤로] ······ [더보기] / `B` 사진 캐러셀 → 제목·가격·설명·(오토바이) 서류 상태 → **판매자 블록**: 아바타·닉네임·판매 N건·리뷰·가입일 · [프로필] [팔로우] · `Lv.n` 배지 → 판매자의 다른 매물 / `K` [♡ n] [가격 제안] [**채팅하기**].
- **요소 표**

| 영역 | 요소 | 무게 | 성격 | 탭하면 | 왜 여기 |
|---|---|---|---|---|---|
| H | 더보기 | 숨김 | 종료 컨테이너 | 시트(신고·차단) | 종료 레인은 시야 밖 |
| B | 판매자 블록(판매건수·리뷰·가입일) | 보조 | 이동 | 프로필 | "이 사람 믿어도 되나"의 답. S2 핵심 보조 |
| B | 팔로우 | 보조(현재) | 진행·가역 | 토글 | 이번 거래의 판단 재료가 아님 — **프로필 링크와 같은 무게일 이유 없음(경미)** |
| B | `Lv.n` 배지 | 표시 | — | — | **전제④(게임화 의도적 폐기) 위반** — 신뢰 판단 자리에 게임 지표를 둘 근거 없음 |
| K | 찜 | 보조 | 가역 | 토글 | 판단 유예. 하단 바 좌측 표준 |
| K | 가격 제안 | 보조(아웃라인) | 진행 | 제안 시트 | 채팅보다 무거우면 "후려치기" 문화 신호 |
| K | 채팅하기 | **주행동** | 진행 | DM 방 | 발견→문의 전환점. 단일 주행동 |
- **제안** ⓐ Lv 배지 제거. ⓑ 팔로우 무게 낮춤(선택).
- **실패 시나리오** 레벨이 높아 보여 신뢰했는데 거래와 무관한 게임 수치.
- **근거** MD-1~8, 16, MD-(잔재), `MarketDetail.tsx:341`.
- **판정(r1)** ☐

##### FR-2 더보기 시트(구매자)
- **조건** 더보기 탭.
- **와이어** `O` 시트 [신고하기][차단하기(위험 톤)].
- **요소 표**

| 영역 | 요소 | 무게 | 성격 | 탭하면 | 왜 여기 |
|---|---|---|---|---|---|
| O | 신고하기 | 숨김 | 종료·가역(제출 전) | 사유 시트 | X 레인 입구 |
| O | 차단 | 파괴적 | 종료·비가역(상대에 즉시 영향) | **확인 유무 `[확인]`** → 차단 | 같은 시트에 종료 행위만 — P-1 충족 |
- **제안** 차단 확인 없으면 추가.
- **근거** MD-2, 17, 18.
- **판정(r1)** ☐

---

#### F-S3-01 문의(DM 방 구조) — V1 직결
> DM 방은 S3~S7·X를 한 화면에서 순차로 겪는 유일한 화면. 헤더는 고정, 컨텍스트·본문·하단은 상태에 따라 바뀐다. 아래 FR-1은 **첫 문의 상태(약속 없음)**의 기준 프레임이고, 이후 상태의 변화는 F-S4/S5/S6/S7 프레임에서 다룬다.

##### FR-1 첫 문의(약속 없음)
- **조건** 1:1 거래방, 약속 없음 또는 PROPOSED 이전. 구매자·판매자 공통.
- **와이어(현재)**
```
H  [←]                    [프로필][무전기][⋮]     ← 넷이 픽셀 단위 같은 크기·색
C  ┌ 매물 썸네일 · 제목 · 가격 ┐               ← 탭=매물 상세
B  메시지 목록 (텍스트 / 카드형: 가격제안·약속·위치·무전기 초대·QR)
K  [+] [메시지 입력…………] [전송]
O  (무전기·위치공유 활성 시 드래그 캡슐/버블 — F-N-01)
```
- **요소 표**

| 영역 | 요소 | 무게 | 성격 | 탭하면 | 왜 여기 |
|---|---|---|---|---|---|
| H | 뒤로 | 보조 | 이동·안전 | 이전 화면 | 셸. 헤더 자격 |
| H | 프로필 | 보조 | 이동·안전 | 상대 프로필 | 대화 중 "이 사람 누구지" 재판단. 단계 불변 — 헤더 자격 |
| H | **무전기** | 보조(현재) | **즉시·단계 특이(S5)** | 채널 참여/개설, **채널 없으면 상대에게 초대 카드 자동 전송** | **이유 없음(이 상태에서).** 이동 단계가 아직 없고, 헤더 넷 중 유일하게 상대에게 흔적을 남기는 버튼이 나머지 셋(가서 본다)과 구분되지 않음. 2026-08-28 승격 동기("이동 중 못 찾는다")는 S5에서만 성립 |
| H | 더보기 | 숨김 컨테이너 | 안전(메뉴 열기) | 시트(FR-2) | 헤더 우측 끝 표준 |
| C | 매물 카드 | 보조 | 이동 | 매물 상세 | "무슨 물건 얘기 중" 상시 문맥 |
| B | 메시지 | — | — | 롱프레스(FR-4) | — |
| K | 전송 | **주행동** | 진행 | 전송 | 대화 화면의 단일 주행동 |
| K | + | 숨김 컨테이너 | — | 메뉴(FR-3) | 첨부 표준 |
- **주행동** 전송. 당위: 문의 단계의 목적.
- **제안** 헤더를 [뒤로][프로필][더보기] 셋으로. 무전기는 F-S3-03의 판정에 따라 S5 게이트(약속 확정 후) 또는 고정 바로. **셋만 남으면 "같은 크기·색"은 문제가 아니라 정답** — 셋 다 안전한 이동/컨테이너.
- **실패 시나리오** "아직 있나요?" 보내고 프로필 보려다 옆 아이콘 탭 → 상대 방에 음성 초대 카드 → "왜 갑자기 음성?" → 취소 불가. 신뢰 형성 순간의 최악 신호.
- **근거** DD-0~4, 불일치 #2, `DmDetail.tsx:1400-1458`, `TopBar.module.css:44-52`≡`DmDetail.module.css:870-880`.
- **판정(r1)** ☐

##### FR-2 더보기 시트 — V1
- **조건** 1:1 방, 더보기 탭.
- **와이어(현재)** `O` 시트 세로 3행, 같은 클래스 텍스트 행:
```
  신고하기
  위치 공유하기        ← 진행 행위가 종료 행위 사이에
  나가기  (위험 색)
```
- **와이어(제안)** `O` 시트 2행: `신고하기 / 나가기(위험 색)`. 위치 공유는 이 시트에서 **제거**(진입로는 "+" 메뉴와 활성 약속 카드 두 곳이 이미 있음).
- **요소 표**

| 영역 | 요소 | 무게 | 성격 | 탭하면 | 왜 여기 |
|---|---|---|---|---|---|
| O | 신고하기 | 숨김 | 종료 | 사유 시트 | X 레인 입구. 더보기가 제자리 |
| O | **위치 공유하기** | 숨김 | **진행·즉시(상대에게 초대 전송)** | 실시간 위치 채널 시작 + 초대 | **이유 없음.** 같은 동작이 이미 2곳에 있음(세 번째 진입로). 종료 행위 사이에 낀 진행 행위 — **P-1 위반, 판정자 피드백 V1 그대로** |
| O | 나가기 | 파괴적 | 종료·비가역(이력 상실) | 확인 → 나감 | 위험 색 + 확인 — 적정 |
- **주행동** 없음(메뉴).
- **제안** [위치 공유하기] 행 제거. 결과 시트는 종료 행위 2개만 → "위치를 신고한다"는 오독 구조가 사라짐.
- **실패 시나리오(현재)** 수상한 상대를 신고하려고 시트를 열고 첫 행 아래를 오탭 → **신고하려던 상대에게 내 실시간 위치 초대 전송.** 직거래 앱에서 가장 반대 방향의 사고.
- **근거** V1, DD-39·40·42, `DmDetail.tsx:2281-2313`, `.leaveItem` `DmDetail.module.css:927-932`.
- **판정(r1)** ☐

##### FR-3 "+" 첨부 메뉴
- **조건** 입력창 + 탭.
- **와이어(현재)** `O` 패널 6항목: [앨범][약속 잡기*][현재 위치][실시간 위치][가격 제안*][이모티콘] (*조건부).
- **요소 표**

| 영역 | 요소 | 무게 | 성격 | 탭하면 | 왜 여기 |
|---|---|---|---|---|---|
| O | 앨범 | 숨김 | 진행 | 갤러리 | 첨부 표준 |
| O | **약속 잡기**(1:1 + 열림 조건) | 숨김(현재) | 진행 | 약속 시트 | **서비스 차별점의 유일 입구가 이모티콘과 동열 — 이유 없음(→ F-S4-01 FR-1에서 승격)**. 메뉴 항목 자체는 유지 무방 |
| O | 현재 위치 | 숨김 | 진행 | 위치 카드 전송 | 상태 무관 도구. S5엔 약속 카드가 시야 안 경로 제공 — 숨김 적정 |
| O | 실시간 위치 | 숨김 | 진행·즉시 | 채널 시작 + 초대 | 동일 |
| O | 가격 제안(조건부) | 숨김 | 진행 | 제안 시트 | 매물 상세가 시야 안 경로 제공 → 재진입은 숨김으로 충분 |
| O | 이모티콘 | 숨김 | 진행 | 그리드 | 표준 |
- **제안** 항목 유지. 약속 잡기의 **추가 진입로**를 C 영역에 둔다(F-S4-01).
- **근거** DD-33~38, `DmDetail.tsx:2141-2207`.
- **판정(r1)** ☐

##### FR-4 메시지 롱프레스
- **조건** 메시지 롱프레스(1:1).
- **와이어** `O` [공감 6종][답장][수정(내 것)][삭제(내 것)].
- **요소 표**

| 영역 | 요소 | 무게 | 성격 | 탭하면 | 왜 여기 |
|---|---|---|---|---|---|
| O | 삭제 | 보조(확인 유무 `[확인]`) | 비가역(플레이스홀더 잔존) | 소프트 삭제 | 거래 대화에서 삭제는 "합의 증거 삭제"가 될 수 있음 → 확인 단계 여부가 판정 좌우 |
- **제안** 확인 없으면 추가 여부 판정.
- **근거** DD-43~47.
- **판정(r1)** ☐

---

#### F-S3-02 가격 제안
##### FR-1 제안 카드(대화 내)
- **조건** 구매자가 제안 전송 후.
- **와이어** `B` 카드: 금액 · (판매자 뷰) [**수락**][거절] / (구매자 뷰) [제안 취소].
- **요소 표**

| 영역 | 요소 | 무게 | 성격 | 탭하면 | 왜 여기 |
|---|---|---|---|---|---|
| B | 수락 | 카드 주행동(채움) | 진행 | 합의 진입 | 카드 스코프 주행동 허용 |
| B | 거절 / 취소 | 보조(ghost) | 가역(재제안 가능) | 상태 변경 | 손실 없음 → 확인 불필요 |
- **제안** 없음.
- **근거** DD-23~25.
- **판정(r1)** ☐

---

#### F-S3-03 무전기(워키토키) — V2 직결, 지난 피드백의 쟁점
- **단계** S5 도구(현재 S3부터 노출) · **상태** 제안 · **대표 지시 이력** 2026-08-28 헤더 승격(발견성)

##### FR-1 진입점(현재 4곳)
- **조건** 기기가 무전기 지원.
- **와이어** ① DM 방 헤더 아이콘(항상) ② DM 내 초대 카드 [참여하기] ③ 홈·채팅 목록 헤더 진입 버튼(**다른 아이콘**) ④ 캡슐 롱프레스 [채널 변경].
- **요소 표**

| 영역 | 요소 | 무게 | 성격 | 탭하면 | 왜 여기 |
|---|---|---|---|---|---|
| H(DM) | 헤더 아이콘 | 보조 | 즉시 | 참여/개설/초대 전송 | **S3 상태에서 이유 없음**(FR-1 F-S3-01) |
| B(DM) | 초대 카드 참여하기 | 카드 주행동 | 진행 | 참여 | **올바른 진입로.** 초대에 응답하는 자리 |
| H(홈·목록) | 진입 버튼 | 보조 | 진행 | 활성 채널 핑 / 최근 대화 선택 시트 | 약속 없는 홈에서 "누구와" 말할지 고르는 시트를 띄움 — **이유 약함**(약속이 있는 대화에서 시작하는 것이 자연) |
| O | 롱프레스 채널 변경 | 숨김 | 진행 | 채널 시트 | 다중 채널 사용자용. 빈도 낮음 |
- **제안** 진입로를 **두 곳**으로: (a) 약속 확정(ACCEPTED) 방의 진행 배너/약속 카드 [무전기] 버튼, (b) 초대 카드 [참여하기]. 홈·목록 헤더 버튼은 제거 또는 "활성 채널 있을 때만" 고정 바로 흡수(F-N-01). 아이콘 1종으로 통일.
- **근거** DD-3a/b, DD-28, `WalkieTalkieEntryButton.tsx:18-65`, `DmDetail.tsx:1437-1447`.
- **판정(r1)** ☐

##### FR-2 캡슐(현재) — 접힘/펼침/녹음 중
- **조건** 채널 활성. 모든 화면 위.
- **와이어** `O` 자유 위치 pill. 접힘: 아이콘+점. 더블탭 펼침: 채널명·상태(수신대기/발신중/재생중)·"○○님이 말하는 중"·파형·[X 닫기]. 탭: 녹음 시작/정지·전송. 드래그: 이동. 채널 등장·타인 발화·어텐션 핑 시 **자동 펼침**.
- **요소 표**

| 영역 | 요소 | 무게 | 성격 | 탭하면 | 왜 여기 |
|---|---|---|---|---|---|
| O | 캡슐 탭(녹음) | 보조 | 즉시 | 녹음 토글 | "한 손 즉시"는 정당 — **위치가 떠다닌다는 점만 문제** |
| O | 더블탭 펼침 | 숨김 | 표시 | peek | 근거 없음 — 발견 불가한 숨은 제스처. **제안 대상**(P-2) |
| O | 자동 펼침 | 표시 | — | — | 타인 발화 알림 — 정당하나 떠 있는 위치에서 펼치면 가림 면적이 예측 불가 |
| O | 닫기 X(펼침 시만) | 보조 | 종료 | 캡슐 닫힘 | **닫는 법이 더블탭 뒤에 숨어 있음 — 이유 없음.** 판정자 질문 "어떻게 닫히길 기대하는가"의 현재 답: "더블탭 후 X" |
| O | 드래그 | — | — | 이동 | 근거 없음. B0-3 V2(고정 위치 원칙)와 정면 충돌. **제안 대상**(P-2, F-N-01) |
- **제안** 캡슐을 F-N-01 제안대로 자유 위치 pill에서 **고정 바**(B0-4 "하단 바" 패턴)로 전환. 드래그 폐지. 펼침은 더블탭 대신 **바 탭 1회**로 대체해 발견 가능하게 하고, 닫기는 펼친 상태의 X를 바 축소 탭으로 대체해 숨은 제스처를 없앤다.
- **근거** `WalkieTalkieFloatingButton.tsx:75-99,420-433,680-734,850-857`.
- **판정(r1)** ☐ (F-N-01 FR-1과 함께)

##### FR-3 롱프레스 메뉴
- **조건** 캡슐 롱프레스.
- **와이어** `O` [대화방으로 이동][채널 변경][초대장 다시 보내기][홈화면 고정(Android)].
- **요소 표**

| 영역 | 요소 | 무게 | 성격 | 탭하면 | 왜 여기 |
|---|---|---|---|---|---|
| O | 대화방으로 이동 | 숨김 | 이동 | DM 방 | 가장 잦은 후속 행동인데 롱프레스 뒤 — 고정 바에서는 바 탭으로 승격 |
| O | 채널 변경 / 초대장 재전송 | 숨김 | 진행 | 각각 | 빈도 낮음. 숨김 적정 |
| O | 홈화면 고정 | 숨김 | 앱 밖 | 위젯 | OUT(네이티브 위젯) |
- **제안** 고정 바 롱프레스 메뉴로 이관(항목 유지, "대화방으로 이동"은 바 탭으로).
- **근거** `WalkieTalkieFloatingButton.tsx:942-949`, i18n `walkieTalkie.contextMenu*`.
- **판정(r1)** ☐

##### FR-4 종료
- **조건** 사용자가 끝내고 싶을 때.
- **현재** 펼침 → X. 채널 자체는 참가자 이탈·타임아웃으로 종료 `[정확한 규칙 확인]`. 초대 카드는 취소 불가.
- **제안** 고정 바 우측 [닫기] 상시 노출 → 확인 1회("무전기 채널에서 나갈까요?") → 바 사라짐. 상대에게는 "○○님이 나갔어요" 시스템 메시지 `[신규 여부 확인]`.
- **당위** 종료는 어디서든 2탭 이내 + 확인 1회(X 레인 원칙). 세션 종료는 상대에게 즉시 영향 → 파괴적 분류.
- **판정(r1)** ☐

##### FR-5 제안 종합: S5 게이트 + 하단 고정 바
- **조건** 약속 확정(ACCEPTED) 이후.
- **와이어(제안)**
```
H  [←]  상대 닉네임            [프로필][⋮]
C  ┌ 거래 진행 배너: QR 대기 중 · [거래 열기] ┐
   ┌ 매물 카드 ······················ [약속 잡기] ┐   (F-S4-01)
B  … 약속 카드(ACCEPTED): [길안내][위치 공유][무전기][거래 열기] · [약속 취소]
K  ┌ 진행 중 바: ● 무전기 · 채널명 · [말하기]      [닫기] ┐   ← 활성 시에만, 고정
   [+] [메시지 입력…] [전송]
```
- **요소 표(변경분만)**

| 영역 | 요소 | 무게 | 성격 | 탭하면 | 왜 여기 |
|---|---|---|---|---|---|
| B(약속 카드) | [무전기] 버튼 | 보조(ghost) | 진행·즉시(초대) | 채널 개설 + 초대 카드 | S5 도구가 S5가 표면화되는 자리에. 길안내·위치공유와 같은 성격 묶음(P-1). "이동 중 못 찾는다"는 승격 동기를 **약속이 있을 때** 충족 |
| K(고정 바) | 말하기(PTT) | 보조 | 즉시 | 누르는 동안 녹음 | 항상 같은 자리(P-2). 운전 중 더듬어 찾을 수 있는 위치 |
| K(고정 바) | 닫기 | 파괴적 | 종료 | 확인 → 나감 | 종료 상시 노출 |
- **판정자에게 반드시 물을 것** ① 언제 보이길 기대하는가(첫 문의부터 / 약속 확정 후 / 이동 중만) ② 누가 시작하는가 — 한쪽이 열면 상대에게 초대가 가는 현재 방식이 맞는가, 아니면 "둘 다 눌러야 연결"인가 ③ 홈·채팅 목록 진입 버튼 필요 여부. (닫는 방식과 하단 고정 여부는 F-N-01 FR-2에서 이미 정해짐 — 위 와이어·요소 표가 그 설계를 그대로 따름)
- **레퍼런스** 당근에 없음(고유). Zello·Discord PTT는 "채널에 들어간다"는 명시 진입이 있고 헤더 원탭 즉시 초대가 아님. iOS 통화 바 = 고정.
- **실패 시나리오(제안 적용 후 남는 것)** 약속 없이 음성으로 흥정하고 싶은 사용자는 더보기에서 찾아야 함 → 빈도 낮다고 판단, 판정 요청.
- **근거** V2, 불일치 #2 권고 A·B, F-N-01.
- **판정(r1)** ☐

---

#### F-S4-01 약속 잡기(합의)
##### FR-1 진입(현재 vs 제안)
- **조건** 1:1 방, 약속 열림(`appointmentUnlocked` — 판매자가 거래 진행을 시작한 뒤 `[정확한 트리거 확인]`).
- **와이어(현재)** `K` "+" 메뉴 6항목 중 2번째. 열렸다는 표시 없음.
- **와이어(제안)** `C` 매물 카드 우측에 [약속 잡기] 칩(열림 상태에서만). "+" 메뉴 항목 유지.
- **요소 표**

| 영역 | 요소 | 무게 | 성격 | 탭하면 | 왜 여기 |
|---|---|---|---|---|---|
| C | 약속 잡기 칩(제안) | 보조 | 진행 | 약속 시트 | "만나는 걸 앱이 정해준다"가 차별점 → 시야 안. 매물 카드는 "이 물건에 대한 다음 행동"이 붙을 자연스러운 자리 |
| K | "+" 약속 잡기(현재) | 숨김 | 진행 | 약속 시트 | 유지 무방(중복 진입로 허용 — 종료 행위와 섞이지 않음) |
- **제안** 칩 승격(기존 핸들러 재사용).
- **실패 시나리오(현재)** 가격 맞은 뒤 "그럼 언제 볼까요"가 텍스트로 오감 → 앱 약속 미생성 → 예약중·S5 도구·S6 절차 전부 비활성 → 차별점이 작동하지 않은 거래.
- **근거** DD-9, DD-34, 불일치 #5, `DmDetail.tsx:1525-1533, 2156-2164`.
- **판정(r1)** ☐

##### FR-2 약속 시트
- **조건** 약속 잡기 탭.
- **와이어** `O` 시트: 날짜·시간 → 장소(기본값 = S0 거래 희망 장소 `[확인]`) → [**제안 보내기**].
- **요소 표**

| 영역 | 요소 | 무게 | 성격 | 탭하면 | 왜 여기 |
|---|---|---|---|---|---|
| O | 장소 기본값 | 표시 | — | 변경 가능 | S0 입력이 S4 재료가 되는 연결점. 기본값이 없으면 S0 필수 게이트의 의미가 없음 |
| O | 제안 보내기 | **주행동** | 진행 | 약속 카드 전송 | 시트 목적 |
- **제안** 장소 기본값 확인.
- **근거** MC-11, DD-34.
- **판정(r1)** ☐

##### FR-3 약속 카드 PROPOSED
- **조건** 제안 전송 후 수락 전.
- **와이어** `B` 카드: 일시·장소 · (수신자) [**수락**][거절] / (제안자) [취소].
- **요소 표**

| 영역 | 요소 | 무게 | 성격 | 탭하면 | 왜 여기 |
|---|---|---|---|---|---|
| B | 수락 | 카드 주행동 | 진행 | ACCEPTED → 매물 예약중, 배너 등장 | 합의 성립 |
| B | 거절/취소 | 보조(ghost, 위험 톤) | 가역(성립 전) | 상태 변경 | 손실 없음 → 확인 불필요. 위험 톤은 과함(경미) |
- **제안** 없음(위험 톤 완화는 선택).
- **근거** DD-15, DD-22a.
- **판정(r1)** ☐

---

#### F-S5-01 이동 — V3 직결
##### FR-1 약속 카드 ACCEPTED(도구 모음)
- **조건** 약속 확정. DM 방 B 영역 최신 약속 카드 + C 영역 진행 배너 등장.
- **와이어** `C` 배너 "QR 대기 중/QR 준비됨/송금 신고됨/입금 확인됨" [거래 열기] / `B` 약속 카드: 일시·장소 · [길안내][위치 공유][(제안) 무전기][거래 열기] · [약속 취소](ghost 위험 톤) · [장소 다시 확인].
- **요소 표**

| 영역 | 요소 | 무게 | 성격 | 탭하면 | 왜 여기 |
|---|---|---|---|---|---|
| C | 진행 배너 | 보조 | 이동 | 거래 화면 | "지금 진행 중"이 단계 표면화되는 자리. 당근 상단 배너와 동형 |
| B | 길안내 | 보조 | 이동 | 내비 | S5 핵심 도구. 약속 카드가 제자리 |
| B | 위치 공유 | 보조 | 진행·즉시(초대) | 채널 시작 | **S5 도구의 올바른 자리**. "활성 약속에서만" 조건이 단계 게이트 |
| B | 무전기(제안) | 보조 | 진행·즉시 | 채널 개설 | 같은 성격 묶음(F-S3-03 FR-5) |
| B | 약속 취소 | **파괴적** | 종료·비가역(상대 즉시 영향) | **현재 확인 없음** → 제안: 확인 1회 | 상대가 이동 중일 수 있는 약속을 한 탭 파기. 거래 화면의 같은 행위는 확인 있음 — 마찰 비대칭 |
- **제안** ⓐ 약속 취소에 확인 1회. ⓑ 취소 시 상대에게 **푸시**(현재 인앱 갱신만). ⓒ 무전기 버튼 추가.
- **실패 시나리오** 판매자가 취소했는데 구매자는 30분을 달려 도착.
- **근거** DD-5, DD-16, DD-20~22b, 불일치 #4, 개선계획 G1-15, `DmDetail.tsx:864-878,1389,1461-1477`.
- **판정(r1)** ☐

##### FR-2 실시간 위치 초대 카드
- **조건** 한쪽이 위치 공유 시작.
- **와이어** `B` 카드 "○○님이 실시간 위치 공유를 시작했어요" [**참여하기**]. 참여 시 동의 모달(정밀 위치 공유 범위·자동 종료 규칙·나가기 시 즉시 삭제 고지).
- **요소 표**

| 영역 | 요소 | 무게 | 성격 | 탭하면 | 왜 여기 |
|---|---|---|---|---|---|
| B | 참여하기 | 카드 주행동 | 진행 | 동의 모달 → 참여 | 초대에 응답하는 자리 — 올바른 진입로 |
| O | 동의 모달 | 표시+확인 | — | 동의/취소 | 민감정보(정밀 좌표) 공유 전 명시 동의 — 유지 |
- **제안** 없음.
- **근거** DD-27, `LocationShareConsentModal.tsx`, i18n `consentBody/consentRetention`.
- **판정(r1)** ☐

##### FR-3 위치공유 활성 중(현재: 드래그 버블 + 모달)
- **조건** 채널 참여 후. 모든 화면 위.
- **와이어(현재)** `O` 원형 버블(참여자 수 배지), 자유 드래그 → 탭 → 모달: 참가자 목록·목적지 제안/투표 카드·길안내·[나가기].
- **요소 표**

| 영역 | 요소 | 무게 | 성격 | 탭하면 | 왜 여기 |
|---|---|---|---|---|---|
| O | 버블 | 보조 | 이동 | 모달 | **이유 없음(떠다님).** 판정자 피드백 V3 |
| O | 모달 [나가기] | 파괴적 | 종료 | 내 위치 즉시 삭제 | 동의문이 약속한 "언제든 나가기" — 정당. 확인 유무 `[확인]` |
| O | 목적지 제안/투표 | 보조 | 진행 | 투표 | 이동 중 합류점 재조정 — 고유 가치 |
- **근거** `LiveLocationFloatingButton.tsx:44-116`, 인벤토리 B 위치공유 절.
- **판정(r1)** ☐ (F-N-01 FR-1과 함께)

##### FR-4 제안: 고정 바의 위치공유 칸
- **조건** 채널 활성.
- **와이어** `K` 고정 바 칸: 📍 위치공유 · 참여자 n · (목적지까지 거리 `[가능 여부 확인]`) → 탭 = 모달. 무전기와 같은 바.
- **당위** P-2. 같은 성격(진행 중 세션)끼리 한 묶음(P-1). 겹침 소멸. 운전 중 예측 가능한 위치.
- **판정(r1)** ☐

---

#### F-S6-01 만남·교환(확인 → 결제 → 전달) — 최상위 원칙, 대표 결정 C2
> 거래 화면의 주행동은 역할·상태에 따라 하나씩 바뀐다. 어느 순간에도 한 사용자에게 채움 버튼은 하나.

##### FR-1 판매자 · QR 없음
- **조건** ACCEPTED, 판매자, QR 미등록.
- **와이어** `H` [뒤로] 거래 / `B` 타임라인(현재 5단계) → [QR 등록](**아웃라인**) → 신고·FAQ·고객센터.
- **요소 표**

| 영역 | 요소 | 무게 | 성격 | 탭하면 | 왜 여기 |
|---|---|---|---|---|---|
| B | QR 등록 | 보조(현재 아웃라인) | 진행 | QR 이미지 등록 | 이 상태에서 판매자가 온 목적 → 주행동 후보. 단 현금 거래 가능해 "필수"는 아님 → **경미, 판정 요청** |
- **제안** QR 등록을 **채움**으로 올린다(B0-4 "카드 안 채움 버튼" 패턴). 근거: 이 프레임에는 QR 등록 외에 진행 행동이 없어 주행동이 0개인 상태(A1 Q1 위반) — 유일한 진행 행동을 아웃라인·보조로 두면 사용자가 이 화면에서 뭘 눌러야 하는지 판단해야 한다(A1 Q2). 현금 거래로 QR 없이도 거래가 끝날 수 있다는 사실은 이 버튼을 없애는 근거는 되어도 격하하는 근거는 아니다 — 채움으로 올리되 아래에 "QR 등록 없이 현금으로도 거래할 수 있습니다" 보조 문구를 남겨 선택성을 유지한다.
- **근거** TT-2a.
- **판정(r1)** ☐

##### FR-2 구매자 · 약속 시각 창 밖(제안: 절차 미리보기)
- **조건** ACCEPTED, 구매자, 현재 시각이 약속 T-30분 이전(또는 T+60 이후).
- **와이어(현재)** `B` 타임라인 → QR 이미지 → [**송금했어요**](채움, 활성) → [신고하기] → FAQ → 고객센터. **약속 시각과 무관하게 활성.**
- **와이어(제안)** `B` 타임라인(재배열, FR-5) → QR(표시) → [송금했어요](**비활성** + 사유 "약속 시간에 만나 물건을 확인한 뒤 눌러 주세요") → [신고하기] → FAQ. 주행동 **없음**(정보 화면). 길안내는 약속 카드로.
- **요소 표**

| 영역 | 요소 | 무게 | 성격 | 탭하면 | 왜 여기 |
|---|---|---|---|---|---|
| B | 송금했어요(현재) | **주행동** | 진행·비가역(취소 불능 진입) | 확인 → 신고됨 | **시점이 틀림.** 약속 수락 직후부터 화면의 첫 행동이 "앱 가이드가 금지하는 행동"(선입금). 조건이 ACCEPTED·RESERVED·QR 존재만(`market.py:2306-2311`) |
| B | 송금했어요(제안, 창 밖) | 비활성 표시 | — | 사유 토스트 | 확정 전제 "물건 확인 후 결제". 화면이 순서를 강제 |
| B | 신고하기(QR 옆) | 숨김(ghost) | 종료 | 사유 시트 | 사기 발생 지점에 X 레인 입구 — 정당 |
- **제안** 약속 시각 창(T-30~T+60) 게이트, 서버 동일 조건. QR 등록도 같은 창으로 맞추면 타임라인이 정렬됨(선택).
- **실패 시나리오(현재)** 판매자 "미리 보내주시면 챙겨둘게요" → 앱 흐름이 그것을 정상 절차로 뒷받침 → 원격 선입금 사기. 정직한 거래에서도 "만나기 전에 눌러야 하나?" → 누르면 취소 불능 → 만나기 전 교착.
- **근거** TT-5, 불일치 #1, `TradeTransaction.tsx:138-148`, `market.py:2295-2318`, `translation.json:2186,2193-2194`.
- **판정(r1)** ☐

##### FR-3 구매자 · 창 안(대면 시점)
- **조건** ACCEPTED, 구매자, 약속 시각 창 안, QR 존재.
- **와이어** `B` 타임라인(현재 단계 "결제" 강조) → QR(크게) → [**송금했어요**](채움, 활성 + 확인 다이얼로그) → [신고하기].
- **요소 표**

| 영역 | 요소 | 무게 | 성격 | 탭하면 | 왜 여기 |
|---|---|---|---|---|---|
| B | 송금했어요 | **주행동** | 진행·비가역 | 확인 "송금을 완료하셨나요?" → 신고됨 | 대면·확인 후의 결제 진술. 무게·확인 모두 적정 — **시점만 여기로 옮긴 것** |
- **제안** 없음(FR-2의 게이트가 이 프레임을 만든다).
- **근거** TT-5.
- **판정(r1)** ☐

##### FR-4 판매자 · 송금 신고됨
- **조건** PAYMENT_REPORTED, 판매자.
- **와이어(현재)** `B` 타임라인 → "구매자가 송금했다고 해요" → [**입금을 확인했어요**](채움, **확인 없음**) → [거래 취소](비활성: 신고 후 불가).
- **와이어(제안)** 동일 + 탭 시 확인 다이얼로그 "ZaloPay/은행 앱에서 입금 내역을 직접 확인하셨나요?" [확인했어요][아직요].
- **요소 표**

| 영역 | 요소 | 무게 | 성격 | 탭하면 | 왜 여기 |
|---|---|---|---|---|---|
| B | 입금을 확인했어요 | **주행동 + 파괴적** | 진행·비가역(자기에게 불리한 기록, 인도 트리거) | 현재 즉시 / 제안 확인 1회 | 이 진술은 "실제 계좌 내역을 확인했다"는 정의를 가진 기록. 구매자 폰 화면(조작 가능)만 보고 누르면 분쟁 시 판매자에게 불리. **구매자 진술(TT-5)엔 확인이 있고 더 무거운 판매자 진술엔 없음 — 마찰 역전** |
- **제안** 확인 1회.
- **실패 시나리오** 가짜 입금 화면을 보고 확인 → 물건 인도 → 기록상 "판매자가 확인했다".
- **근거** TT-6, 불일치 #4, `TradeTransaction.tsx:254-258`, `translation.json:1023`.
- **판정(r1)** ☐

##### FR-5 타임라인 재배열
- **조건** 모든 역할·상태(표시 요소).
- **현재** `약속 확정 → QR 등록 → 송금 신고 → 수령 확인 → 상품 전달·완료` — 전달이 송금·확인 뒤. 문구 "상품 전달 후 기존 거래 완료 절차를 별도로 진행해요"가 순서를 못박음.
- **제안** `약속 확정 → 만나서 물건 확인 → 결제(QR 제시·송금) → 판매자 수령 확인 → 상품 전달·완료`. "만나서 물건 확인"의 done 판정은 약속 시각 창 진입(또는 기존 출발/도착 이벤트 `[프론트 소비처 확인]`).
- **당위** 표시지만 행동 순서를 규정한다. 가이드("물건 확인 후 결제")와 화면이 반대말을 하는 자기모순 해소. 3로케일 문구 변경.
- **근거** TT-(타임라인), `TradeTransaction.tsx:225-233`, `translation.json:1016-1025`.
- **판정(r1)** ☐

---

#### F-S7-01 완료·후기
##### FR-1 완료 요청/처리 카드
- **조건** 입금 확인 후(또는 현금 거래 후), DM 약속 카드.
- **와이어** `B` 약속 카드: (구매자) [완료 요청](ghost) / (판매자) [거래 완료 처리](ghost, **확인 없음**) · 요청 오면 [수락][거절].
- **요소 표**

| 영역 | 요소 | 무게 | 성격 | 탭하면 | 왜 여기 |
|---|---|---|---|---|---|
| B | 완료 요청(구매자) | 보조 | 가역(거절 시 재요청) | 요청 | 손실 없음 |
| B | 거래 완료 처리(판매자) | **파괴적** | 비가역(SOLD 복귀 불가) | 현재 즉시 → 제안 확인 1회 | 물건 넘기기 전 오탭이면 매물 종료. ghost 무게에 비가역 |
| B | 완료 요청 거절(판매자) | 보조 | 가역 | 거절 | 재요청 가능 |
- **제안** 완료 처리에 확인 1회.
- **근거** DD-17~19, 불일치 #4, `DmDetail.tsx:1748-1752,1786-1790`.
- **판정(r1)** ☐

##### FR-2 SOLD 이후 DM 방
- **조건** 매물 SOLD.
- **와이어** `C` 후기 배너 [**거래 후기 보내기**] / `B` 대화 / `K` 입력창(유지).
- **요소 표**

| 영역 | 요소 | 무게 | 성격 | 탭하면 | 왜 여기 |
|---|---|---|---|---|---|
| C | 후기 배너 | 보조(사실상 이 방의 남은 목적) | 진행 | 후기 시트 | SOLD 이후 유일한 남은 행동. 컴포저가 살아 있어 주행동 표기는 전송 유지 |
| — | 진행 도구(길안내·위치공유·무전기·거래 열기) | — | 진행 | — | B1 S7 성공 신호는 후기 제출 하나뿐 — 종료된 방에 진행 성격 액션이 남으면 한 영역에 진행·종료가 섞인다(P-1). 현재 노출 여부 `[확인]` |
- **제안** ① SOLD 전환 시 `C` 의 진행 배너를 후기 배너로 **교체**한다 — 당근의 "진행 중 상태는 상단 고정 배너"(B0-2)를 종료 상태에 그대로 적용, `C` 에 배너는 항상 하나. ② 같은 시점에 고정 도구 바(무전기·위치공유)와 `+` 메뉴의 진행 항목(길안내·거래 열기)을 제거한다. `K` 입력창은 유지(대화·분쟁 잔여). ③ 후기 미제출이면 후기 배너를 **주행동**(카드 안 채움 버튼 패턴), 제출 후에는 `표시`로 격하. 검증: SOLD 전환 직후 DM 방에서 성격 `진행` 인 액션이 0개이고 `C` 배너가 1개인지 확인.
- **근거** DD-10~11, 단계표 S7 "금지".
- **판정(r1)** ☐

---

#### F-S7-02 거래 이력
##### FR-1 이력 목록
- **조건** 프로필 → 거래 이력.
- **와이어** `B` 행: 썸네일·제목·상대·일자·[후기 작성](위치·무게 `[확인]`). 행 탭 → **현재 매물 상세**.
- **요소 표**

| 영역 | 요소 | 무게 | 성격 | 탭하면 | 왜 여기 |
|---|---|---|---|---|---|
| B | 행 탭 | 보조 | 이동 | 현재 매물 광고 / 제안 그 거래의 대화방 | 사후 사용자가 원하는 것은 "그 거래"(상대·합의·후기). 코드 주석은 원래 대화방 |
| B | 후기 작성 | **주행동** | 진행 | 후기 시트 | B1 S7 성공 신호가 "후기 제출" — 이 화면에 남은 유일한 진행 행동 |
- **제안** ① 행 탭 목적지를 **그 거래의 대화방(DM)** 하나로 고정한다(현재 매물 상세는 제거). 근거: 대화방 `C`에 이미 후기 배너가 붙어 있어(F-S7-01 FR-1, B0-4 "상단 고정 진행 배너") 상대·합의·후기가 한 자리에 모이고, 매물 상세는 SOLD 이후 "현재 광고"라 재판매·삭제 시 목적지가 깨진다. ② 매물을 다시 보려는 경로는 대화방 `C` 매물 카드로 남긴다(별도 진입점 추가하지 않음). **검증:** 이력 행 탭 → 해당 거래 대화방 진입, 매물이 삭제·재등록된 거래에서도 행 탭이 유효할 것.
- **근거** TH-1~3, `TradeHistory.tsx:15 vs :69`.
- **판정(r1)** ☐

---

#### F-X-01 취소·교착 — 대표 결정 C1
##### FR-1 취소 경로 두 개(마찰 비교)
- **조건** ACCEPTED, 송금 신고 전.
- **와이어** ① DM 약속 카드 [약속 취소](ghost 위험 톤, **확인 없음**) ② 거래 화면 [거래 취소](위험 톤 + **확인 있음**). 같은 서버 동작.
- **요소 표**

| 영역 | 요소 | 무게 | 성격 | 탭하면 | 왜 여기 |
|---|---|---|---|---|---|
| B(DM) | 약속 취소 | 파괴적(표기만) | 비가역·상대 즉시 영향 | 즉시 취소 | 자리는 근거 있음 — 약속 상태를 들고 있는 카드 안 액션(B0-4 "카드 안 채움 버튼: 약속·제안 카드"). 근거 없는 것은 **마찰**: `파괴적`은 위험 톤 + 확인 1회 필수(B0-4 무게 정의)인데 확인이 없어 B1 X 레인 성공 신호("2탭 이내 + 확인 1회")를 못 맞춤 |
| B(거래) | 거래 취소 | 파괴적 | 동일 | 확인 → 취소 | 적정 |
| — | 취소 가능 조건 | — | — | — | 두 화면이 다르게 구현(DM 카드는 "신고됨"을 안 봄 `[런타임 확인]`) → 신고 후 DM 카드에 취소가 남을 가능성 |
- **제안** ⓐ DM 약속 카드 [약속 취소]에 거래 화면과 **같은 확인 다이얼로그**를 붙인다 — 앱에 이미 있는 `useConfirmStore`(B0-4, 3곳 사용)를 그대로 쓰고 문구·위험 톤도 거래 화면과 동일하게. 검증: 두 경로 모두 "탭 → 확인 1회 → 취소"로 탭 수와 문구가 같다(B1 X 레인 "2탭 이내 + 확인 1회"). ⓑ 취소 가능 조건을 서버 한 곳 기준으로 단일화하고, 신고 이후에는 DM 카드에서 [약속 취소]를 **감춘다**(P-1: 실행 불가한 종료 액션을 진행 카드에 남기지 않는다). 대신 같은 자리에 `표시` 무게로 "취소 불가 — 고객센터 문의"를 두고 `F-CS-01`로 보낸다. 검증: 신고 후 DM 카드에 취소 버튼이 없고, 거래 화면과 취소 가능 여부가 항상 일치한다.
- **근거** DD-22b, TT-7, 4.2 결함 5.
- **판정(r1)** ☐

##### FR-2 교착 상태
- **조건** PAYMENT_REPORTED 후 거래가 깨짐(노쇼·변심·사기 의심).
- **현재** 양측 취소 불가. 판매자 완료(SOLD)만 가능 → 매물을 판매중으로 되돌릴 수 없음(재등록 우회). **운영자도 되돌릴 수 없음**(어드민은 조회·메모만). 노쇼 기록 수단 없음.
- **제안** ⓐ 운영자 "거래 되돌림"(감사 로그 + 양측 알림) — C1 권고 (a). ⓑ 약속 시각 게이트(F-S6-01 FR-2)로 원격 신고 벡터 축소 — C1 권고 (e). ⓒ 노쇼 표시는 파일럿 후.
- **당위** 운영자가 풀 수 없는 상태는 상태기계 결함. 당근은 채팅 기반이라 이런 교착 자체가 없음.
- **정의 요청(요소 표 없음 — 화면이 아니라 정책이 아직 없다)** 위 ⓐ~ⓒ는 방향일 뿐 화면이 아니다. 요소 표를 만들려면 대표가 먼저 셋을 정의해야 한다. **① 범위** — 교착으로 볼 상태가 `PAYMENT_REPORTED` 하나인가, `ACCEPTED` 노쇼·장기 무응답까지인가. 플랫폼은 결제를 검증하지 않으므로(B0-1 전제 ②) 되돌림 판단의 근거를 무엇으로 삼을지가 같이 정해져야 한다(양측 진술 / 약속 시각 경과 / 운영자 재량). **② 권한** — 되돌림을 운영자만 하는가, 양측 합의(한쪽 요청 + 상대 확인)로도 여는가, 상대 무응답이면 시한 경과로 자동 해제되는가. 되돌린 뒤 매물은 판매중으로 돌아가는가, 구매자 이력에는 무엇으로 남는가(무표시 / 취소됨 / 노쇼). **③ 자리** — 사용자에게 보이는 출구가 거래 화면 `K`인가, DM `C` 진행 배너인가, 아니면 고객센터 접수(`F-CS-01`)로만 여는가. X 레인의 기준선은 "2탭 이내 + 확인 1회"(B1)이고, 되돌림(종료)을 ⓑ 시각 게이트 같은 진행 요소와 한 묶음에 두지 않으며(P-1), 어느 쪽이든 떠 있는 요소가 아니라 고정 영역에 놓는다(P-2). 세 답이 모이면 그때 요소 표를 작성하고 판정 대상으로 올린다.
- **실패 시나리오** 만나기 전에 송금 신고를 눌렀는데 거래가 깨짐 → 아무도 못 되돌림 → 판매자 재등록, 구매자 이력에 미완 거래.
- **근거** 개선계획 C1·G1-2·G1-3, `market.py:2347-2398`, `admin_api/transactions.py`.
- **판정(r1)** ☐

---

#### F-X-02 신고·차단(기록, 이번 라운드 보류 후보)
##### FR-1 신고 진입점 매트릭스
| 대상 | 진입점 | 기신고 표시 | 비고 |
|---|---|---|---|
| 매물 | 상세 더보기 | 있음("신고함") | 기준 구현 |
| 사용자 | 프로필 더보기 `[확인]` | 없음(409 토스트) | |
| 대화 | DM 더보기 | 없음 | 1:1은 메시지 단위 신고 없음 |
| 거래/QR | QR 카드 옆·거래 화면 | 없음 | 사기 지점 입구 — 정당 |
| 채널 게시글 | **없음** | — | UGC 정책상 스토어 제출 전 권고(G1-5) |
- **제안** 6종 통일·기신고 표시 → 보류(파일럿 후). 채널 게시글 신고만 이번 주 후보.
- **정의 요청(요소 표 없음 — 화면이 아니라 신고 진입점 정책이 아직 없다)** 위 "6종 통일"은 방향일 뿐 화면이 아니다(제안은 6종, 표는 5행 — 대상 목록부터 어긋나 있다). 요소 표를 만들려면 대표가 먼저 셋을 정의해야 한다. **① 범위** — 통일 대상이 위 표의 5행(매물·사용자·대화·거래/QR·채널 게시글)인가, 표에 없는 대상(그룹 글·댓글·후기)까지인가. 플랫폼이 결제를 검증하지 않으므로(B0-1 전제 ②) 신고는 사용자가 사고를 남길 수 있는 사실상 유일한 기록이다 — 무엇을 신고 가능 대상으로 둘지가 먼저 정해져야 한다. **② 표시** — 기신고 상태를 매물처럼 항상 "신고함"으로 보여주는가, 지금처럼 409 토스트로만 알리는가. 같은 대상을 다시 신고하려 할 때 사용자가 무엇을 보고 이미 접수됐음을 아는가(진입점을 감춤 / 비활성 + 표시 / 접수 내역으로 이동). 접수 **이후**(조회·취소·추적)는 X가 아니라 CS(`F-CS-01`)의 몫이므로(B1 X↔CS 경계), 그 경계로 넘기는 링크를 신고 지점에 둘지도 같이 정한다. **③ 자리** — 다섯 진입점이 전부 `숨김`(더보기)이어도 되는가, 사기 지점 입구인 거래/QR만 더 앞에 나와야 하는가. 어느 쪽이든 신고는 `종료` 성격이므로 진행 액션과 한 묶음에 두지 않고(P-1 — V1 더보기 시트와 같은 기준), 떠 있는 요소가 아니라 고정 영역에 놓는다(P-2). X 레인 기준선은 "2탭 이내 + 확인 1회"(B1)이며, 진입점이 화면마다 다른 깊이여도 이 기준은 같아야 하는지도 답에 포함한다. 세 답이 모이면 그때 요소 표를 작성하고 판정 대상으로 올린다.
- **판정(r1)** ☐

##### FR-2 차단
- **현재** 진입점: 매물 상세 더보기 · 설정 > 차단 목록(해제만). **공개 프로필에 없음** → 매물 없는 상대는 차단 불가.
- **제안** 프로필 더보기에 차단 추가 → 보류(파일럿 후) 또는 소규모면 이번 주.
- **정의 요청(요소 표 없음 — 차단이 무엇을 끊는지가 아직 정의되지 않았다)** 위 제안은 자리(프로필 더보기) 하나만 말하고 있어 요소 표를 만들 수 없다. 대표가 먼저 셋을 정의해야 한다. **① 효력 범위** — 차단이 끊는 것이 DM 하나인가, 프로필 열람·매물 노출·커뮤니티 피드/댓글(`F-CM-01`·`F-CM-02`)·진행 중 세션(무전기·위치공유)까지인가. 일방(내 쪽만 안 보임)인가 상호(상대도 나를 못 봄)인가, 상대에게 차단 사실이 보이는가(`즉시` 성격 여부가 여기서 갈린다). **② 진행 중 거래와의 충돌** — `ACCEPTED`·`PAYMENT_REPORTED` 상대를 차단할 수 있는가. B1 X 레인은 "어느 단계에서든 빠져나온다"이지만 플랫폼은 결제를 검증하지 않으므로(B0-1 전제 ②) 차단이 곧 교착(`F-X-01 FR-2`)을 만들 수 있다 — 차단 시 거래를 같이 취소하는가, 차단을 막고 고객센터(`F-CS-01`)로 보내는가. **③ 해제·표시** — 현재 설정 > 차단 목록은 해제만 가능하다. 차단 상태가 프로필·DM·매물에서 어떻게 보이는지, 해제하면 무엇이 되돌아오는지(과거 대화·찜·팔로우)를 정해야 목록 화면의 요소 표가 나온다. 어느 답이든 차단은 `종료` 성격이므로 팔로우·DM 같은 `진행` 액션과 한 묶음에 두지 않고(P-1), 기준선은 X 레인의 "2탭 이내 + 확인 1회"(B1)다. 세 답이 모이면 `F-P-01`(상대 프로필)과 함께 요소 표를 작성하고 판정 대상으로 올린다.
- **판정(r1)** ☐

---

#### F-P-01 상대방 프로필(신뢰 확인) — `/profile/:userId`
- **단계** S2(판단)의 신뢰 확인 경로 · S3(문의) 중 재판단 경로 · **상태** 제안 · **등급** A(깊게) · **적용 대상** 타인 프로필, 프로필 매물/게시물 더보기

##### FR-1 타인 프로필 — "이 사람 믿어도 되나"의 답
- **조건** 구매자가 매물 상세(F-S2-01)의 판매자 블록 또는 DM 헤더(F-S3-01)의 [프로필]을 탭해 진입. `isOther = true`(내 id 아님). 비로그인은 라우트 자체가 막혀 있음(`App.tsx:667` PrivateRoute).
- **와이어** `H` [뒤로] · 제목 · [⋮ 더보기] / `B` 아바타 + 닉네임 행(레벨 배지 → 인증 배지 → 신뢰 티어 칩 → 친구 칩) → 라이더 스타일 칩 → 팔로워/팔로잉 2칸 카운트 → 액션행 [팔로우 또는 팔로잉(외곽선)] [DM 아이콘] → **신뢰 섹션**(가입 시기 · 판매 완료 수 · 마켓 평점 ★ + 리뷰 수) → 매물 레일(섹션 헤더 우측 "더보기") → 게시물 레일(섹션 헤더 우측 "더보기", 카드 안에 응원 하트) / `K` 없음(하단 고정 바 없음).
- **요소 표**

| 영역 | 요소 | 무게 | 성격 | 탭하면 | 왜 여기 |
|---|---|---|---|---|---|
| B | 신뢰 섹션(가입 시기·판매 완료 수·평점+리뷰 수) | 표시 | 표시 | 없음 | **S2의 질문("이 사람 믿어도 되나")에 직접 답하는 유일한 블록.** 이 화면이 존재하는 이유 그 자체 |
| B | 신뢰 티어 칩(`TrustTierChip`) | 표시 | 표시 | 없음 | 서버가 변환한 티어만 받는다(원값 매너온도 비노출) — 매물 상세의 판매자 배지 그룹과 같은 배치 관례 |
| B | 레벨 배지(`LevelBadge level={profile.level}`) | 표시 | 표시 | 없음 | **이유 없음.** 게임화는 확정 전제 ④로 폐기됐는데 이 배지만 **어떤 피처플래그로도 가려지지 않고 항상 노출**된다. 신뢰 지표(평점·거래 수) 옆에서 "레벨이 신뢰"라는 오독을 만든다 |
| B | 팔로우 / 팔로잉 토글 | **주행동** | 진행·가역 | 팔로우 / (언팔은 확인 다이얼로그 1회) | **이유 없음(위계).** S2 사용자가 여기서 할 일은 "판단하고 연락"인데, 채움 버튼(primary)은 팔로우가 가져가고 연락(DM)은 아이콘이다 |
| B | DM 아이콘 | 보조 | 진행 | 대화 생성 → `/dm/:id` | **자리는 B1 근거 있음** — S2의 성공 신호가 "채팅/제안/찜"이므로 액션행에 있는 것이 맞다. 다만 **무게는 관성** — 실제 S2→S3 전이 경로인데 팔로우보다 낮다. 제안 ①에서 뒤집는다 |
| B | 팔로워 / 팔로잉 카운트 | 보조 | 이동 | `/followers/:id` · `/following/:id` | 소셜 지표. 거래 판단 기여도는 낮음 |
| B | 매물 레일 카드 | 보조 | 이동 | `/market/:id` | "이 사람이 또 뭘 파나" — 판단 재료로 정당 |
| B | 매물 레일 "더보기" | 보조 | 이동 | `/profile/:id/listings` | 레일이 넘칠 때만 노출 |
| B | 게시물 레일 카드 | 보조 | 이동 | `/feed/post/:id` | 커뮤니티 활동을 신뢰 신호로 쓰는 구성 — 거래 여정과의 유일한 접점 |
| B | 게시물 카드 응원(하트) | 보조 | 진행·가역 | 응원 토글(`stopPropagation`) | **이유 없음.** 상대를 판단하러 온 화면에서 상대 글에 응원하는 행동은 S2와 무관 |
| H | 더보기(⋮) | 숨김 | 종료 | 신고 사유 시트(FR-2) | 종료 행위는 숨김 자리가 맞다(P-1) |
- **주행동** (현재) 팔로우. (제안) **DM(연락하기)**.
- **제안** ① 액션행 위계를 뒤집는다 — [메시지 보내기]를 채움 버튼(주행동), 팔로우를 외곽선(보조)으로. 매물 상세 하단 바 [찜][제안][채팅]에서 [채팅]만 채움인 것과 같은 위계(B0-4 하단 바 패턴). 검증: 이 화면의 채움 버튼이 1개이고 그것이 DM인가. ② 닉네임 행의 레벨 배지를 제거한다(확정 전제 ④ 게임화 폐기). 검증: `isOther` 프로필에서 `LevelBadge` 렌더가 남지 않았는가. ③ 신뢰 섹션을 액션행 **위**로 올린다 — 판단 재료를 보고 나서 행동을 고르는 순서. 검증: DOM 순서가 닉네임 행 → 신뢰 섹션 → 액션행인가. ④ 차단 진입점을 더보기에 추가한다(FR-2 참조). ⑤ 게시물 레일 카드에서 응원 하트를 뺀다 — 같은 화면의 매물 레일 카드와 동일하게 **이동 전용** 카드로 통일하고, 응원은 목적지 `/feed/post/:id`(CM2)에서만. 검증: 게시물 카드 탭의 결과가 이동 하나뿐인가. ⑥ 신뢰 섹션에 신규 사용자 빈 상태 문구를 넣는다 — 평점 `—`·거래 0건이면 "아직 거래 기록이 없는 사용자입니다"로 표시(B0-4 빈 상태 패턴, 단 CTA 없음 — 판단 재료이지 행동 유도가 아니다). 검증: 거래 0건 계정 프로필에서 `—` 대신 문구가 보이는가.
- **실패 시나리오** 매물이 없는 상대(피드에서만 만난 사람)는 이 화면에서 차단할 수 없다 — 차단하려면 그 사람의 매물 상세로 가야 하는데 매물이 없으므로 **경로가 아예 없다**. 신뢰 섹션이 평점 `—`·거래 0건인 신규 사용자에게는 "판단 불가"가 "나쁨"으로 읽힌다(빈 상태 문구 없음).
- **근거** `frontend/src/pages/profile/UserProfile.tsx:255-263`(닉네임 행 배지 4종), `:287-308`(액션행 팔로우/DM), `:310-326`(신뢰 섹션 3지표), `:331-341,353`(매물 레일), `:363-398`(게시물 레일·응원), `:219-230`(케밥). 인벤토리 `260918_flow_inventory_D_trust.md:341-372`(9액션 전수표).
- **판정(r1)** ☐

##### FR-2 더보기 시트(타인 프로필) — 신고만 있고 차단이 없다
- **조건** FR-1에서 [⋮] 탭. `isOther`일 때만 버튼이 존재한다(내 미리보기에서는 없음).
- **와이어** `O` 하단 바텀시트: 제목("신고 사유") + 사유 리스트 행 N개. **다른 항목 없음** — 차단도, 취소도, 이력도 없다.
- **요소 표**

| 영역 | 요소 | 무게 | 성격 | 탭하면 | 왜 여기 |
|---|---|---|---|---|---|
| O | 신고 사유 행(리스트) | **파괴적** | 종료·비가역 | 신고 접수(제출 후 재신고 불가, 취소는 `/settings/support`에서만) | 종료 행위를 숨김 자리에 두는 것은 P-1에 맞다. 다만 **되돌릴 수 없는데 확인 단계가 없다** — 사유를 탭하면 바로 접수 |
| O | 기신고 표시 | 표시 | 표시 | — | **없다.** 매물 신고(`MarketDetail`)에만 "신고함" 라벨이 있고 사용자 신고는 항상 "신고하기"로 보이다가 409 토스트로만 알려준다 |
| O | 차단 | 보조 | 종료 | — | **없다(부재).** `blockUser()` 호출처는 `MarketDetail.tsx` 한 곳뿐 |
- **주행동** 없음(종료 전용 시트).
- **제안** ① 사유 탭과 접수 사이에 **확인 1회**를 넣는다(P-1 "파괴적 = 확인 1회 필수"). 신설하지 않고 앱에 이미 있는 확인 다이얼로그(`useConfirmStore`, B0-4 기존 패턴)를 쓴다. ② 시트를 **[신고하기][차단하기(위험 톤)] 2행**으로 바꾸고 사유 리스트는 [신고하기] 탭 뒤 2단계로 내린다 — `F-S2-01 FR-2`(매물 상세 더보기)가 이미 쓰는 시트 구조를 그대로 옮기는 것이고, 차단 동작은 기존 `blockUser()`(`MarketDetail.tsx:255-278`)를 재사용한다. 두 행 모두 성격이 `종료`라 P-1을 깨지 않는다. F-X-02 FR-2는 "차단을 프로필에 둔다"는 정책 판정이고, 이 프레임은 그 정책이 놓이는 화면·행 위치다(중복 신설이 아니라 배치 지정). ③ 접수 후 행 라벨을 "신고하기"→**"신고함"**(비활성)으로 바꾼다 — 매물 신고가 이미 쓰는 표기이므로, 409 토스트로만 알리는 현재를 같은 문법으로 통일한다.
- **실패 시나리오** 사기 의심자를 만난 사용자가 "차단"을 찾다가 못 찾고 신고만 한다 → 상대는 계속 DM을 보낼 수 있다.
- **근거** `UserProfile.tsx:162-180,410-417`(신고 시트, 확인 단계 없음), 인벤토리 `260918_flow_inventory_D_trust.md`(교차 화면 표 — 차단 노출 2곳: `MarketDetail.tsx:255-278`, `/settings/blocked` 해제만), 킷 `F-X-02 FR-2`.
- **판정(r1)** ☐

##### FR-3 내 프로필 미리보기 모드
- **조건** 내 id로 이 화면에 들어옴(`/profile`의 "다른 사람에게 보이는 내 프로필" → `ProfileMain.tsx:682-690`). `isSelfPreview = true`.
- **와이어** `H` [뒤로] 제목 (더보기 없음) / `B` 상단에 미리보기 배너("다른 사람에게 보이는 화면입니다") → 이하 FR-1과 동일하되 **액션행(팔로우·DM) 전체가 없음** / `K` 없음.
- **요소 표**

| 영역 | 요소 | 무게 | 성격 | 탭하면 | 왜 여기 |
|---|---|---|---|---|---|
| B | 미리보기 배너 | 표시 | 표시 | 없음 | "내가 보는 화면이 아니라 남이 보는 화면"을 알리는 유일한 신호. 2026-09-10 대표 요청으로 자기관리 화면과 공개 화면을 분리한 결과물 |
| B | 액션행(팔로우·DM) 부재 | 표시 | 표시 | — | 자기 자신에게 팔로우·DM은 불가 — 당연한 분기 |
| B | 레벨 배지 | 표시 | 표시 | 없음 | **이유 없음**(FR-1과 동일 잔재. 여기서는 "내 레벨"로 읽혀 게임화 기대를 재생산) |
- **주행동** 없음(확인 전용).
- **제안** ① 배너 우측에 [프로필 수정] 보조 행동 하나를 붙여 `/profile/edit`로 보낸다 — 신설이 아니라 B0-4 "빈 상태 CTA" 패턴과 같은 *표시 블록 안의 단일 행동* 형태이며, 미리보기는 확인 전용이므로 채움 버튼이 아닌 외곽선·텍스트 무게를 쓴다. 검증: 미리보기에서 프로필 수정 화면까지 뒤로가기 없이 1탭으로 도달하는가. ② 레벨 배지를 제거한다 — 확정 전제 ④(게임화 폐기)로 여기 있을 근거가 없고, FR-1 제안 ②와 같은 처분이다. 검증: `isSelfPreview` 분기에서 `LevelBadge` 렌더가 남지 않았는가. ③ 배너를 `B` 최상단 스크롤 영역이 아니라 헤더 아래 `C`(컨텍스트 고정)로 올린다 — 앱이 이미 쓰는 상단 고정 진행 배너(B0-4 기존 패턴, B0-2 당근 상단 고정 배너)와 같은 자리로, 스크롤해도 "남이 보는 화면"이라는 맥락이 사라지지 않게 한다. 검증: 목록 끝까지 스크롤한 상태에서도 배너가 보이는가.
- **실패 시나리오** 배너를 못 보면 "내 프로필인데 왜 팔로우 버튼이 없지"로 읽힌다. 배너가 본문 최상단이라 한 번 스크롤하면 이 유일한 신호가 사라진다.
- **근거** `UserProfile.tsx:212,246-250,287`(isSelfPreview 분기), `ProfileMain.tsx:682-690`(진입).
- **판정(r1)** ☐

##### FR-4 프로필 매물/게시물 전체 목록
- **조건** FR-1의 레일 "더보기" 탭. `/profile/:userId/listings` · `/profile/:userId/posts`.
- **와이어(현재)** `H` [뒤로] 제목 / `B` 그리드 목록 / `K` 없음. **내부 액션 구성 미확인.**
- **요소 표**

| 영역 | 요소 | 무게 | 성격 | 탭하면 | 왜 여기 |
|---|---|---|---|---|---|
| B | 그리드 카드 | 보조 | 이동 | `/market/:id` · `/feed/post/:id` (추정) | 레일이 넘칠 때의 전량 열람 — 판단 재료 확장 |
| B | 나머지 요소 | 표시 | 표시 | — | **미확인(인벤토리가 "본문 미독 — 커버리지 갭"으로 명시, 이번 작업에서도 파일 본문 미확인)** |
- **주행동** 미확인 — 실측 전 가정: "전량을 훑고 하나를 골라 상세로 이동"(그리드 카드 탭). 이 가정이 틀리면 ①의 실측에서 뒤집힌다.
- **제안** ① **실측 먼저** — `/profile/:userId/listings`·`/posts` 화면 컴포넌트 본문을 읽어 요소 표의 "나머지 요소" 행을 실제 요소로 대체한다. 검증: 이 프레임 요소 표에 `미확인` 행이 0이 되고 모든 행에 파일:라인 근거가 붙는다. ② **실측 결과와 무관한 기본선** — B 영역에는 `이동` 성격만 둔다. 찜·팔로우·신고처럼 `즉시`·`종료` 성격인 것을 그리드 카드 위나 같은 줄에 섞지 않는다(P-1). 검증: 요소 표 B 영역 성격 열에 `즉시`·`종료`가 하나도 없다. ③ **빈 목록** — 앱에 이미 있는 **빈 상태 CTA 3종**(B0-4) 패턴을 그대로 쓴다. 검증: 매물 0건·게시물 0건에서 빈 상태 문구가 렌더되고, 타인 프로필이므로 CTA는 생성이 아니라 이동(레일로 되돌아가기)이다.
- **실패 시나리오** ①이 생략된 채 판정되면 FR-1(신뢰 판단)에서 넘어온 사용자가 이 화면에서 무엇을 할 수 있는지 문서와 실제가 어긋난 채로 확정된다. ②가 깨지면 "전량 열람" 중에 종료 행위가 손에 닿아 오탭으로 이어진다.
- **근거** 라우트 존재는 확정(`App.tsx:213-214,668-669`), 진입은 `UserProfile.tsx:336,368`. 액션표 미확인 — 인벤토리 `260918_flow_inventory_D_trust.md`(§고아·도달불가 3번, "커버리지 갭").
- **판정(r1)** ☐

---

#### F-P-02 개인 프로필 허브(내 프로필) — `/profile`
- **단계** S0·S7의 상시 허브(거래 여정 전 구간에서 진입) · **상태** 제안 · **등급** A(깊게) · **적용 대상** `ProfileMain.tsx` 3레이어(고정헤더 / 소셜·액션 / 드래거블 시트)
- **왜 이 F-ID가 깊이 쪼개지는가** 단일 화면 최대 액션 수(25). S0(내 매물 관리)·S7(거래이력·후기)·파트너 라운지 진입이 전부 여기서 갈라지는데, 그 위에 게임화 잔재(퀘스트 이력·뱃지 탭)가 같은 탭 줄에 얹혀 있다.

##### FR-1 고정헤더 + 소셜·액션 행
- **조건** 로그인 사용자가 탭바 프로필 탭 또는 설정 프로필 카드로 진입. 시트를 내린(접은) 상태.
- **와이어** `H` 배경 이미지 위 아바타 · 닉네임 · (플래그 OFF 시 숨김: 레벨 배지 / LV 레벨바 / 주행거리 오도미터) · 우상단 [설정] 아이콘 / `B` 소셜 섹션: [팔로워 n][팔로잉 n] 2칸 + 액션행 [공유(QR)] [친구추가] [채팅(미읽음 배지)] / `K` 탭바 / 아래에 드래거블 시트(FR-2~5)가 겹쳐 올라옴.
- **요소 표**

| 영역 | 요소 | 무게 | 성격 | 탭하면 | 왜 여기 |
|---|---|---|---|---|---|
| H | 설정 아이콘 | 보조 | 이동 | `/settings` | 헤더에는 단계 불변·안전한 것만 — 규칙에 맞음 |
| B | 팔로워 / 팔로잉 카운트 | 보조 | 이동 | `/followers/:id` · `/following/:id` | 소셜 지표. 거래 여정 기여 없음 |
| B | 공유(QR 시트) | 보조 | 이동 | QR 바텀시트 | 오프라인 친구추가용 — B1 어느 단계(S0~S7)에도 붙지 않는 소셜축. P-1에 따라 거래 진입과 같은 행에 둘 근거 없음 → 제안 ②에서 격하 |
| B | 친구추가 아이콘 | 보조 | 이동 | `/friends/add` | 위와 동일(소셜축, 거래 여정 기여 없음) → 제안 ②에서 격하 |
| B | 채팅 아이콘 + 미읽음 배지 | 보조 | 이동 | `/dm` | S3 재진입 경로지만 탭바에 같은 경로가 상시 있음. B1 N축 "항상 같은 자리"에 어긋나는 중복 → 제안 ①에서 제거 |
| H | 레벨 배지 / LV 레벨바 / 주행거리 오도미터 | 표시 | 표시 | 없음 | 게임화 잔재. **`SHOW_LEGACY_GAME_ECONOMY` / `SHOW_LIFETIME_DISTANCE` 플래그로 이미 가려져 있음** — 코드만 남은 상태(제거는 별도 작업) |
- **주행동** 없음(허브 상단. 실제 행동은 시트 안에서 일어난다).
- **제안** ① 액션행에서 [채팅] 제거 — 탭바 `/dm` 과 동일 경로 중복. ② [공유(QR)]·[친구추가]는 상시 노출에서 빼고 헤더 [설정] 시트 안 또는 시트 상단 **접힘 액션 열**(B0-4 앱 내 기존 패턴, 판매자 상세와 동일)로 격하 — 소셜축이라 P-1상 거래 진입과 같은 묶음이 될 수 없다. ③ 비운 자리에 S0(내 매물)·S7(거래이력) 진입 2개를 올려, 시트를 올리지 않은 초기 상태에서 거래축 진입이 보이게 한다. **검증**: 시트를 접은 초기 화면에서 (a) 상시 노출 액션이 [설정]+거래축 2개로 줄었고, (b) `/dm` 진입점이 화면당 1개이며, (c) 스크롤 없이 "내 매물"·"거래이력" 두 진입이 보이는지 확인.
- **실패 시나리오** 헤더가 화려하고(그래프·아바타) 정작 S0·S7 진입은 시트를 끌어올려야 보인다 → "내 매물 어디 갔지".
- **근거** `frontend/src/pages/profile/ProfileMain.tsx:381-383`(설정), `:434,439`(팔로워/팔로잉), `:446-455`(액션행 3종), `:392-396,418-428`(플래그로 가려진 잔재). 인벤토리 `260918_flow_inventory_D_trust.md:302-339`(25액션 전수표).
- **판정(r1)** ☐

##### FR-2 시트 상단 — 인증 카드 + 파트너 라운지 요약(파트너 계정)
- **조건** `activeBizProfile` 존재(APPROVED 업체 보유). 시트를 올린 상태.
- **와이어** `B`(시트) 최상단: 휴대폰 인증 카드(미인증이면 진행 가능 스타일 + 탭 가능 / 완료면 완료 스타일 + 비활성) → **파트너 라운지 요약 카드**(미답변 수 배지) → 탭 줄 → 진입 행들(FR-3).
- **요소 표**

| 영역 | 요소 | 무게 | 성격 | 탭하면 | 왜 여기 |
|---|---|---|---|---|---|
| B | 휴대폰 인증 카드(미인증) | **주행동** | 진행 | `/auth/phone-verify` | **확정 전제 ③의 게이트.** 인증 없이는 S0 출품 자체가 막히므로 시트 최상단이 맞다 |
| B | 휴대폰 인증 카드(완료·비활성) | 표시 | 표시 | 없음(disabled) | **근거 없음** — 성격이 `표시`로 바뀌었는데 자리·크기는 `주행동`일 때 그대로다. 한 자리에 두 성격이 섞이므로 P-1 위반 → 제안에서 한 줄로 격하 |
| B | 파트너 라운지 요약 카드(미답변 수 배지) | 보조 | 이동 | 단일 업체면 `/biz/manage`, 다중이면 업체 선택 시트 | 파트너에게는 이 화면이 라운지 진입의 **유일한 상시 경로** — 배지(미답변)가 있으니 작업 유입 신호로 정당 |
| O | 업체 선택 시트 행 | 숨김 | 이동 | 선택한 업체의 `/biz/manage` | 다중 업체 보유자만. 빈도 낮음 → 숨김 자리 적정 |
- **주행동** 미인증이면 인증하기, 인증 완료 파트너면 라운지 진입.
- **제안** ① 인증 완료 시 카드를 없애고 FR-3의 **진입 행 형식**(B0-4 "앱 안에 이미 있는 패턴" — 한 줄 행 + 우측 상태 텍스트)으로 격하해 시트 하단(설정 근처)으로 내린다. 미인증일 때만 카드 형태로 최상단에 남긴다. ② 그 자리를 파트너는 라운지 요약 카드가, 비파트너는 거래 축 진입 행(내 매물·거래)이 받는다. **검증:** 인증 완료 계정으로 시트를 올린 직후(스크롤 0) 첫 요소가 라운지 요약 카드(파트너) 또는 [내 매물] 행(비파트너)이고, 완료된 인증 항목이 차지하는 높이가 진입 행 1행과 같은가.
- **실패 시나리오** 인증 완료 파트너가 시트를 올릴 때마다 "이미 끝난 카드"를 지나쳐야 라운지에 닿는다.
- **근거** `ProfileMain.tsx:480-498`(인증 카드 분기), `:326-336,510-530`(라운지 요약·openBizLounge), `:1010-1027`(업체 선택 시트).
- **판정(r1)** ☐

##### FR-3 시트 중단 — 거래 축 진입(S0·S7 분기점)
- **조건** 시트를 올린 상태. 파트너/비파트너 공통(맨 아래 한 행만 분기).
- **와이어** `B`(시트) 진입 행 3~4개: [내 매물]([market/search?mine=1]) → [다른 사람에게 보이는 내 프로필] → (비파트너만) [비즈니스 파트너 시작하기] → **거래 섹션**: 헤더 "거래" + 우측 [전체 보기] / [구매|판매] 서브탭 / 거래 행 목록(행마다 [리뷰 남기기]).
- **요소 표**

| 영역 | 요소 | 무게 | 성격 | 탭하면 | 왜 여기 |
|---|---|---|---|---|---|
| B | 내 매물 진입 행 | 보조 | 이동 | `/market/search?mine=1` | **S0(출품·관리)로 가는 상시 경로.** 판매자가 F-S0-02(판매자 상세)에 닿는 실제 입구 |
| B | 다른 사람에게 보이는 내 프로필 | 보조 | 이동 | `/profile/:내 id`(F-P-01 FR-3) | 자기관리 화면과 공개 화면을 겹치지 않고 분리한 결과 — 의도된 설계(2026-09-10 대표 요청) |
| B | 비즈니스 파트너 시작하기 | 보조 | 이동 | `/biz/status` | 비파트너에게만. 파트너는 FR-2 요약 카드로 승격 — 중복 없음 |
| B | 거래이력 [전체 보기] | 보조 | 이동 | `/trades`(F-S7-02) | **S7로 가는 경로.** 요약은 여기, 전량은 이력 화면 — 위계 정당 |
| B | 구매 / 판매 서브탭 | 보조 | 표시 전환 | 목록 전환 | 사용자가 자기 역할로 나눠 보는 축. 앱 내 세그먼트 칩 관례와 같음 |
| B | 거래 행 탭 | 보조 | 이동 | `/market/:id` | **B1 S7(완료·사후)의 되돌아오는 경로이므로 행 자체는 정당.** 다만 B1이 적은 S7 표면화 자리는 "DM C(후기 배너)·이력"이고, 현재 목적지 `/market/:id` 는 그 축 밖이다 — 존재 이유가 아니라 목적지가 어긋난 행이며 F-S7-02 FR-1이 같은 문제를 들고 있다 |
| B | 리뷰 남기기(행 내 버튼) | **주행동** | 진행 | `ReviewSheet` 열림 | S7의 성공 신호(후기 제출)가 목록 안에 있음 — 후기 미작성 거래가 있을 때만 의미가 있으므로 조건부 승격이 맞다 |
- **주행동** 미작성 후기가 있으면 리뷰 남기기, 없으면 내 매물.
- **제안** ① 거래 행 탭의 목적지를 `/market/:id` 에서 **그 거래의 DM 방**으로 바꾼다 — B1 S7이 표면화되는 자리가 "DM C(후기 배너)"이므로 이력 행의 착지점도 같아야 한다. 같은 행동이 두 화면에서 다른 곳에 떨어지지 않도록 F-S7-02 FR-1과 **한 판정으로 묶고**, 그 프레임이 매물 상세 유지로 판정되면 이 프레임도 그대로 따른다. **검증:** 후기 미작성 거래 행 하나를 이 시트에서 탭했을 때와 `/trades`(F-S7-02)에서 탭했을 때 도착 화면이 같고, 도착 화면에서 상대·약속·후기 상태를 추가 이동 없이 확인할 수 있는지. ② 시트 안 순서를 [내 매물]·[다른 사람에게 보이는 내 프로필]·**거래 섹션** → 탭 줄(내 피드·퀘스트·뱃지) 순으로 고정한다 — B1에서 P축의 역할이 "S0·S7 진입이 갈라지는 상시 허브"이고 게임화는 확정 전제 ④(의도적 폐기) 대상이므로 위계가 뒤집히면 안 된다. **검증:** 시트를 최대로 올린 직후 스크롤 0 상태에서 거래 섹션 헤더("거래")까지가 모두 보이고, 탭 줄이 그보다 아래에 있는지.
- **실패 시나리오** 후기 미작성 거래가 목록 하단에 밀리면 S7이 끝나지 않는다(후기 제출률 하락).
- **근거** `ProfileMain.tsx:669-677`(내 매물), `:682-690`(공개 미리보기), `:694-711`(파트너 시작), `:717-721`(전체 보기), `:723-737`(서브탭), `:761-762,1030-1036`(행 탭·리뷰 시트).
- **판정(r1)** ☐

##### FR-4 시트 탭 — 내 피드(게시물 CRUD의 유일한 자리)
- **조건** 시트 탭 줄에서 `feeds` 선택(기본값).
- **와이어** `B`(시트) 탭 줄 [내 피드 | 퀘스트 이력 | 뱃지] → 우측 [새 게시물] → 피드 카드 목록(카드마다 우상단 [⋮]) → 하단 [더보기].
- **요소 표**

| 영역 | 요소 | 무게 | 성격 | 탭하면 | 왜 여기 |
|---|---|---|---|---|---|
| B | 새 게시물 작성 | 보조 | 진행 | `/feed/new` | 커뮤니티 축의 작성 진입. 거래 여정과 무관 |
| B | 피드 카드 탭 | 보조 | 이동 | `/feed/post/:id` | 자기 글 열람 |
| B | 피드 카드 케밥(⋮) | 숨김 | — | 드롭다운 열림 | 숨김 자리 — 빈도상 적정 |
| B | 게시물 수정(드롭다운) | 숨김 | 진행·가역 | `/feed/edit/:id` | **이 앱에서 피드 수정이 가능한 유일한 자리.** 정작 글을 보고 있는 화면(`/feed/post/:id`)에는 진입점이 없다 |
| B | 게시물 삭제(드롭다운) | **파괴적** | 종료·비가역 | 확인 다이얼로그 → 삭제 | 확인 1회를 거치므로 P-1 충족 |
| B | 더보기(페이지네이션) | 보조 | 표시 | 다음 페이지 | 관례 |
- **주행동** 새 게시물(커뮤니티 축 기준). 거래 여정 기준으로는 주행동 없음.
- **제안** ① 피드 상세(`/feed/post/:id`)가 **자기 글일 때만** 헤더에 [더보기]를 열고 그 시트에 [수정] → 구분선 → [삭제]를 둔다. 수정은 일반 행, 삭제는 B0-4 "위험 톤 메뉴 행" + `useConfirmStore` 확인 1회를 그대로 쓰고, 진행(수정)과 종료(삭제)를 구분선으로 갈라 P-1을 지킨다. 당근마켓이 자기 글 상세의 헤더 더보기에서 수정·삭제를 처리하는 것과 같은 자리다(B0-2). ② 이 프레임(프로필 내 피드 탭)의 케밥은 **제거하지 않고 목록 일괄 관리용으로 유지**한다 — 여러 글을 훑으며 정리하는 동선은 상세로 대체되지 않는다. 중복 판정은 F-CM-01 FR-2와 함께 본다. **검증:** 자기 글 상세에서 수정 화면까지 [더보기]→[수정] 2탭 이내이고 프로필 경유가 0회인가, 남의 글 상세에는 [더보기]가 수정·삭제 없이(신고만) 뜨는가, 삭제는 두 경로 모두 확인 다이얼로그 1회를 거치는가.
- **실패 시나리오** 오탈자를 고치려는 사용자가 상세 → 프로필 → 탭 → 케밥까지 4단계를 거친다.
- **근거** `ProfileMain.tsx:660-666`(탭 줄 TABS 3종), `:774-776`(새 게시물), `:793`(카드 탭), `:823-836`(케밥·수정·삭제), `:338-353`(삭제 확인), `:841-849`(더보기). 인벤토리 `260918_flow_inventory_B_comms.md`(교차 화면 2번 — 수정/삭제 경로 분산).
- **판정(r1)** ☐

##### FR-5 퀘스트 이력 · 뱃지(게임화 잔재, 코드만 존재 — UI 도달 불가)
- **조건** 코드상 시트 탭 줄에서 `history` 또는 `badges`를 선택하면 진입하는 화면이지만, **탭 줄 자체가 CSS로 숨겨져 있고**(`ProfileMain.module.css:921-922` `.tabRow { display: none !important; /* SGR-287: 피드/이력/뱃지 탭 제거(코드 보존, 피드만 노출) */ }`) 기본 탭도 `feeds`로 고정돼 있어, **현재 UI에서는 이 화면에 도달할 방법이 없다.**
- **와이어** (참고용 — 실제로는 렌더되지 않음) `B`(시트) history: 퀘스트 완료 행 목록(체크 썸네일 · 제목 · 완료일 + 주행거리 km · 안전등급 A/B 칩) + [더보기] / badges: 뱃지 그리드(획득/미획득 흐림) → 셀 탭 시 상세 모달.
- **요소 표**

| 영역 | 요소 | 무게 | 성격 | 탭하면 | 왜 여기 |
|---|---|---|---|---|---|
| B | 탭 [퀘스트 이력] | 숨김 | — | 도달 불가(탭 줄이 `display:none`) | SGR-287로 이미 숨겨짐. 배치를 판정할 대상이 아니라 **존치 여부**만 남았다 |
| B | 탭 [뱃지] | 숨김 | — | 도달 불가(위와 같음) | 위와 같음 |
| B | 퀘스트 행(주행거리 km · 안전등급 A/B) | 숨김 | — | 도달 불가 | RP·미션 피벗의 잔재 — 확정 전제 ④(게임화 의도적 폐기)에 따르면 존치 근거가 없다. 탭 줄이 숨겨져 있어 사용자는 볼 수 없다 |
| B | 뱃지 셀 → 상세 모달 | 숨김 | — | 도달 불가 | 위와 같음 |
| B | 퀘스트 이력 [더보기] | 숨김 | — | 도달 불가 | 위와 같음 |
- **주행동** 없음(화면 자체에 도달 불가).
- **제안** 이 프레임의 판정 대상은 배치가 아니라 **잔재 코드의 처분**이다. 기본안은 **제거** — 확정 전제 ④(게임화 의도적 폐기)에 따라 `TABS` 배열의 `history`·`badges` 항목, 두 탭의 렌더 분기(퀘스트 이력 목록·[더보기]·뱃지 그리드·상세 모달), 그 분기에서만 쓰이는 상태·핸들러, 그리고 숨김만을 위해 남아 있는 `.tabRow { display: none !important }` 규칙을 함께 걷어낸다(탭이 `feeds` 하나면 탭 줄 자체가 불필요하다). 존치를 택한다면 **되살릴 조건(플래그 이름 · 시점 · 담당)을 이 자리에 적는다** — 조건 없는 존치는 선택지로 두지 않는다. **검증 기준** 제거안은 `ProfileMain.tsx`·`ProfileMain.module.css`에서 `history`·`badges`·`tabRow` 식별자가 0건이고 프로필 시트가 피드 목록만 렌더하며 FR-4의 기존 동작(새 게시물 · 카드 탭 · 케밥 수정/삭제 · 더보기)이 그대로 통과하는 것, 존치안은 되살릴 조건 세 항목이 문서에 기록되는 것으로 확인한다. **"게임화 잔재 일괄 삭제"는 여전히 범위 밖(B3)** — 이 프레임은 이 화면 하나에 한정한다.
- **실패 시나리오** 없음(사용자 도달 불가) — 다만 코드가 남아 유지보수 시 혼동을 줄 수 있다(죽은 분기인 줄 모르고 수정하는 경우).
- **근거** `frontend/src/pages/profile/ProfileMain.module.css:921-922`(`.tabRow` 숨김, SGR-287), `ProfileMain.tsx:363-367`(TABS 배열에 history·badges 여전히 존재), `:855-905`(퀘스트 이력 목록·주행거리·안전등급, 도달 불가 코드), `:907-932`(뱃지 그리드·모달, 도달 불가 코드). 확정 전제 ④(B0-1).
- **판정(r1)** ☐

---

#### F-CS-01 고객센터(문의·신고 접수) — `/settings/support`
- **단계** S6(만남·교환) 실패·사기 지점과 X(취소·교착)의 안전판 · **상태** 제안 · **등급** A(깊게) · **적용 대상** 2탭(문의/신고) + 새 문의 폼 + 신고 상세 시트
- **X 레인과의 관계** 신고 **접수**는 F-X-02(진입점 매트릭스, 5개 화면에 분산)가 다룬다. 이 F-ID는 접수 **이후**를 다룬다 — 내가 낸 신고/문의의 조회·취소·추적. 두 프레임은 겹치지 않는다: F-X-02는 "어디서 신고하나", F-CS-01은 "신고한 뒤 어떻게 되나".

##### FR-1 문의 탭
- **조건** `/settings/support` 진입(설정 · 정지 안내 화면 · 거래 화면 딥링크 `inquiryDraft`). `?tab=` 기본값.
- **와이어** `H` [뒤로] "고객센터" / `C` 탭바 [문의 | 신고] / `B` 목록 상단 [새 문의] 채움(brand-500) full-width 버튼 → 문의 카드 목록(제목 · 상태 배지 · 날짜) / `K` 없음.
- **요소 표**

| 영역 | 요소 | 무게 | 성격 | 탭하면 | 왜 여기 |
|---|---|---|---|---|---|
| C | 탭 [문의 · 신고] | 보조 | 표시 전환 | URL 쿼리 `?tab=` 반영 | 두 축(내가 물은 것 / 내가 신고한 것)을 한 화면에 모은 구성. 딥링크로 특정 탭에 바로 떨어뜨릴 수 있어 S6 실패 지점에서 유용 |
| B | 새 문의 | **주행동** | 진행 | 폼 뷰로 전환(FR-3) | 이 화면에 오는 가장 흔한 이유 |
| B | 문의 카드 탭 | 보조 | 이동 | `/settings/support/:id`(F-CS-02) | 진행 중 문의의 답변 확인 |
| B | 로딩/빈/에러 구분 | 표시 | 표시 | 없음 | B1 CS 단계의 지배적 불안이 "접수가 된 건가", 성공 신호가 "티켓 접수·운영자 답변 확인"이므로 이 목록은 **조회 성공(0건)과 조회 실패를 구분해** 보여야 단계 목표를 만족한다. 현재는 초기 `[]`로 렌더하고 실패를 `.catch(() => {})`로 삼켜 **조회 실패가 "문의 0건"으로 위장된다** — 불안을 해소할 자리가 불안을 만든다 |
- **주행동** 새 문의.
- **제안** ① 목록 조회를 `로딩 / 성공(0건) / 성공(n건) / 실패` 네 상태로 분기한다. `.catch(() => {})`를 에러 상태 set으로 바꾸고, 실패 시 목록 자리에 `StateBlock`("문의를 불러오지 못했습니다" + [다시 시도])을 렌더한다 — B0-4 기존 패턴 "빈 상태 CTA 3종"을 에러 분기로 재사용하며, 성공(0건)과 실패는 서로 다른 문구·다른 CTA를 쓴다. **검증** 문의 목록 API를 실패로 강제(오프라인/500)했을 때 "문의가 없습니다"가 아니라 에러 문구와 [다시 시도]가 노출되고, [다시 시도] 성공 시 같은 화면에서 목록이 채워진다. ② 새 문의 버튼은 이미 채움 full-width(`CustomerSupport.module.css` `.newBtn`, brand-500)다 — **이미 충족, 판정 불필요.**
- **실패 시나리오** 송금 사기를 당한 직후 접속했는데 네트워크가 흔들려 목록이 비어 보인다 → "접수가 안 됐다"고 판단하고 중복 문의를 넣거나 포기한다.
- **근거** `frontend/src/pages/settings/CustomerSupport.tsx:47-52,142-157`(탭), `:162-164`(새 문의), `:172-187`(문의 카드). 인벤토리 `260918_flow_inventory_D_trust.md:236-265`(8액션 전수표 + 조용한 catch 결함).
- **판정(r1)** ☐

##### FR-2 신고 탭(접수 이후 — 조회·취소)
- **조건** `?tab=` 신고 선택. 내가 접수한 신고 목록.
- **와이어** `C` 탭바 / `B` 신고 카드 목록 — 카드 본문(탭 시 상세 시트)과 우측 [신고 취소] 버튼이 **형제 구조** / 로딩은 스켈레톤, 빈 상태는 `StateBlock`.
- **요소 표**

| 영역 | 요소 | 무게 | 성격 | 탭하면 | 왜 여기 |
|---|---|---|---|---|---|
| B | 신고 카드 본문 탭 | 보조 | 이동 | 신고 상세 시트(FR-4) | 접수 내용 확인 |
| B | 신고 취소 버튼 | **파괴적** | 종료·비가역 | 확인 다이얼로그 → 취소(재신고 불가) | 확인 1회를 거치므로 P-1 충족. 다만 **취소 버튼이 카드 본문과 같은 줄·형제**라 오탭 거리(조회 의도가 취소를 때릴 수 있음) |
| B | `can_cancel` 조건 | 표시 | 표시 | 없음 | 서버가 `PENDING`만 true — 처리 시작된 신고는 취소 불가. 정당 |
| B | 로딩/빈/에러 구분 | 표시 | 표시 | 없음 | 로딩·빈은 구분되나 **에러는 조용한 catch**(문의 탭과 같은 결함) |
- **주행동** 없음(조회 축). 취소가 유일한 행동이나 예외 경로다.
- **제안** ① 목록 카드에서 [신고 취소] 버튼을 제거하고 **상세 시트(FR-4) 하단의 단독 위험 톤 행**으로 옮긴다 — 목록은 `이동`만 남는 조회 축이 되고, 취소는 B0-4 기존 패턴 "위험 톤 메뉴 행" + "확인 다이얼로그(`useConfirmStore`)" 두 개를 그대로 쓴다. `can_cancel=false`면 시트에서 버튼 자체를 렌더하지 않는다(비활성 노출 아님). **검증** 신고 목록 화면에 파괴적 무게 요소가 0개이고, 취소 실행에 `카드 탭 → 시트 [신고 취소] → 확인 다이얼로그 승인` 3단계가 요구된다. `PENDING` 아닌 신고의 시트에는 취소 행이 없다. ② 목록 조회를 `로딩 / 성공(0건) / 성공(n건) / 실패` 네 상태로 분기한다 — FR-1 ①과 같은 처리를 신고 탭에도 적용해 실패 시 `StateBlock`("신고 내역을 불러오지 못했습니다" + [다시 시도])을 렌더한다. **검증** 신고 목록 API를 실패로 강제(오프라인/500)했을 때 "신고 내역이 없습니다"가 아니라 에러 문구와 [다시 시도]가 노출되고, [다시 시도] 성공 시 같은 화면에서 목록이 채워진다.
- **실패 시나리오** 신고 내용을 다시 읽으려다 옆의 [신고 취소]를 눌러 확인 다이얼로그를 보게 된다. 다이얼로그를 습관적으로 승인하면 **재신고가 불가능해진다.**
- **근거** `CustomerSupport.tsx:205-233`(카드), `:234-241,89-105`(취소·확인 다이얼로그·`can_cancel`). 인벤토리 `260918_flow_inventory_D_trust.md:236-265`.
- **판정(r1)** ☐

##### FR-3 새 문의 폼
- **조건** FR-1에서 [새 문의] 탭, 또는 거래 화면에서 `inquiryDraft` 딥링크로 초안이 프리필된 상태로 진입.
- **와이어** `B` 제목 인풋 → 본문 텍스트에어리어 → 하단 [보내기] 채움 버튼(제목·본문 모두 입력해야 활성).
- **요소 표**

| 영역 | 요소 | 무게 | 성격 | 탭하면 | 왜 여기 |
|---|---|---|---|---|---|
| B | 제목 / 본문 입력 | 보조 | 진행 | 입력 | 필수 2개(제목·본문)뿐인 최소 폼 — 카테고리·첨부 없음. 초기값은 딥링크 진입 시 본문 프리필이 전부이고, 그 외 경로는 두 칸 모두 빈 칸 + 무엇을 적어야 하는지 안내 없음 |
| B | 보내기 | **주행동** | 진행 | 티켓 생성 → 목록 복귀 | 폼의 목적 |
| B | 사진 첨부 | 보조 | 진행 | — | **없다(부재).** 사기·상품 불일치 문의는 증거가 사진인데 텍스트만 받는다 |
| B | 거래 연결(어느 거래 건인지) | 표시 | 표시 | — | **없다(부재).** 딥링크 `inquiryDraft`로 본문에 텍스트를 프리필할 뿐, 티켓이 거래 id를 구조적으로 들고 가지 않음 **(프리필 내용의 정확한 형태는 미확인 — 거래 화면은 이번 조사 범위 밖)** |
- **주행동** 보내기.
- **제안** ① `inquiryDraft` 딥링크로 들어온 문의는 본문 텍스트 프리필 대신 **거래 id를 티켓 필드로** 싣고, 화면에는 B0-4 기존 패턴인 **상단 고정 진행 배너**와 같은 형태의 `C` 컨텍스트 행("○○ 거래 건 문의")으로 어느 거래인지 고정 표시한다 — 검증: 딥링크로 접수한 티켓을 어드민에서 열었을 때 본문을 읽지 않고도 거래 건을 특정할 수 있다. ② 본문 아래에 **사진 첨부 슬롯(최대 3장, 선택)**을 둔다 — 당근마켓 문의(B0-2)도 사진은 선택이므로 필수 입력 개수는 2개 그대로다. 검증: 사진 0장에서도 [보내기]가 활성이고, 첨부한 사진이 티켓 상세에 남는다. ③ **제목 필수를 해제**하고 본문만 필수로 둔다 — S6 사고 직후(B1 "돈만 날렸다") 필수 2개는 마찰이 크고, 목록 카드 제목은 본문 첫 줄로 대체 가능하다. 검증: 본문만 채운 상태에서 보내기가 활성되고, FR-1 목록 카드에 본문 첫 줄이 제목 자리에 뜬다. ④ 딥링크가 아닌 진입의 **빈 폼에 안내가 없다** — 본문 플레이스홀더에 운영자가 되묻게 되는 항목(언제 · 어느 거래 · 무엇이 문제인지)을 문장으로 제시한다. 검증: 첫 진입 시 세 항목이 플레이스홀더로 보이고, 입력 시작과 함께 사라진다. ①②를 붙이지 않는다면 운영자가 매번 되물어야 한다.
- **실패 시나리오** "돈을 보냈는데 물건을 못 받았다" 문의가 거래 id 없이 들어와 운영자가 특정하지 못한다 → 왕복이 늘고, 그 왕복을 받을 UI가 또 없다(F-CS-02).
- **근거** `CustomerSupport.tsx:255-271`(입력), `:273-279,118-130`(제출). 진입 `Settings.tsx:186`, `Suspended.tsx:63`, `TradeTransaction.tsx`(딥링크).
- **판정(r1)** ☐

##### FR-4 신고 상세 시트
- **조건** FR-2에서 신고 카드 본문 탭.
- **와이어** `O` 바텀시트: 신고 대상 · 사유 · 접수일 · 상태 → 하단 [대상으로 이동] 채움 버튼(매물 신고일 때만).
- **요소 표**

| 영역 | 요소 | 무게 | 성격 | 탭하면 | 왜 여기 |
|---|---|---|---|---|---|
| O | 신고 내용(대상·사유·상태) | 표시 | 표시 | 없음 | 접수 이후의 유일한 추적 지점 |
| O | 대상으로 이동 | 보조 | 이동 | `/market/:listing_id` | **`LISTING` 타입에서만 동작한다.** USER·DM·POST·BIZ 신고에는 버튼이 없어 "내가 누굴 신고했는지" 화면으로 확인할 수 없음 |
| O | 신고 취소 | 파괴적 | 종료·비가역 | — | **여기에 없다**(목록 카드에 있다 — FR-2 제안이 이 자리로 옮기자는 것) |
- **주행동** 없음(조회 전용).
- **제안** 대상 이동을 5개 신고 타입 전부로 확장할지 판정 요청. 확장안의 목적지는 `LISTING→/market/:listing_id`(현행) · `USER→/profile/:userId`(F-P-01) · `POST→/feed/post/:id`(F-CM-02) · `BIZ→/biz/:id`(F-BZ-01) 넷으로 고정하고, DM 신고와 대상이 삭제·탈퇴된 건은 이동 버튼 대신 사유를 적은 `표시` 행으로 대체한다(B0-4 무게 `표시` — 누를 수 없는 것을 버튼 모양으로 두지 않는다). 확장하지 않는 판정이면 LISTING의 이동 버튼도 빼고 5개 타입 전부 같은 `표시` 행으로 통일한다 — 지금처럼 한 타입만 버튼인 상태는 같은 자리·같은 모양의 요소가 타입에 따라 있다가 없다가 하는 것이고(A1 Q4), B1의 CS 축 정의("접수 이후 — 조회·취소·추적")에서 추적을 타입별로 불균등하게 남긴다. FR-2에서 신고 취소가 이 시트로 내려오는 판정이면 B0-4 `파괴적` 규약대로 위험 톤 + `useConfirmStore` 확인 1회를 붙인다. **검증** 5개 타입 신고를 1건씩 열어 — 확장 판정이면 DM 외 4종이 각 상세로 진입하고 DM·삭제 대상은 `표시` 행이 보이며, 미확장 판정이면 5종 전부 `표시` 행이다. 이동 가능 여부를 화면에서 구분할 수 없는 타입이 하나도 없으면 통과.
- **실패 시나리오** 사용자 신고 건을 열었는데 누구였는지 알 수 없어, 그 사람을 차단하러 갈 수도 없다(F-P-01 FR-2의 차단 부재와 겹쳐 막다른 길이 된다).
- **근거** `CustomerSupport.tsx:84-86,348-359`(대상 이동, LISTING 한정).
- **판정(r1)** ☐

---

#### F-CS-02 문의 상세(답변 스레드) — `/settings/support/:id`
- **단계** S6·X의 안전판 후속 · **상태** 제안 · **등급** A(깊게)

##### FR-1 조회 전용 스레드 — 답글 API는 있는데 입력창이 없다
- **조건** F-CS-01 FR-1에서 문의 카드 탭.
- **와이어(현재)** `H` [뒤로] / `B` 상태 배지 → 문의 제목·본문 → "답변" 구분선 → 답변 버블 목록(운영자/본인 구분 스타일) 또는 "답변 없음" 문구 / `K` **없음(입력창 없음).**
- **와이어(제안)** 위와 같고 `K`에 **답글 입력창 1줄 + [보내기]**를 붙인다(티켓이 종결 상태면 비활성 + 사유 문구).
- **요소 표**

| 영역 | 요소 | 무게 | 성격 | 탭하면 | 왜 여기 |
|---|---|---|---|---|---|
| B | 상태 배지 | 표시 | 표시 | 없음 | 진행 여부를 아는 유일한 신호 |
| B | 답변 버블 목록 | 표시 | 표시 | 없음 | 운영자 답변 열람 |
| K | 답글 입력창 + 보내기 | **주행동** | 진행 | `POST /support/tickets/{id}/replies` | **백엔드 엔드포인트가 이미 있다**(`backend/app/routers/support.py:117`). 프론트만 없어 사용자가 추가 설명·증거를 못 보낸다 — 사기 문의는 한 번에 끝나지 않는다 |
| B | 로딩 / 실패 구분(실패 갈래에 [다시 시도]) | 표시(실패 시 보조) | 표시 / 이동 | 티켓 재조회 | B1 CS 단계의 지배적 불안이 "접수가 된 건가"이고 성공 신호가 "운영자 답변 확인"인데, `ticket`이 null이면 헤더만 렌더돼 로딩과 실패가 같은 화면이 된다 — 안전판 화면이 영구 로딩으로 보이면 그 불안이 해소되지 않는다. 실패 갈래는 B0-4 기존 패턴 **빈 상태 CTA 3종**을 그대로 쓴다 |
- **주행동** (현재) 없음 — 읽기만. (제안) 답글 보내기.
- **제안** ① `K`에 답글 입력창 1줄 + [보내기]를 붙인다 — B0-4 기존 패턴 **하단 바**를 쓰고, 성공 시 낙관적 append 없이 티켓 재조회로 버블 목록을 갱신한다. 티켓이 종결 상태면 입력창을 비활성화하고 사유 문구("종결된 문의입니다 — 새 문의를 작성해 주세요")를 자리에 둔다. 백엔드 변경 없음(프론트 단독). *검증 기준:* 운영자 답변이 달린 티켓에서 사용자가 화면을 떠나지 않고 답글을 보내면 같은 스레드에 버블이 추가되고, `F-CS-01` 목록에 새 티켓이 생기지 않는다. ② `ticket === null`을 로딩과 실패로 갈라, 로딩은 스켈레톤, 실패는 B0-4 **빈 상태 CTA 3종** 패턴의 문구 + [다시 시도]를 렌더한다. *검증 기준:* 네트워크를 끊고 진입하면 무한 스피너 대신 실패 문구와 [다시 시도]가 보이고, 연결 복구 후 [다시 시도] 한 번으로 스레드가 뜬다.
- **실패 시나리오** 운영자가 "거래 id를 알려주세요"라고 답했는데 사용자가 답할 자리가 없다 → 새 문의를 또 만든다(중복 티켓) 또는 포기한다.
- **근거** `frontend/src/pages/settings/SupportDetail.tsx:25-56`(조회 전용, 입력 폼 없음 — 파일 전체 63줄), `backend/app/routers/support.py:117`(답글 엔드포인트 존재). 인벤토리 `260918_flow_inventory_D_trust.md:514`(§고아·도달불가 1번).
- **판정(r1)** ☐

---

#### F-CM-01 커뮤니티 피드 — `/feed`, `/feed/post/:postId`, `/feed/new`, `/feed/edit/:postId`
- **단계** **CM(커뮤니티 — 독립 여정)** · **상태** 제안 · **등급** B(프레임·요소표까지. 화면 샘플은 다음 단계) · **적용 대상** 탭바 커뮤니티 탭 하위 전체
- **왜 독립 여정인가** 거래 여정 화면(S0~S7·X) 어디에서도 피드/그룹을 참조하지 않는다 — 접점은 **단 하나, 타인 프로필(F-P-01 FR-1)의 게시물 레일**뿐이다. 따라서 S0~S7에 끼워넣지 않고 CM 축으로 따로 세운다(§B1 참조).

##### FR-1 피드 목록(커뮤니티 탭 루트)
- **조건** 탭바 커뮤니티 탭. 5필터(전체/내동네/친구/핫/내그룹) 중 기본값.
- **와이어** `H` 좌 [+ 새 글] · 우 [프로필][DM(미읽음 dot)] / `C` 필터 칩 5종 / `B` 2열 그리드 카드(사진·본문 일부·작성자 아바타·닉네임·응원 하트+숫자) → 무한/더보기 / `K` 탭바.
- **요소 표**

| 영역 | 요소 | 무게 | 성격 | 탭하면 | 왜 여기 |
|---|---|---|---|---|---|
| H | 새 글 작성(+) | **주행동** | 진행 | `/feed/new` | 커뮤니티 여정의 생산 행동. 다만 **헤더 좌측 아이콘**이라 무게가 주행동답지 않다(앱 내 다른 작성 진입은 FAB — 마켓·그룹 상세) |
| H | 프로필 아이콘 | 보조 | 이동 | `/profile` | **이유 없음.** B1 에서 개인 프로필은 "모든 단계에서 되돌아오는 상시 허브"이고 그 자리는 탭바가 이미 맡는다 — 목적지가 같은 진입을 헤더에 한 번 더 둘 근거가 없다 → 제안 ①에서 제거 |
| H | DM 아이콘 + 미읽음 dot | 보조 | 이동 | `/dm` | **이유 없음**(목적지가 탭바 DM 탭과 같음). 다만 미읽음 dot 은 앱 안에서 여기에만 있다 — 아이콘이 아니라 **dot 이 유일한 기능**이므로 제안 ①에서 탭바로 옮긴다 |
| C | 필터 칩 5종 | 보조 | 표시 전환 | 목록 재조회(내동네는 GPS 재요청 트리거) | 탐색 축. 칩은 앱 내 관례(세그먼트 칩)와 일치 |
| B | 카드 탭 | 보조 | 이동 | `/feed/post/:id` | 열람 |
| B | 응원(🔥) 토글 | 보조 | 진행·가역 | 낙관적 토글 | 목록에서 가능한 유일한 반응(댓글은 상세 위임) — 위계 일관 |
| B | 아바타·닉네임 탭 | 보조 | 이동 | `/profile/:userId`(F-P-01) | **거래 여정과의 유일한 접점 방향** — 커뮤니티에서 만난 사람을 프로필로 판단하러 갈 수 있다 |
- **주행동** 새 글 작성.
- **제안** ① 헤더 우측 [프로필][DM] 두 아이콘을 제거하고, DM 미읽음 dot 은 탭바 DM 탭 아이콘에 붙인다 — 두 아이콘 다 목적지가 탭바와 같고, 남는 기능인 dot 은 탭바에 있는 편이 어느 화면에서도 보인다(B0-2 당근마켓 헤더는 [뒤로][상대][더보기] 셋 상한). *검증 기준:* `/feed` 헤더에 남는 액션이 작성 진입 하나뿐이고, 읽지 않은 DM 이 있을 때 `/feed` 를 포함한 모든 탭 화면에서 탭바 DM 탭에 dot 이 보인다. ② 새 글 작성을 헤더 아이콘에서 **FAB**(우하단, 탭바 위)로 내려 마켓 목록·그룹 상세의 작성 진입과 같은 자리·같은 모양으로 맞춘다(B0-4 앱 내 기존 패턴). *검증 기준:* 마켓 목록과 피드 목록을 연달아 열었을 때 작성 버튼의 위치·모양이 같고, `/feed/new` 로 가는 경로가 그 하나뿐이다.
- **실패 시나리오** 헤더에 아이콘 3개가 붙어 "작성"이 어느 것인지 즉시 안 보인다.
- **근거** `frontend/src/pages/feed/FeedList.tsx:142-160`(헤더 3종), `:121-203`(필터 5종), `:246-261`(카드), `:129-135,306-314`(응원), `:271-303`(아바타/닉네임). 인벤토리 `260918_flow_inventory_B_comms.md:186-297`.
- **판정(r1)** ☐

##### FR-2 피드 상세 — 내 글인데 고칠 수 없다
- **조건** FR-1 또는 그룹 게시판·프로필 레일에서 카드 탭.
- **와이어** `H` [뒤로] · 우측 [⋮](**타인 글일 때만**, 본인 글이면 자리만 비어 있음) / `B` 사진 캐러셀 → 작성자 행(탭 시 프로필) → 본문 → 댓글 인라인 목록(각 댓글에 [♡][⚑]) / `K` 하단 액션바 [🔥 응원 n] + 댓글 입력 + [↑ 전송].
- **요소 표**

| 영역 | 요소 | 무게 | 성격 | 탭하면 | 왜 여기 |
|---|---|---|---|---|---|
| H | 더보기(⋮) → 신고 | 숨김 | 종료 | 신고 사유 시트(1항목) | 타인 글에만 노출. 종료를 숨김에 — P-1 충족 |
| H | 본인 글의 수정/삭제 | 숨김 | 진행/파괴적 | — | **없다(부재).** 본인 글이면 케밥 자리가 spacer로만 남는다. 수정·삭제는 오직 `/profile` 내 피드 탭(F-P-02 FR-4)에만 |
| K | 댓글 입력 + 전송 | **주행동** | 진행 | 댓글 등록 | 상세 화면의 목적(반응). 하단 고정이 맞다 |
| K | 응원(🔥) | 보조 | 진행·가역 | 토글 | 가벼운 반응을 같은 바에 — 성격 동일(진행·가역), P-1 충족 |
| B | 댓글 좋아요(♡) | 보조 | 진행·가역 | 토글 | 게시물 응원(🔥)과 같은 성격(진행·가역)의 댓글 단위 반응 — CM2(반응·대화)의 성공 신호에 붙는다. 다만 같은 댓글 행에 종료 성격인 [⚑]가 같은 모양으로 나란히 놓여 P-1 위반(V1과 같은 형태) |
| B | 댓글 신고(⚑) | 숨김 | 종료 | 사유 시트 | 타인 댓글만 |
| B | 댓글 삭제 | 파괴적 | 종료 | — | **없다(부재).** 작성자 본인도 자기 댓글을 지울 수 없다 |
- **주행동** 댓글 쓰기.
- **제안** ① 본인 글이면 지금 spacer로 비어 있는 헤더 우측 자리에 [⋮]를 그대로 살려, 시트에 [수정] → 구분선 → [삭제]를 둔다. 수정은 일반 행(`/feed/edit/:id`), 삭제는 B0-4 "위험 톤 메뉴 행" + `useConfirmStore` 확인 1회를 쓰고, 진행(수정)과 종료(삭제)를 구분선으로 갈라 P-1을 지킨다. 타인 글이면 같은 자리 같은 시트에 [신고]만 남긴다 — 자기 글 상세의 헤더 더보기에서 수정·삭제를 처리하는 당근마켓과 같은 자리다(B0-2). **F-P-02 FR-4 제안 ①과 같은 안이며 한 판정으로 묶는다**(프로필 내 피드 탭의 케밥은 목록 일괄 관리용으로 존치). **검증:** 자기 글 상세에서 [⋮]→[수정] 2탭 이내로 수정 화면에 닿고 프로필 경유가 0회인가, 타인 글 상세의 시트에는 [신고]만 뜨는가, 삭제는 상세·프로필 두 경로 모두 확인 다이얼로그 1회를 거치는가. ② 본인 댓글의 [⚑] 자리를 [⋮]로 바꿔 [삭제](위험 톤 행 + `useConfirmStore` 확인 1회)를 둔다 — 한 숨김 슬롯이 작성자 여부로만 갈리므로 진행/종료가 한 줄에 섞이지 않는다(P-1, 요소 표 ♡ 행 참조). 글 삭제와 같은 확인 문법을 쓴다(B0-4 확인 다이얼로그). **검증:** 본인 댓글에 [⚑]가 보이지 않고 [⋮]→[삭제]→확인 1회로 지워지는가, 타인 댓글은 [⚑](신고)만 그대로인가.
- **실패 시나리오** 게시물을 못 찾으면 전용 에러 화면 없이 토스트 + `/feed`로 튕겨낸다 — 딥링크로 들어온 사용자가 이유를 모른 채 목록에 떨어진다.
- **근거** `frontend/src/pages/feed/FeedDetail.tsx:156-162`(케밥 조건·spacer), `:265-290`(하단 액션바), `:235-250`(댓글 반응·신고), `:61-65`(못 찾음 처리). 인벤토리 `260918_flow_inventory_B_comms.md`(§고아 3번 — 댓글 삭제 부재).
- **판정(r1)** ☐

---

#### F-CM-02 커뮤니티 그룹 — `/community/groups`, `/community/groups/new`, `/group/:slug`
- **단계** **CM(커뮤니티 — 독립 여정)** · **상태** 제안 · **등급** B(핵심 프레임만)
- **이 F-ID의 판정 거리** 개별 요소의 배치가 아니라 **"이 기능이 앱에 존재하는가"** 가 먼저다 — 아래 FR-1 참조.

##### FR-1 그룹 탐색 — 앱 UI에서 도달 불가
- **조건** `/community/groups` 직접 URL 또는 딥링크. **앱 안의 어떤 화면에도 이 라우트로 가는 버튼·링크가 없다.**
- **와이어** `H` 제목 · 우 [+ 그룹 개설] / `B` 필터 칩 [전체|내그룹] → 그룹 카드 목록(행) / `K` 없음.
- **요소 표**

| 영역 | 요소 | 무게 | 성격 | 탭하면 | 왜 여기 |
|---|---|---|---|---|---|
| B | **이 화면으로 오는 진입 버튼** | 보조 | 이동 | — | **이유 없음(도달 불가).** `community/groups`를 참조하는 파일은 라우트 등록(`App.tsx`), Up 내비게이션 착지점(`TopBar.tsx`의 `SECTION_ROOTS`), 자기 자신뿐. 홈·탭바·프로필·피드 어디에도 없다. B1 CM4가 이 상태를 단계 정의 안에 이미 적어두었다(**"단, 그룹 탐색 진입점 자체가 없다"**) — 여정 지도가 이 칸을 빈칸으로 확정한 상태다. B0-1 확정 전제(대면 거래 중개)에 그룹은 직접 기여하지 않고, B1은 CM 축을 거래 여정에 붙이지 않는 독립 여정으로 두면서 연결점을 타인 프로필 게시물 레일 하나로 한정했다. 따라서 **이 빈칸이 "빠뜨린 진입점"인지 "범위 밖이라 없는 것"인지가 곧 존폐 판정**이며, 그 답이 나오기 전에는 이 행의 배치를 정할 수 없다 |
| H | 그룹 개설(+) | 보조 | 진행 | `/community/groups/new` | 아이콘만 — 이 화면에 도달한 사람에게는 유일한 생산 행동인데 무게가 낮다 |
| B | 필터 [전체 · 내그룹] | 보조 | 표시 전환 | 목록 재조회(`mine`은 로그인 필요) | **관성(이전 배치 유지).** 목록 화면이면 필터를 단다는 관례 외에 여정 근거가 없다. B1 CM4의 목표는 "그룹에 들어가 게시판·채팅을 쓴다"이고 [내그룹]은 **이미 가입한 사람의 재진입 경로** — 탐색 목록 안의 필터가 아니라 진입점 쪽에 있어야 할 요소다. 살리는 안에서 진입점을 어디 두느냐에 따라 이 칸의 존재 이유가 정해지므로, 존폐 판정 전에는 배치를 고정하지 않는다 |
| B | 그룹 카드 탭 | **주행동** | 이동 | `/group/:slug`(FR-2) | 탐색의 목적 |
| B | 로딩 / 에러 상태 | 표시 | 표시 | 없음 | 빈 목록만 `StateBlock`. 로딩·에러 UI **미확인**(`useInfiniteScroll` 훅 내부 미검증) |
- **주행동** 그룹 카드 탭. **단 아래 존폐 판정에서 A(살린다)를 고를 때만 유효하다** — B를 고르면 이 프레임에 주행동은 없다.
- **제안** **배치 판정이 아니라 존폐 판정을 먼저 요청한다.** 묻는 것은 "요소를 어디 두느냐"가 아니라 **"그룹 기능을 오픈 범위에 넣느냐"** 하나다. 두 안에 각각 따라오는 작업·영향·검증 기준을 붙여 제시하고, 대표는 A/B 중 하나를 고른다.
  - **A안 — 살린다(진입점을 만든다).**
    - *어디에 두는가*: `/feed`(CM1 둘러보기) 필터 줄의 **세그먼트 칩**(B0-4 기존 패턴 — 마켓 [판매중/예약중]과 같은 부류)을 한 칸 늘려 `[전체 · 동네 · 친구 · 그룹]`으로 두고, `그룹` 칩이 `/community/groups`로 보낸다. 진입점이 커뮤니티 축 안에서 닫히므로 거래축(탭바·홈·매물 화면)을 건드리지 않고, B1이 CM을 독립 여정으로 둔 전제와 충돌하지 않는다.
    - *따라오는 변경*: 진입점이 생기면 목록의 [전체 · 내그룹] 필터는 역할이 겹치므로 [내그룹]은 진입점 쪽(칩 또는 `/profile` 피드 탭 옆)으로 올리고 목록에서는 내린다. 빈 목록은 **빈 상태 CTA**(B0-4 기존 패턴 3종)로 [+ 그룹 개설]을 채움 버튼으로 승격한다 — 헤더 아이콘만이라 무게가 낮던 개설 행동(위 표 H행)이 여기서 같이 해소된다.
    - *검증 기준*: ① `/feed`에서 그룹 목록까지 **2탭 이내** 도달. ② 로그아웃 상태에서 칩 → 목록 → 카드 탭이 끝까지 가고 `mine` 필터만 로그인을 요구. ③ 목록·상세의 로딩·에러 상태가 실제로 렌더되는지 확인(위 표 마지막 행의 **미확인** 해소). ④ 오픈 후 그룹 목록 진입이 0건이 아닐 것.
    - *비용*: FR-2(그룹 상세)와 개설 폼이 미검증 상태로 같이 열린다 — A를 고르면 두 화면의 배치 판정을 다음 라운드에 **추가로** 받아야 한다.
  - **B안 — 접는다(오픈 범위에서 제외).**
    - *라우트·코드 처분*: `App.tsx`의 `/community/groups`·`/community/groups/new`·`/group/:slug` 라우트 등록과 `TopBar.tsx` `SECTION_ROOTS`의 착지점 항목을 제거한다. 화면 코드(`GroupList.tsx`·`GroupCreate.tsx`·그룹 상세)와 서버 API·테이블은 **남긴다** — 도달 경로만 끊는 처분이므로 되살릴 때 라우트 복구로 끝난다.
    - *기존 가입자 영향*: UI 경로가 없으므로 앱에서 가입한 사용자는 없고 딥링크 유입만 가능하다. 다만 **그룹 채팅이 DM 인프라 위에 올라가 있어**(FR-2 표 `채팅방 입장` 행) 이미 생성된 그룹 대화방이 있으면 DM 목록에 계속 남고, 라우트만 내리면 그 방에서 그룹 상세로 올라가는 경로가 끊긴 채 방만 남는다. 따라서 **기존 그룹 대화방·가입 레코드 존재 여부를 먼저 조회**하고, 있으면 그 방을 숨길지 종료 처리할지를 같은 판정에서 정한다.
    - *검증 기준*: ① 라우트 제거 후 `community/groups`·`/group/` 참조 grep 결과가 화면 파일 자신을 빼고 0건. ② 해당 URL 직접 입력 시 흰 화면이 아니라 앱 표준 라우트 폴백으로 착지. ③ 그룹 대화방이 존재하면 DM 목록·채팅방 진입이 에러 없이 동작하거나 정한 대로 숨겨질 것. ④ 커뮤니티 피드(CM1~CM3)와 타인 프로필 게시물 레일이 영향받지 않을 것.
    - *비용*: 구현된 기능을 오픈에서 뺀다. 되살릴 때 A안의 진입점 설계를 그때 다시 해야 한다.
  - **"그대로 둔다(딥링크 전용)"는 판정 후보에서 뺀다** — 도달 경로가 없으면 사용 지표가 0으로 고정돼 어느 쪽 검증 기준도 세울 수 없고, 판단을 오픈 후로 미루는 것에 지나지 않는다.
  - **개설 폼(`/community/groups/new`)과 그룹 상세(`/group/:slug`)는 이 판정에 종속되므로 별도 프레임을 만들지 않는다**(개설 폼은 공개/비공개·자유/승인 칩 + [만들기] 하단 고정 버튼으로 구성 — 근거만 기록).
- **실패 시나리오** 그룹 기능이 구현돼 있는데 아무도 쓸 수 없다. 오픈 후 "그룹 어디 있냐"는 문의가 CS(F-CS-01)로 들어온다.
- **근거** `frontend/src/pages/community/GroupList.tsx:42-49,24-32,54-77,83-103`, `GroupCreate.tsx:27-43,66-118`. 인벤토리 `260918_flow_inventory_B_comms.md:188`(진입점 전수 grep 결과 — 도달 불가 확정), 같은 문서 §고아·도달불가 1번.
- **판정(r1)** ☐

##### FR-2 그룹 상세(게시판·채팅·멤버 3탭)
- **조건** FR-1 또는 딥링크로 `/group/:slug` 진입. 멤버/비멤버/관리자(owner·manager)로 상태가 갈린다.
- **와이어** `H` [뒤로] 그룹명 / `C` 소개 + 멤버 수 + (비멤버면) [가입하기] 채움 버튼(커버 이미지는 렌더되지 않음 — `coverUrl`은 `GroupList`에서만 쓰인다) / `B` 탭 [게시판|채팅|멤버] → 게시판: 게시글 카드 목록(응원 🔥) / 채팅: [채팅방 입장] 채운 버튼 / 멤버: 대기 목록(관리자만, [승인] 아이콘) + 활성 목록([강퇴] 아이콘) / `O` 게시판 탭일 때 글쓰기 FAB(멤버만).
- **요소 표**

| 영역 | 요소 | 무게 | 성격 | 탭하면 | 왜 여기 |
|---|---|---|---|---|---|
| C | 가입하기 | **주행동** | 진행·가역 | 가입 또는 승인 대기 | 비멤버에게는 이것 말고 할 수 있는 게 없다 — 위계 정당 |
| B | 탭 [게시판 · 채팅 · 멤버] | 보조 | 표시 전환 | 탭 전환 | 비멤버 안내 분기가 탭마다 구현돼 있음 |
| O | 글쓰기 FAB | 보조 | 진행 | `/feed/new?groupId=…` | 그룹 게시판 글쓰기를 피드 작성기와 통합 — 구현 중복이 없어 정당. **떠 있는 요소이나 P-2의 "진행 중 세션"이 아니라 앱 표준 FAB**(마켓 등록 FAB와 같은 부류)이므로 F-N-01 정책 대상 아님 |
| B | 채팅방 입장 | 보조 | 이동 | `/dm/:conversationId` | 그룹 채팅이 DM 인프라 위에 올라가 있음 |
| B | 멤버 승인(관리자) | 보조 | 진행·가역 | 승인 | 아이콘만(인라인 style) — 관리 행동인데 발견성이 낮다 |
| B | 멤버 강퇴(관리자) | **파괴적** | 종료·즉시 | **확인 없이 즉시 실행** | 요소 자체는 정당 — CM4(모임)에서 그룹 운영자가 멤버를 정리할 유일한 수단이고 승인의 짝이다. **자리와 마찰이 규약 위반**: B0-4 무게 규약상 `파괴적`은 "위험 톤 + 확인 1회 필수"인데 무확인·평톤이고, 같은 앱의 DM 나가기(`DmList`·`DmDetail`)는 모두 `useConfirmStore` 확인을 거친다. 승인(진행)과 같은 줄·같은 모양인 것도 P-1 위반 |
- **주행동** 비멤버는 가입하기, 멤버는 글쓰기.
- **제안** ① 강퇴를 `useConfirmStore` 확인 1회 뒤로 옮긴다(B0-4 기존 패턴 "확인 다이얼로그" 재사용 — DM 나가기와 같은 호출부 구조, 신규 컴포넌트 불필요). **검증:** 관리자가 강퇴 아이콘을 탭했을 때 확인 없이 멤버가 목록에서 사라지는 경로가 0이고, 마찰이 DM 나가기와 같다(2탭 + 확인 1회 — B1 X 레인 성공 신호). ② 승인·강퇴를 아이콘 단독에서 텍스트 라벨 행으로 바꾸고, 강퇴만 위험 톤으로 분리한다(B0-4 기존 패턴 "위험 톤 메뉴 행" 재사용 — 진행(승인)과 종료(강퇴)를 같은 모양으로 묶지 않는다, P-1). **검증:** 멤버 행에서 승인과 강퇴가 같은 색·같은 모양으로 나란히 놓인 경우가 없고, 두 행동의 이름이 아이콘 없이 읽힌다.
- **실패 시나리오** 관리자가 목록을 스크롤하다 강퇴 아이콘을 스쳐 멤버가 즉시 쫓겨난다. 되돌리려면 재가입 요청을 받아야 한다.
- **근거** `frontend/src/pages/community/GroupDetail.tsx:46-57,83-89`(가입), `:97-122`(탭), `:170-178`(FAB), `:127-137`(채팅방), `:254-265,283-291`(승인), `:245-252,300-309`(강퇴 — 확인 없음). 인벤토리 `260918_flow_inventory_B_comms.md:186-297`(문서-코드 불일치 정정 포함: PENDING 승인은 실제로 구현돼 있음).
- **판정(r1)** ☐

---

#### F-MP-01 동네지도 → 업체 발견 — `/map`, `/map/search`
- **단계** **D(발견 — 업체축)**. S1(매물 발견)과 나란한 두 번째 발견축이며 종착지가 매물이 아니라 업체(F-BZ-01)다 · **상태** 제안 · **등급** B(핵심 프레임만)

##### FR-1 동네지도 루트
- **조건** 탭바 지도탭 또는 홈 위치 행. **비로그인도 열람 가능**(개인화 하위 화면만 `requireAuth` 게이트).
- **와이어** `H` 없음(상단 인라인 바가 대신) / `C` 상단 바: [위치 시트 열기(아이콘+동네명)] + 우측 [검색][찜][동네프로필] 아이콘 3개 / `B` 카테고리 칩 바(전체 + 개별) + [내 주변만] 토글 → 업체 카드 리스트 / `O` [지도로 보기] pill(플로팅), 지도 모드에서는 캔버스 오버레이에 [카테고리 더보기] 칩과 [지도 종료] / `K` 탭바.
- **요소 표**

| 영역 | 요소 | 무게 | 성격 | 탭하면 | 왜 여기 |
|---|---|---|---|---|---|
| C | 위치 시트 열기 | 보조 | 이동 | 위치 바텀시트 | 동네 기반 서비스의 전제 입력 — 상단 고정이 맞다 |
| C | 검색 아이콘 | 보조 | 이동 | `requireAuth` → `/map/search`(FR-2) | **자리는 정당**(B1 D단계 "동네 업체를 지도로 찾는다"의 보조 탐색 축 — 마켓 검색과 같은 상단 우측 슬롯, B0-4 앱 내 관례). **게이트는 근거 없음** — 목록·카드는 비로그인에 열려 있는데 같은 탐색 행동인 검색만 막혀 게이트 기준이 화면 안에서 갈린다(개인화 아님) |
| C | 찜 아이콘 | 보조 | 이동 | `requireAuth` → `/map/favorites` | 개인화이므로 게이트 정당 |
| C | 동네프로필 아이콘 | 보조 | 이동 | `requireAuth` → `/map/profile` | 개인화 허브(찜/단골/쿠폰/업체상태 숏컷). 게이트 정당 |
| B | 카테고리 칩 / [내 주변만] 토글 | 보조 | 표시 전환 | 목록·마커 재조회 | 탐색 축. 앱 내 칩 관례와 일치 |
| B | 업체 카드 | **주행동** | 이동 | `/biz/:id`를 **오버레이로**(`backgroundLocation` state) | 이 화면의 목적. 오버레이라 뒤로가기가 지도로 복귀 — 탐색 연속성 정당 |
| O | [지도로 보기] pill | 보조 | 표시 전환 | 지도 캔버스 모드 | 떠 있으나 **진행 중 세션이 아닌 뷰 전환 컨트롤** — F-N-01 정책 대상 아님. 마켓의 지도뷰 pill과 같은 패턴 |
| O | 카테고리 더보기 칩(지도 오버레이) | 숨김 | 이동 | `requireAuth` → `/map/categories` | 지도 모드에서만 — 발견 난이도 높음 |
| B | 빈 상태 / 위치 권한 거부 UI | 표시 | 표시 | 없음 | **미확인** — `NeighborhoodMapCanvas.tsx`(1378줄) 내부로 추정, 인벤토리·이번 작업 모두 본문 미독 |
- **주행동** 업체 카드 탭.
- **제안** ① 검색 아이콘의 `requireAuth` 를 제거해 `/map/search` 를 비로그인에 연다. 게이트는 개인화 화면(찜·동네프로필·카테고리)에만 남긴다 — 화면의 게이트 기준을 "탐색은 열고 개인화만 막는다" 하나로 통일(B1 D단계). *검증: 로그아웃 상태에서 지도탭 → 검색 → 결과 리스트 → 업체 상세 오버레이까지 로그인 벽 없이 도달하고, 찜·동네프로필 탭에서만 로그인 요구가 뜬다.* ② 상단 우측 아이콘을 [검색][동네프로필] 2개로 줄이고 찜은 동네프로필 안의 기존 찜 숏컷으로만 둔다 — 같은 개인화 허브로 가는 입구가 둘이라 상단 슬롯이 중복이다(B0-4 중복 제거). *검증: 상단 바 아이콘이 2개이고 `/map/favorites` 진입 경로가 동네프로필 경유 하나만 남는다.* ③ 지도 캔버스 모드를 F-S1-01 FR-2(마켓 지도뷰)와 같은 문법으로 맞춘다 — 핀 탭 시 하단 업체 캐러셀 카드, 카드 탭 시 `/biz/:id` 오버레이, [지도 종료]는 "리스트로" 복귀 아이콘으로 통일하고 오버레이의 [카테고리 더보기] 칩은 리스트 모드 카테고리 칩 바와 중복이므로 제거한다(B0-4 기존 패턴 재사용, 신규 컴포넌트 불필요). *검증: 매물 지도뷰와 동네지도 지도 모드에서 핀 → 하단 카드 → 상세, 리스트 복귀의 동선과 컨트롤 위치가 동일하다.*
- **실패 시나리오** 처음 온 비로그인 사용자가 업체를 찾으려 검색을 눌렀다가 로그인 벽을 만난다 — 목록은 보여주면서 검색만 막으므로 "왜?"가 남는다.
- **근거** `frontend/src/pages/map/NeighborhoodMap.tsx:204,212,215,218`(상단 4종), `:229,238,257`(칩·토글), `:281`(재로드), `:304`(업체 카드 오버레이), `:317`(지도 pill), `:192`(지도 종료), `NeighborhoodMapCanvas.tsx:1251`(카테고리 더보기). 인벤토리 `260918_flow_inventory_C_discovery.md:77-233`.
- **판정(r1)** ☐

##### FR-2 지도 내 업체 검색
- **조건** FR-1의 검색 아이콘(로그인 필요).
- **와이어(현재)** `H` [뒤로] + 검색 입력 / `B` 카테고리 필터 칩 → 업체 카드 결과 / `K` 없음.
- **요소 표**

| 영역 | 요소 | 무게 | 성격 | 탭하면 | 왜 여기 |
|---|---|---|---|---|---|
| B | 업체 카드 | **주행동** | 이동 | `/biz/:id` 오버레이(`backgroundLocation`) | FR-1과 같은 오버레이 문법 — 일관 |
| B | 카테고리 필터 칩 | 보조 | 표시 전환 | 해당 카테고리로 결과 재조회([전체] 포함 단일 선택) | FR-1 카테고리 칩 바와 같은 탐색 축을 검색 안에서 반복 — B0-4 세그먼트 칩 관례와 일치 |
| B | 빈 결과 / 로딩 상태 | 표시 | 표시 | 에러 상태에서만 [다시 시도] | 입력 전 힌트 · 스켈레톤 · 에러(재시도 CTA) · 빈 결과 4상태를 `StateBlock` 으로 처리 — B0-4 빈 상태 CTA 관례와 일치 |
- **주행동** 업체 카드 탭.
- **제안** ① 이 화면 자체에는 인증 분기가 없으므로, FR-1 제안 ①(검색 아이콘 `requireAuth` 제거)만으로 비로그인 검색이 성립한다 — 이 프레임에 추가 변경은 없고, 게이트 기준은 "탐색은 열고 개인화만 막는다"로 FR-1과 같다. *검증: 로그아웃 상태로 `/map/search` 직접 진입 → 키워드 입력 → 결과 카드 → `/biz/:id` 오버레이까지 로그인 요구 없이 도달한다.* ② 조회 범위를 FR-1과 맞춘다 — 현재 검색은 호치민 전역 bbox 고정이라 FR-1 에서 고른 동네·[내 주변만] 설정이 넘어오지 않는다. FR-1 이 쓰는 범위 파라미터를 그대로 전달하고, 결과 0일 때 빈 상태에 [전체 지역에서 찾기] CTA 로 범위를 넓히게 한다(B0-4 빈 상태 CTA 재사용, 신규 컨트롤 불필요). *검증: 동네 A 를 고른 뒤 검색한 결과에 A 범위 밖 업체가 없고, 빈 결과에서 CTA 한 번으로 전역 결과가 나온다.*
- **실패 시나리오** 동네를 고른 사용자가 검색하면 호치민 반대편 업체가 섞여 나오는데, 목록과 검색이 서로 다른 범위를 본다는 것을 화면 어디에서도 알 수 없다.
- **근거** `frontend/src/pages/map/MapSearch.tsx:92,120,177`. 인벤토리 `260918_flow_inventory_C_discovery.md`(§지도 내 검색 — 미확인 명시).
- **판정(r1)** ☐

---

#### F-BZ-01 업체 공개 상세 — `/biz/:id`
- **단계** **D(발견 — 업체축)**의 종착. S2(판단)의 업체판이며, 성공 신호는 매물 채팅이 아니라 **업체 문의(채팅/전화)** 다 · **상태** 제안 · **등급** B(핵심 프레임만)

##### FR-1 업체 상세(방문자)
- **조건** 지도·지도검색에서 **오버레이**로, 또는 지도 찜·홈 업체소식에서 **일반 네비게이션**으로 진입. 탭바는 숨고 그 자리를 하단 고정 CTA가 쓴다.
- **와이어** `H` 커버 이미지 위 [뒤로] · 우측 [찜][공유][신고] 아이콘 / `B` 업체 아이덴티티(이름·업종·인증) → [팔로우] → 탭 [홈|소식|가격|매물|후기] → 탭별 본문(가격표, 매물 카드, 소식 카드, 후기 목록 + [후기쓰기]/[이상신청]) / `K` 하단 고정 바 [채팅 문의] 채움 + [전화] .
- **요소 표**

| 영역 | 요소 | 무게 | 성격 | 탭하면 | 왜 여기 |
|---|---|---|---|---|---|
| K | 채팅 문의 | **주행동** | 진행 | `requireAuth` → 대화 생성 → `/dm/:convId` | 업체 발견의 성공 신호. **하단 고정 바**는 매물 상세(F-S2-01)의 [찜][제안][채팅]과 같은 앱 내 패턴 — 일관 |
| K | 전화 문의 | 보조 | 진행 | 전화 앱 호출 | 번호가 있을 때만. 베트남 현지에서 전화 문의 비중이 높아 정당 |
| H | 찜(즐겨찾기) 토글 | 보조 | 진행·가역 | 토글 | `/map/favorites`에 쌓인다 — 재방문 경로 |
| H | 공유 | 보조 | 이동 | 공유 시트 | 여정 근거 없음 — B1 D축의 성공 신호는 업체 문의이고 공유는 어느 단계의 성공 신호도 아니다. 헤더에서 찜(진행)·신고(종료)와 성격이 섞여 **P-1 위반**. 제안 ②의 격하 대상 |
| H | 업체 신고 | 숨김 | 종료 | 사유 + 선택 메모 입력 모달 | **다른 신고와 문법이 다르다** — 업체 신고에만 메모 입력란이 있고, 아이콘도 헤더에 직접 노출(다른 화면은 케밥 뒤). **노출 조건 미확인**(오너 제외 추정) |
| B | 팔로우 토글 | 보조 | 진행·가역 | 토글 | 단골(`/map/follows`)로 쌓인다 |
| B | 탭 [홈 · 소식 · 가격 · 매물 · 후기] | 보조 | 표시 전환 | 탭 전환 | 업체 정보가 많아 탭이 필요. 5탭은 많으나 판정 대상. 쿠폰은 별도 탭이 아니라 "홈" 탭 안 미리보기 카드로 노출 |
| B | 쿠폰 받기(홈 탭 미리보기) | 보조 | 진행 | 쿠폰 수령(1회성) | 취소 불가 여부 **미확인** |
| B | 업체 매물 카드 | 보조 | 이동 | `/market/:id`(F-S2-01) | **업체축 → 거래축 전이 지점.** 여기서 일반 매물 여정으로 합류한다 |
| B | 후기쓰기 / 이상신청(어필) | 보조 | 진행 | 작성 화면 / 어필 모달 | 거래 이력 조건 **없음** — 오너 본인만 막히고(`profile.isOwner`), 그 외에는 항상 노출된다(`requireAuth`만 거침). 이미 작성한 후기가 있으면 라벨이 "후기 수정"으로 바뀐다 |
- **주행동** 채팅 문의.
- **제안**
  - ① **업체 신고를 헤더 직접 노출에서 케밥(⋮) 뒤 `숨김`으로 격하** — 다른 5개 신고 화면과 같은 문법(F-X-02 FR-1 "6종 통일"과 같은 줄기), 기존 패턴은 B0-4 "위험 톤 메뉴 행(나가기)". 근거는 P-1 — 헤더 한 줄에 진행(찜)·이동(공유)·종료(신고)가 섞여 있다. *검증:* 헤더 아이콘에 종료 성격이 남지 않고, 신고 도달이 2탭(⋮ → 신고) 이내이며, 6개 신고 진입점의 진입 문법이 같은가.
  - ② **공유도 같은 케밥 안으로 격하해 헤더 우측은 [찜]만 남긴다** — 공유에는 여정 근거가 없어 `보조`(헤더 아이콘)를 유지할 이유가 없다(B0-4 무게 정의). 케밥 안에서는 공유(이동)와 신고(종료)를 구분선으로 나누고 신고만 위험 톤으로 둔다(P-1, B0-4 기존 패턴). *검증:* 헤더 우측 아이콘이 3개 → 1개([찜])로 줄고, [뒤로]와 종료 성격 요소가 인접하지 않는가.
  - ③ **매물 카드 → `/market/:id` 전이에서 업체 맥락 유지** — 매물 상세(F-S2-01)의 판매자 블록에 개인 판매자 대신 **업체 아이덴티티(로고·업체명·인증 배지)와 업체 상세로 돌아가는 링크**를 표시한다. 근거는 B1 — 이 카드가 업체축과 거래축의 유일한 합류점이고, 합류 직후 업체 맥락이 끊기면 S2의 지배적 불안("이 사람 믿어도 되나")이 개인 판매자 기준으로 잘못 평가된다. *검증:* 업체 매물로 진입한 `/market/:id`에서 업체명·인증 배지가 보이고, 뒤로가기가 진입한 업체 상세로 착지하는가.
- **실패 시나리오** 헤더에 아이콘 3~4개가 붙어 [뒤로]와 [신고]가 가까워진다. 오버레이로 들어온 경우와 일반 네비게이션으로 들어온 경우의 뒤로가기 착지가 달라(지도 vs 찜 목록) 같은 화면인데 다른 결과를 준다.
- **근거** `frontend/src/pages/biz/BizPublic.tsx:219,503-518,542,557-559,667,686,737-739,863,879,940,949-950`. 진입 `NeighborhoodMap.tsx:304`(오버레이), `MapSearch.tsx:177`(오버레이), `MapFavorites.tsx:145`(비오버레이), `HomePage.tsx:507`(비오버레이). 인벤토리 `260918_flow_inventory_C_discovery.md:77-233`.
- **판정(r1)** ☐

---

#### F-BZ-02 비즈니스 라운지 — `/biz/manage`
- **단계** **PT(파트너 — 업체 운영축)**. 개인 프로필(F-P-02 FR-2)에서 분기하는 별도 역할 축이며 개인 거래 여정(S0~S7)과 단계를 공유하지 않는다 · **상태** 제안 · **등급** B(핵심 프레임만)

##### FR-1 라운지 허브(승인된 업체)
- **조건** APPROVED 업체 보유자가 `/profile`의 파트너 라운지 요약 카드 또는 `/biz/status`에서 진입. 다중 업체면 상단 선택기로 대상 전환.
- **와이어** `H` [뒤로] "비즈니스" / `C` 업체 선택기(2개 이상일 때만 활성) + 아이덴티티(로고·이름·업종) + 액션행 [공개 프로필 보기][정보 수정] / `B` 탭 [운영|성과] → **우선조치 카드**(검증 필요 / 광고 반려 / 계약 — 있을 때만) → 광고 요약 카드 → 관리 메뉴 5행 [소식][가격][쿠폰][매물][리뷰] → 검증 상태 행 → 가이드 아코디언 → 도움말 행 / `O` [정보 수정] 시 인라인 편집 바텀시트(이름·업종·전화·소개·사진) / `K` 탭바 없음.
- **요소 표**

| 영역 | 요소 | 무게 | 성격 | 탭하면 | 왜 여기 |
|---|---|---|---|---|---|
| B | 우선조치 카드(검증/광고반려/계약) | **주행동** | 진행 | `/biz/verification` · 광고 상세 · 계약 액션 | 조건부로만 뜨고, 뜨면 그것이 유일한 할 일 — "화면당 주행동 하나" 규칙에 맞는 드문 사례 |
| B | 광고 요약 카드 | 보조 | 이동 | `/biz/ads` | 매출 경로. 요약+이동은 정당 |
| B | 관리 메뉴 5행(소식·가격·쿠폰·매물·리뷰) | 보조 | 이동(4행) / 표시 전환(리뷰 1행) | 소식·가격·쿠폰·매물은 각 관리 화면으로 페이지 이동, **리뷰만 `focusDashboard('reviews')`로 같은 화면 안 성과 탭 전환+스크롤** | 허브의 본체이나 **5행이 동일하지 않다** — 같은 모양의 행 4개가 페이지 이동이고, 리뷰 1개만 화면 안 탭 전환이라 사용자가 다른 종류의 이동을 같은 행 스타일로 만난다 |
| C | 공개 프로필 보기 | 보조 | 이동 | `/biz/:id`(F-BZ-01) | "손님이 보는 화면"을 확인 — F-P-02 FR-3(내 프로필 미리보기)과 같은 원리. 일관 |
| C | 정보 수정 | 보조 | 진행·가역 | 편집 바텀시트 | 인라인 편집. 취소 가능 |
| C | 업체 선택기 | 보조 | 표시 전환 | 업체 선택 시트 | 다중 보유자만 활성 |
| B | 탭 [운영 · 성과] | 보조 | 표시 전환 | 탭 전환 | B1 PT 축을 **처리(운영)와 조회(성과)** 두 관점으로 가른다 — 같은 대상에 관점만 바꾸는 전환이므로 앱 기존 패턴인 세그먼트 칩(판매중/예약중)과 같은 문법 |
| B | 미답변(문의) 처리 | **주행동** | 진행 | — | **없다(부재).** 프로필의 라운지 요약 카드는 **미답변 수 배지**로 사람을 불러오는데, 라운지에 와도 그 미답변으로 바로 가는 행이 없다 — 배지가 가리키는 목적지가 이 화면에 없음 |
| B | 업체 철회·폐업 | 파괴적 | 종료·비가역 | — | **없다(부재).** grep(`withdraw`/`delete`/`deactivate`) 미검출. 그만두려면 `partner@saigon-rider.com` 이메일 문의에 의존 |
| B | 도움말 행 | 보조 | 표시 전환 | 대시보드 support 포커스(화면 안 탭 전환, 페이지 이동 아님) | **UI 경로(F-CS-01)와 다르다**(라운지 안 탭 vs `/settings/support` 별도 화면). 단 **데이터 계층은 이미 같은 `SupportTicket` 큐로 수렴**(`source="BIZ"`) — 확인됨 |
- **주행동** 우선조치가 있으면 그것, 없으면 관리 메뉴 진입(주행동 불명확).
- **제안** ① 미답변이 1건 이상이면 **우선조치 카드 스택 최상단**에 "미답변 문의 N건" 카드를 같은 카드 문법으로 추가하고, 탭하면 해당 문의 목록으로 이동시킨다(B0-4 기존 패턴: 조건부 우선조치 카드 — 새 컴포넌트 불필요). *검증:* 프로필 요약 카드 배지의 N과 라운지 카드의 N이 같은 값이고, 라운지 진입 후 **2탭 이내**로 답장 입력 지점에 도달한다. ② 업체 철회 경로를 만들지 판정 요청 — 만든다면 운영 탭 최하단에 **위험 톤 메뉴 행**(B0-4 기존 패턴: 나가기 행) + `useConfirmStore` 확인 1회로 놓는다(P-1: 진행 행 묶음에 섞지 않는다). *검증:* `partner@saigon-rider.com` 메일 없이 화면 안에서 철회 요청 상태로 전환되고, 확인 다이얼로그를 거치지 않고는 전환되지 않는다. ③ 도움말 행은 **데이터 계층에서는 이미 고객센터와 수렴해 있다** — `createBizIssue`(`frontend/src/api/biz.ts:314-320`) → `POST /biz/issues` → `backend/app/routers/biz.py:526-580`가 일반 CS와 **같은 `SupportTicket`**을 `source="BIZ"`로 생성한다. 남은 것은 UI 문법 통일뿐 — 도움말 행을 화면 안 탭 포커스 대신 `/settings/support`로 **페이지 이동**시켜 F-CS-01과 같은 목적지에 붙인다. *검증:* 라운지 도움말 행과 설정 > 고객센터가 같은 화면·같은 문의 목록에 도달하고, 라운지에서 접수한 건이 그 목록에 보인다. ④ 관리 메뉴 5행 중 **리뷰 행만 화면 안 탭 전환(`focusDashboard('reviews')`)**인 불일치를 해소한다 — 리뷰 행을 나머지 4행과 같은 페이지 이동으로 맞추거나, 행 묶음에서 빼 성과 탭 안 항목으로 격하한다(P-1: 같은 모양이면 같은 성격). *검증:* 관리 메뉴에 남은 행은 전부 페이지 이동 한 종류이고, 탭한 뒤 뒤로가기 동작이 모든 행에서 같다.
- **실패 시나리오** 미답변 배지를 보고 들어온 파트너가 어디를 눌러야 답할 수 있는지 몰라 관리 메뉴 5개를 차례로 눌러본다.
- **근거** `frontend/src/pages/biz/BizManage.tsx:153,157,161-162,165,167,178-183,184,185,189`, 진입 `ProfileMain.tsx:326-336`, `BizStatus.tsx:27`(이메일 문의는 `:101`). 인벤토리 `260918_flow_inventory_D_trust.md:461-483`(11액션 전수표 + 철회 부재).
- **판정(r1)** ☐

---

### B3. 범위 밖(OUT)

**여전히 OUT.** 홈(`/`) · 알림 인박스 · 채널 게시판 본문(`DmBoard`/`DmBoardPost`) · 그룹 채팅 관리 · **게임화 잔재 일괄 삭제**(플래그 뒤에 남은 코드의 제거 작업 자체) · 홈화면 위젯(Android 고정) · 설정 하위 정적 화면(약관·공지·FAQ·안전거래 가이드) · 광고/쿠폰 운영 하위 화면(`/biz/ads` 등 라운지에서 이동하는 화면들).

**v3에서 OUT을 벗어난 것(§B2에 프레임이 생겼다).**

| 영역 | 라우트 | 신설 F-ID | 등급 |
|---|---|---|---|
| 상대방 프로필 | `/profile/:userId` (+`/listings`, `/posts`) | `F-P-01` | A |
| 개인 프로필 화면 | `/profile` | `F-P-02` | A |
| 고객센터 | `/settings/support`, `/settings/support/:id` | `F-CS-01`, `F-CS-02` | A |
| 커뮤니티 | `/feed`, `/feed/post/:postId`, `/feed/new`, `/feed/edit/:postId`, `/community/groups`, `/community/groups/new`, `/group/:slug` | `F-CM-01`, `F-CM-02` | B |
| 동네지도→업체 상세 | `/map`, `/map/search`, `/biz/:id` | `F-MP-01`, `F-BZ-01` | B |
| 비즈니스 라운지 | `/biz/manage` | `F-BZ-02` | B |

**등급의 뜻.** A등급 = 거래 여정과 직결되므로 상태별로 프레임을 쪼개고 제안까지 붙였다. B등급 = 간접 영향이므로 **핵심 프레임의 요소 표까지만** 만들었고 **손그림 화면 샘플은 다음 단계**다.

**경계에 걸친 것(부분 OUT).** `/map/favorites`·`/map/follows`·`/map/coupons`·`/map/profile`·`/map/categories`(지도 개인화 하위 5종)는 `F-MP-01 FR-1`의 요소 표에 **이동 대상으로만** 기록하고 각각의 프레임은 만들지 않았다. `/profile/:userId/listings`·`/posts`는 `F-P-01 FR-4` 한 프레임에 묶어 "미확인" 상태로 올렸다.

---

## C. 판정문 스키마 (검토 AI 출력 — 제목·열 그대로)

```markdown
# 서비스 플로우 판정문 r<N> — <YYYY-MM-DD>

| 항목 | 값 |
|---|---|
| 킷 버전 | v<N> |
| 판정자 | 대표 / 작성자(대리, 가판정) |
| 검토 AI | <모델명> |
| 소요 | <분> |

## 1. 요약
프레임 승인 <n> · 수정 <n> · 보류 <n> · 신규 <n> · OUT <n>. 한 줄 총평(판정자 답변 기반).

## 2. 서비스 전체 판정 (A0-1~5)
| # | 질문 | 판정자 답(원문에 가깝게) | 확정된 원칙 |
|---|---|---|---|

## 3. 피드백 샘플(V1~V3) 해소 여부
| 피드백 | 대응 프레임 | 제안이 문제를 푸는가(판정자 답) | 남는 것 |
|---|---|---|---|

## 4. 프레임별 판정표
| F-ID | FR | 판정 | 수정 내용(영역 · 요소 · 어떻게 — 없으면 "-") | 판정자 답변 요지 | AI 단독 분석 의견(동의/이견 + (a)앱 내 패턴/(b)레퍼런스) | 근거 유형(전제/레퍼런스/판정자 직관) | 작성자 반영 시 주의 |
|---|---|---|---|---|---|---|---|

## 5. "왜 여기 = 이유 없음/관성" 요소의 처분
| F-ID·FR | 요소 | 판정자 처분(유지/이동/제거) | 이유 |
|---|---|---|---|

## 6. 신규 요소·흐름 (NEW-nn)
| 임시 ID | 목표 | 시작 | 끝 | 영역 | 판정자 답변 | 작성자 후속 |
|---|---|---|---|---|---|---|

## 7. "불편" 분해 기록
| F-ID·FR | 영역 | 사용자가 기대하는 것 | 실제로 보이는·일어나는 것 | 수정으로 연결됐나 |
|---|---|---|---|---|

## 8. 검토 AI 자체 점검
- [ ] 모든 프레임에 판정이 있다(일괄 판정은 "일괄" 표기)
- [ ] 모든 `수정`에 영역·요소·어떻게가 있다 — 없는 것은 `보류(어떻게 미정)`
- [ ] 판정자 답변과 AI 의견을 섞지 않았다
- [ ] 이유 없는 판정은 `판정자 직관`으로 표기했고 이유를 만들어 넣지 않았다
- [ ] V1~V3 각각에 대해 해당 프레임에서 "푸는가"를 물었다
- [ ] "이유 없음/관성" 요소를 전부 §5에 올렸다
- [ ] "불편" 답변은 전부 §7에 분해돼 있다
- [ ] 범위 밖은 OUT으로만 기록했다
- [ ] 코드·구현 방법을 논하지 않았다

## 9. 미해결 질문(다음 라운드)
- ...
```

---

## D. 반영 규칙 (작성자가 판정문을 §B에 적용하는 방법)

| 판정 | 프레임 처리 | 상태값 | 구현 |
|---|---|---|---|
| **승인** | 내용 불변. `판정(rN)` 칸에 "승인 rN, 날짜" | `승인(rN)` | **착수 가능** |
| **수정** | 요소 표의 해당 행과 "제안"을 판정문의 '수정 내용' 그대로 갱신. "왜 여기"에 `판정자 결정(rN): …` 추가. 이전 문안은 변경 로그 | `수정반영(rN, 재판정)` | 착수 금지 — 다음 라운드 `승인` 후 |
| **보류** | 사유·재검토 조건 기입 | `보류(사유)` | 금지 |
| **NEW-nn** | 작성자가 단계·프레임을 정해 F-ID·FR 부여, 요소 표 전 열 작성. 판단 불확실하면 판정문 §9에 질문 | `제안` | 금지 |
| **근거 = 판정자 직관** | "왜 여기"에 `판정자 직관(rN)` 표기 + **D-2 파일럿 검증 목록**에 추가 | (판정 그대로) | (판정 그대로) |
| **§5 이유 없음 처분** | 유지 → "왜 여기"를 판정자 이유로 교체 / 이동·제거 → 해당 행 갱신 | — | — |
| **§7 불편 분해** | 해당 프레임 "실패 시나리오"에 사용자 관점 한 줄 추가 | — | — |

**D-1. 버전과 로그.** 반영 후 헤더 "킷 버전" v(N+1), 변경 로그에 프레임 단위로 한 줄씩. 구현 후 워크스루 결과를 `판정` 칸 아래 `검증(rN 워크스루): PASS/FAIL — 비고`로. FAIL은 구현 결함(프레임은 승인 유지)과 설계 결함(상태를 `수정반영`으로 되돌림)을 구분.

**D-2. 판정자 직관으로 결정된 프레임 — 파일럿 숫자로 검증할 것.**
| F-ID·FR | 결정 | 검증 지표 | 확인 시점 |
|---|---|---|---|
| (r1 후 채움) | | | |

**D-3. 승인 후 변경.** 승인된 프레임을 바꾸려면 새 라운드에서 `재검토 요청`. 조용히 고치지 않는다.

**D-4. 대면 라운드.** AI 없이 대면 판정한 경우 작성자가 회의 직후 §C 스키마로 `verdict_r1.md`를 직접 쓴다. 판정자 답변은 회의 중 프레임 옆에 적은 메모를 옮긴다(기억 아님).

**D-5. 프레임 추가 규칙.** 새 상태(예: 업체 판매자 전용 뷰)가 발견되면 기존 F-ID 아래 FR-n을 추가한다. 새 화면이면 새 F-ID. 프레임 없이 요소만 논하지 않는다.

---

## 변경 로그

| 버전 | 날짜 | 라운드 | 변경 |
|---|---|---|---|
| v1 | 2026-09-19 | r0 | 초판. 흐름 단위 카드 15개 |
| v2 | 2026-09-19 | r0 | **스토리보드판.** 피드백 샘플 V1~V3 요지 기록 + 원칙 P-1(같은 묶음=같은 성격)·P-2(떠 있는 요소 0) 도출. 전 플로우를 프레임(화면×상태) 단위로 재작성 — F-N-01 신설(전역 오버레이 정책, 2프레임) 포함 총 16 F-ID · 44 프레임. 각 프레임에 영역별 요소 표(무게·성격·탭 결과·당위성) 부여, 당위성 없는 요소는 "이유 없음/관성"으로 명시. 무전기 캡슐·위치공유 버블의 현재 동작(fixed+자유 드래그+전역 마운트+겹침)을 코드로 확인해 기재. A0-5·Q4·Q5 추가, §C에 V1~V3 해소표·이유없음 처분표 추가 |
| v3.1 | 2026-09-21 | r0(사실확인) | **코드 재확인 후 사실 오류 6건 정정(구현 아님, 판정 전 사실관계 수정).** `F-CS-01 FR-1`: 새 문의 버튼은 이미 채움 full-width(`CustomerSupport.module.css` `.newBtn`)라 판정 불필요로 정정. `F-P-02 FR-5`: 탭 줄이 `.tabRow{display:none!important}`(SGR-287)로 이미 숨겨져 있고 기본 탭이 `feeds`라 퀘스트 이력·뱃지는 UI 도달 불가 — 판정 성격을 "배치"에서 "잔재 코드 존치 여부"로 전환, 요소 표 무게를 전부 `숨김`으로 정정, 화면 샘플 `storyboard/screens/F-P-02__FR-5.html`(탭 줄이 보이게 그려짐) 삭제. `F-BZ-01 FR-1`: 탭을 실제 값 `[홈·소식·가격·매물·후기]`로 정정(쿠폰은 별도 탭이 아니라 홈 탭 미리보기), 후기쓰기 노출 조건에서 "거래 완료자 추정" 삭제(거래 이력 조건 없음, `isOwner`만 분기). `F-BZ-02 FR-1`: 관리 메뉴 5행이 전부 동일하다는 기술을 정정 — 리뷰 관리 행만 `focusDashboard('reviews')`로 화면 안 탭 전환(페이지 이동 아님). 도움말 행의 CS 큐 통일 제안을 확인 완료로 갱신 — `createBizIssue → POST /biz/issues → backend/app/routers/biz.py:526-580`가 일반 CS와 같은 `SupportTicket`(`source="BIZ"`)을 생성함을 확인, 남은 것은 UI 문법 통일뿐. `F-CM-02 FR-2`: 와이어에서 커버 이미지 삭제(`GroupDetail.tsx`는 `coverUrl`을 렌더하지 않음, `GroupList`에서만 사용). |
| v4 | 2026-09-21 | r0(제안 정련) | **판정 전 제안 문안 일괄 정련 — 프레임 66개 전수 검토.** 사실관계·요소 표 구조·판정 칸(전부 ☐ 미판정 유지)은 그대로 두고, 각 프레임의 **"왜 여기" 근거와 "제안"만** 손봤다. ① "이유 없음/관성"으로 시작하던 행은 B0-2 레퍼런스·B0-4 기존 패턴·B1 여정 근거로 교체하거나, 근거를 못 찾은 것은 P-1·P-2에 따른 처분(제거·이동·격하)을 제안 쪽으로 옮겼다. ② "~할지 판정 요청" 식으로 검증 불가하던 제안을 **구체안 + 검증 기준 한 줄** 형태로 다시 썼고, 새 대안은 전부 §B0-4 "앱 안에 이미 있는 패턴"이나 §B0-2 관행에 연결했다(신규 컴포넌트 제안 없음). ③ 요소 표가 없는 6개(F-S3-03 FR-4·F-S5-01 FR-4·F-S6-01 FR-5·F-X-01 FR-2·F-X-02 FR-1·F-X-02 FR-2)는 표를 지어내지 않고 **"정의 요청" 항목**(범위·권한/표시·자리 3문항)만 붙였다. ④ F-CM-02 FR-1은 존폐 판정 성격을 유지한 채 A안(살린다)/B안(접는다)에 각각 따라오는 작업·영향·검증 기준을 붙였다. 나머지 프레임은 이미 근거·제안이 구체적이라 무변경. 스토리보드 HTML(`storyboard/index.html`) 재생성 완료 |
| v3 | 2026-09-21 | r0 | **범위 확장(6개 영역 편입).** v2가 OUT으로 선언했던 프로필·고객센터·커뮤니티·동네지도·업체·파트너를 킷 안으로 들여왔다. 신설 **9 F-ID · 22 프레임** — A등급: `F-P-01`(상대 프로필 4프레임, S2 신뢰 확인 경로로 정식 편입) · `F-P-02`(개인 프로필 허브 5프레임, S0·S7·파트너 분기 + 게임화 잔재 탭 분리) · `F-CS-01`(고객센터 4프레임) · `F-CS-02`(문의 상세 1프레임, 답글 API 고아 기록). B등급: `F-CM-01`(피드 2) · `F-CM-02`(그룹 2, FR-1은 도달 불가 존폐 판정) · `F-MP-01`(지도 2) · `F-BZ-01`(업체 상세 1) · `F-BZ-02`(라운지 1). 문서 틀 갱신: 헤더 범위 행 · §B0-4에 **F-ID 접두사 체계표** 신설(`F-P-`/`F-CS-`/`F-MP-`/`F-BZ-`/`F-CM-`) · §B1에 P·CS·D·PT 축 4행 추가 + **CM 커뮤니티 독립 여정표** 신설 · §B3을 "여전히 OUT / 이번에 벗어난 것 / 부분 OUT" 3단으로 재작성 · §0·§1의 프레임 수 44→66. **기존 44개 프레임은 무변경**(화면 샘플 38개 유지). 신설 22개는 손그림 샘플 없이 요소 표 기반 자동 목업 |
