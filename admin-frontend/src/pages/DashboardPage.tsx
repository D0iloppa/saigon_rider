import { Card, Typography } from 'antd'

/** P3 에서 실제 지표/차트로 교체 예정. */
export default function DashboardPage() {
  return (
    <Card>
      <Typography.Title level={4}>대시보드</Typography.Title>
      <Typography.Text type="secondary">준비 중입니다.</Typography.Text>
    </Card>
  )
}
