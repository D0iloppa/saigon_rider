# 동네지도 테스트 시나리오 명세 (2026-07-07)

> 동네지도 전 기능의 기대 동작 명세 + 회귀 케이스. GPS 컨텍스트(A: HCMC 내 / B: HCMC 밖 / C: GPS 거부·실패 / D: 미측정 초기)로 분기.
> 원칙 전제: service-rules — 진입 시 GPS 자동측정 금지, 지도·시트 단일 데이터 소스(게이트 미만 무데이터+가이드), 줌 게이트(뷰포트 폭 3500 미만=블록/도로, 700 미만=건물 Layer3), 내 위치 포커스 = Layer3 최소 줌(vbW 630).

## 컨텍스트 정의

| ctx | GPS 상태 | 대표 좌표 |
|---|---|---|
| A | HCMC 내 | 10.7756, 106.7019 (Bến Thành) / 10.80, 106.65 (Tân Bình 쪽) |
| B | HCMC 밖 | 37.50, 127.00 (서울) / 10.03, 105.78 (Cần Thơ — 인접 위성도시) |
| C | 권한 거부/측정 실패 | geolocation permission denied |
| D | 미측정 (신규 세션, 저장 없음) | — |

## 케이스 및 기대 동작

### 1. 진입/초기 상태
| # | 케이스 | 기대 |
|---|---|---|
| 1.1 (D) | 최초 진입 (저장 뷰포트/좌표 없음) | 전역 뷰. **GPS 호출 0회**. 지도 무데이터(배지·핀 없음), 접힘 시트 헤더 = 힌트 필("확대해서 주변 보기"), 시트 펼침 = 가이드+[내 동네 보기]. 콘솔에러 0 |
| 1.2 | 재진입 (이전에 줌인했던 세션) | 마지막 뷰포트 정확 복원(GPS 0회). 게이트 이상이면 리스트/핀 자동 조회 |
| 1.3 | 재진입 (storedCoords 有, 저장 뷰포트 無) | 좌표 중심 Layer3 딥줌(측정 없이 기억 좌표) + bbox 리스트 연결. ~~ward 선택~~(개정: viewport 모드 유지 — 단일 소스 정합) |

### 2. 줌 게이트 / 단일 소스
| # | 케이스 | 기대 |
|---|---|---|
| 2.1 | 게이트 미만(전역~광역) | listings/feed fetch **0건**, 지도 핀/배지 0, 시트 가이드. 헤더에 건수 표기 없음(힌트 필) |
| 2.2 | 줌인으로 게이트 통과 | bbox fetch 발화(탭당 1회), 핀 = 시트 리스트 동일 집합(좌표 보유분), 힌트 필 → 건수로 전환 |
| 2.3 | 게이트 통과 후 다시 줌아웃 | 리스트/핀 제거 + 가이드 복귀, 추가 fetch 없음 |
| 2.4 | 게이트 상태에서 팬(이동)만 | fetch 0 유지 |
| 2.5 | 매물↔피드 탭 전환 (게이트 미만) | 양 탭 모두 가이드, fetch 0 |
| 2.6 | 매물↔피드 탭 전환 (게이트 이상) | 각 탭 소스로 핀·리스트 동시 교체, 색상 구분(매물/피드) |
| 2.7 | 3-stage 렌더 | vbW≥3500 폴리곤 / <3500 블록·도로 / <700 건물. 전환 시 크래시·잔상 없음 |

### 3. 내 위치 (◎ 컨트롤 / 힌트 필 / 가이드 버튼 — 3개 진입점 동일 동작)
| # | 케이스 | 기대 |
|---|---|---|
| 3.1 (A) | HCMC 내에서 탭 | GPS 1회 측정 → 사용자 지점 중심 vbW 630(Layer3, 건물 보임), 현위치 점 표시, 좌표를 위치 스토어에 기억(onLocated), 리스트 = 그 뷰포트 bbox 결과. ~~ward 선택~~(개정: region 필터 전환 안 함 — ward 필터는 뷰포트와 리스트를 다시 어긋나게 함) |
| 3.2 (A) | 연타(2~3회 빠르게) | GPS 중복 호출 폭주 없음(합리적 횟수), 최종 상태 3.1과 동일 |
| 3.3 (B) | HCMC 밖에서 탭 | **지도 가장자리 줌인 금지.** 안내 토스트 + **호치민 중심가(Bến Thành) 딥줌**(개정 260707: 막다른 길 방지 — 원격 사용자도 매물 탐색 가능). 가짜 위치점·좌표 저장 없음 |
| 3.4 (C) | 권한 거부 상태에서 탭 | 실패 토스트(3로케일), 크래시 없음, 뷰포트 불변. **토스트는 검색창 등 다른 UI와 겹치지 않게**(상단 오버레이 아래 — 실기기 status bar 인셋 포함 판정) |
| 3.5 | 접힘 힌트 필 탭 | ◎와 동일 (드래그 캡처에 삼켜지지 않음 — 계측 geo 1회) |

### 4. 지역 선택 (region 모드)
| # | 케이스 | 기대 |
|---|---|---|
| 4.1 | 게이트 미만에서 동 폴리곤 탭 | region 모드 진입: 해당 동 필터 리스트 + 칩 표시, 가이드 아님. 헤더 = region 건수 |
| 4.2 | region 모드에서 다른 동 탭 | 필터 교체, 리스트/핀 갱신 |
| 4.3 | region 해제(칩 X 등) | viewport 모드 복귀 — 현재 줌 기준 게이트 판정 재적용 |
| 4.4 | region 모드 + 지도 핀 탭 | 해당 카드 selected 강조(틴트+링, 매물·피드 모두) + 리스트 스크롤 |

### 5. 검색
| # | 케이스 | 기대 |
|---|---|---|
| 5.1 | 게이트 미만에서 검색 | 게이트 무관 동작: 결과 리스트 + 결과 핀 + auto-fit. 헤더 = 결과 건수 |
| 5.2 | 검색 결과 0건 | 빈 상태 문구, 지도 핀 0, 크래시 없음 |
| 5.3 | 검색 해제(X) | viewport 모드 복귀 + **검색 fit 위치 유지**(개정: 지도앱 관례 — 보던 곳 유지. 직전 뷰포트 복귀 아님), 그 줌 기준 게이트 판정 정상 |
| 5.4 | 최근 검색어 | 저장/재실행/개별삭제/전체삭제 |

### 6. 시트/카드
| # | 케이스 | 기대 |
|---|---|---|
| 6.1 | 시트 드래그 스냅 collapsed↔mid↔full | 스냅 정상, 접힘 상태에서 힌트 필/건수 항상 노출 |
| 6.2 | 매물 카드 탭 | /market/:id 이동 |
| 6.3 | 광고 카드 | 플랫 리스트 사이 박스+간격, 탭 시 광고 상세 |
| 6.4 | 리스트 무한스크롤/개수 | (개정) 뷰포트 내 매물 전건 표시 — 페이지 병합(50/페이지, **상한 300**, 중복 제거). 지도 핀=리스트 동일 집합 유지가 목적. 스크롤 파손 없음 |

### 7. 사소/방어
| # | 케이스 | 기대 |
|---|---|---|
| 7.1 | 줌 컨트롤 +/− 연타 한계 | MIN_VBW(100)~최대 클램프, NaN/음수 viewBox 없음 |
| 7.2 | 좌표 없는 매물 | bbox 브라우즈에서 핀·리스트 양쪽 제외(단일 소스). **검색 모드는 예외**(전역 키워드 검색 — 좌표 없어도 리스트 노출, 핀만 없음. 발견성 우선) |
| 7.3 | 뷰포트 저장 방어 | localStorage 값 오염(비정상 JSON) 시 무시하고 전역 시작 |
| 7.4 | 다크모드 강제(data-theme) | 가이드/힌트 필/카드 색 토큰 정상 |
| 7.5 | i18n 3로케일 | 가이드·힌트·빈상태 문구 ko/en/vi 모두 존재(하드코딩 defaultValue 노출은 ko 뷰만 허용) |

## 실행 노트
- dev-login 번호별 신규 계정으로 컨텍스트 격리. playwright `context.geolocation`/`permissions`로 A/B/C 시뮬레이션.
- fetch 계측: `/market/listings`·`/feed` 요청 카운트. GPS 계측: getCurrentPosition 훅.
- 판정: 기대와 다르면 FAIL + 근거(스크린샷/계측값/콘솔) — 수정 후 재검증.
