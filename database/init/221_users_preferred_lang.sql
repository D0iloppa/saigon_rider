-- 221: 유저 표시 언어 — 푸시/인앱 알림 문안을 수신자 언어로 만들기 위한 서버측 사본.
-- 앱은 선택 언어를 localStorage 에만 두므로 서버가 알 방법이 없었다(키워드 알림이 항상 한국어).
-- NULL = 미동기화(아직 앱이 한 번도 알려주지 않은 계정) → 문안은 앱 기본 언어 vi 로 폴백한다.
-- 기본값을 두지 않는 이유: "vi 를 고른 유저"와 "모르는 유저"를 구분해 두면 나중에 추정 로직을
-- 넣을 때 되돌릴 수 있다.
ALTER TABLE users ADD COLUMN IF NOT EXISTS preferred_lang VARCHAR(8);
