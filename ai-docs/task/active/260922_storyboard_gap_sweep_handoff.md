# 260922 스토리보드 재감사·갭스윕 — 세션 핸드오프

이 세션은 사용자가 실기기로 앱을 확인하다가 260922 구현보고서를 신뢰 못 하겠다고 지적한 것에서 시작해, 스토리보드(`ai-docs/review/260919_service_flow_review_kit.md`) 전체 재감사·미구현 갭 조사·신규 화면 SoT 정의까지 확장됐다. 세션이 길어서 다음 세션에서 이어갈 수 있게 정리한다.

## 이번 세션에서 바뀐 것 (커밋 순서대로)

| 커밋 | 내용 |
|---|---|
| `34495cc8` | ActiveSessionBar 레이어링 버그 3건 수정(시트 겹침·TopBar 좌측쏠림·업체 CTA바 가림) |
| `5b23d490` | 스토리보드 SoT 동기화, 260922 구현보고서 오류 5건 정정 |
| `1ac0110f` | 커뮤니티 글쓰기 사진 섹션 분리(F-CM-01 FR-3) |
| `0832440f` | codebase-memory 재인덱싱 |
| `76068287`~`f789e1b2` | 프로필 헤더 IA 재설계(F-P-02 FR-1/FR-3, 안B 확정) |
| `699346a9` | frontend-page-map.md ↔ 스토리보드 F-ID 교차링크 12곳 |
| `7a7bef37` | 워키토키 세션바 UX 재설계(접속 3분법·경과시간·말하는중, F-S3-03 FR-6) |
| `4aa717a0` | page-map 워키토키 이력 12차 동기화 |
| `f094e75e` | DM 채팅 스크롤이 인라인 세션바 높이를 못 따라가던 버그 수정 |
| **`e7505be8`** | 워키토키 롱프레스 메뉴 복원(채널변경/초대재전송, F-S3-03 FR-3) |
| **`07b1ed66`** | **판정 18건 일괄정리 + 신규 SoT 12개 화면(68→93프레임) + B3 정리** |

⚠️ **`e7505be8`, `07b1ed66` 두 건은 `--no-verify`로 커밋됐다** — 세션 막바지 WSL 재부팅 전후로 pre-commit 훅이 `.codebase-memory` 재색인과 지속적으로 경합해 정상 커밋이 불가능했다(사용자 승인 받고 우회, 실제 코드는 diff 0줄·ESLint 0 errors로 반복 확인됨). **다음 세션에서 여유 있을 때 `pre-commit run --all-files` 한 번 정상 실행해서 훅 전체가 실제로 통과하는지 사후 검증 권장.**

## 인프라 사고 기록 (재발 시 참고)

세션 후반, WSL2의 9p 파일시스템 캐시 버그(`dmesg`: "FS-Cache: Duplicate cookie detected")로 `/mnt/c` 접근이 광범위하게 불안정해졌다 — 수십 개 서브에이전트가 동시에 파일을 두드린 게 원인으로 추정. 사용자가 WSL을 재부팅했고, **재부팀 직전 아직 디스크에 flush 안 된 미커밋 작업트리 변경분이 유실**됐다(review kit md 2610→1669줄로 순간 되돌아감). `/home/doil/.cache/pre-commit/patch*`(WSL 네이티브 ext4, Windows 마운트와 무관)에 남아있던 pre-commit 스태시 캐시에서 전량 복구해 커밋까지 마쳤다 — **git fsck로 실제 손상은 없었음을 확인**. 앞으로 이런 사고가 또 나면: (1) `git fsck --no-progress`로 손상 여부 먼저 확인(보통 손상 아니라 일시적 I/O), (2) 커밋 안 된 대형 변경은 `/home/doil/.cache/pre-commit/patch*`에서 파일별로 `awk '/^diff --git a\/<path>/,/^diff --git a\/<다음파일>/' <patch파일> > /tmp/recover.patch` 로 추출 후 `git apply --check`로 검증하고 적용.

## 오늘 확정된 핵심 원칙 (다음 세션도 지켜야 함)

**"코드에 있다고 PASS/완료로 보지 않는다."** 이번 세션의 진짜 반성점 — 이전 260922 보고서와 초기 감사(W1~W5)가 코드 diff·유닛테스트만 보고 "완료"라고 판정했는데, 실제로는 z-index 겹침·헤더 좌우쏠림·CTA바 가림 같은 **실제 렌더링 확인 없이는 절대 못 잡는 버그**가 여러 건 있었다. 사용자가 스크린샷으로 하나씩 잡아낼 때마다 근본 원인이 반복 패턴(전역 세션바가 다른 전역 표면과의 우선순위를 정의 안 함)이었음이 드러났다. **앞으로 "완료" 판정은 실제 화면 렌더링(브라우저/실기기) 확인을 거쳐야 한다.**

## 다음 세션 우선순위 — 실제 코드 버그 (판정·SoT는 있고 구현만 남음)

우선순위 순. `F-ID`는 `ai-docs/review/260919_service_flow_review_kit.md`에서 검색.

1. **F-BZ-03 FR-3 (높음, 돈 관련)** — `/biz/status`가 리다이렉트 허브인데 APPROVED 1건이라도 있으면 무조건 `/biz/manage`로 보내버려서, **승인 1건+반려 1건을 가진 파트너는 반려 사유·재신청 화면에 영영 도달 못 함.**
2. **F-BZ-04 FR-3 (높음, 돈 관련)** — 반려 광고가 처리 우선순위 정렬에서 1순위로 뜨는데 정작 액션 버튼이 없는 막다른 길(`BizAdDetail.tsx:141`). 광고 노출 상태가 항상 "알 수 없음". **[게시 중단]이 확인 없이 즉시 실행**(다른 삭제 액션은 확인 1회인데 돈 들어간 이것만 없음).
3. **F-SET-01 FR-6 (높음, 안전)** — 차단 해제(`BlockedUsers.tsx:24-38`)가 확인 없이 즉시 실행되고, 앱 안에 재차단 경로가 없음(F-P-01 FR-2 "타인 프로필에 차단 없음"과 맞물려 막다른 길).
4. **F-SET-01 FR-4 (높음)** — 알림 설정(`NotiSettings.tsx:56,70-82`) 로드 실패 시 조용히 "전부 켜짐"으로 렌더된 뒤, 토글 하나만 건드려도 그 상태 그대로 서버에 PUT — 꺼놨던 알림이 켜진 채 저장됨.
5. **F-MP-02 FR-1 (중간)** — `/map/favorites`가 `ListingCard`에 `onToggleLike`를 안 넘겨서, 이미 F-S1-02에서 고친 "찜 목록에서 해제 안 됨" 버그가 이 화면에만 그대로 남아있음.
6. **F-COUP-01 (중간, 재발 방지)** — QR 오버레이(`MyCoupons.tsx:113-128`)가 `BottomSheet`가 아니라 자체 div라 `useSheetPresenceStore`를 안 올림 → 오늘 고친 세션바-시트 겹침 버그가 여기서 재발 가능.
7. **F-S7-03 FR-3 (중간)** — 거래이력 화면이 `user.id` 미확정 상태로 진입하면 `setLoading(false)`가 호출 안 돼 **로딩이 영구히 안 풀림.**
8. **F-H-01 (중간, 잠복)** — 광고 활성화 시(`ADS_ENABLED=false`로 현재 비활성) `priceVnd:0`인 가짜 상품카드로 위장돼 `AD` 표시 없이 노출, 탭하면 404. 찜 하트 아이콘이 장식(탭 무동작). 필터 버튼 누르면 검색어 증발. 재시도 버튼 6개가 사실 전체 재조회.
9. **F-S5-01 FR-4 (낮음, 판정 미확정 ☐)** — 위치공유 칸 문구·presence 폴백이 워키토키(F-S3-03 FR-6)처럼 3분법·상태문구로 통일 안 됨. 의도적으로 판정을 열어둔 상태.
10. **F-S3-03 FR-4 (낮음, 정의 자체가 불확실)** — "OO님이 나갔어요" 시스템 메시지. 원문이 `[신규 여부 확인]`으로 표기 — 대표 확인 먼저 필요, 구현 착수 전 질문할 것.
11. **F-S4-01 FR-2 (낮음)** — 약속 시트 열 때 서버 전송 폴백은 있지만(DmDetail.tsx:692) 시트 UI 자체엔 기본 장소가 안 보임("지도를 탭해 장소 찍기"만 뜸).
12. **`WalkieTalkieEntryButton.tsx` (낮음, 정리)** — 여전히 고아 컴포넌트. 롱프레스 메뉴 복원(FR-3) 작업에서도 재사용 안 됨 — 삭제할지 어디에 쓸지 판단 필요.

## 신규 SoT 12개 화면 — 판정(☐) 대표 확인 필요

이번 세션에서 `F-H-01`(홈), `F-DM-01/02`(채팅), `F-S7-03`(거래이력), `F-RN-01`(길안내), `F-MP-02`(동네지도 확장 4종), `F-SET-01`(설정), `F-SOC-01`(소셜그래프), `F-COUP-01`(쿠폰함), `F-BZ-03`(업체온보딩), `F-BZ-04`(업체관리)를 신설했다(전부 판정 ☐, 68→93프레임). 각 프레임에 "제안"과 "검증 기준"이 있으니, 대표/사용자가 실제로 화면을 보면서 판정만 하면 되는 상태 — **이 판정이 나야 위 우선순위 목록 항목들의 구현 방향도 최종 확정된다.**

## 참고 — 재빌드/검증 명령

```bash
node ai-docs/review/storyboard/build.mjs   # review kit md → storyboard/index.html 재생성 (프레임 수 검증 출력됨)
docker compose --env-file .env up --build -d frontend   # 프론트 반영
git fsck --no-progress   # git 손상 의심될 때 (보통 정상, 빈 출력 = 클린)
```

## 열린 질문 (다음 세션 시작 시 사용자에게 물을 것)

1. F-BZ-03/04, F-SET-01의 안전/돈 관련 버그(위 1~4번) — 바로 구현 진행해도 될까, 아니면 대표 확인 먼저 받을까?
2. 신규 SoT 12개 화면 판정(☐) — 사용자가 직접 화면 보면서 판정할까, 아니면 AI가 "정당성 있는 기본안"을 먼저 제안하는 재설계 패스(T2)를 한 번 더 돌릴까?
3. F-S3-03 FR-4("OO님이 나갔어요" 시스템 메시지) — 신규 기능으로 확정할지 여부.
