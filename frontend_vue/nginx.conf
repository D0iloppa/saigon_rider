server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback — 새로고침/직접 URL 진입 처리
    location / {
        try_files $uri $uri/ /index.html;
    }

    gzip on;
    gzip_types text/plain text/css application/javascript application/json;
}
