"""016 §4-5 #40 — 판매자 패턴 라벨(업자 후보 식별). D-33=(a): 라벨링 + 비즈 프로필 전환 유도만
한다. 자동 제재는 절대 하지 않는다(§9 완료조건: "자동 제재 없음").

016 §4-5 판단 근거 — 업자를 배제하지 말고 라벨링해야 하는 이유 3가지:
  1. 공급이 병목이다 — 213건 중 188건이 단일 계정인 상황에서 대량 등록자를 쫓아내는 건 자해.
  2. 베트남 오토바이 중고시장은 원래 딜러 중심 — 개인만 남기는 건 시장 구조와 싸우는 일.
  3. 문제는 업자가 아니라 위장 — 배제가 아니라 양성화(비즈 프로필 전환)가 정답이고, 그대로
     B2B 수익 경로가 된다.

임계는 전부 하드코딩(B2), 이 파일 한 곳에 모은다.
"""

# 등록 속도: 최근 이 기간(일) 내 신규 등록 건수가 임계 이상이면 신호 1건.
REGISTRATION_VELOCITY_WINDOW_DAYS = 7
REGISTRATION_VELOCITY_THRESHOLD = 5

# 동일 모델 반복 — 모델명 정규화 필드가 없어 "같은 카테고리 동시 보유 건수"로 근사한다
# (016 §4-5 예시: "같은 모델 3건 이상 동시 보유"). 카테고리는 모델보다 넓은 근사치라 임계를
# 그대로 3으로 두면 오탐이 늘 수 있으나, 후보 판정은 아래 CANDIDATE_MIN_SIGNALS 로 다중신호를
# 요구해 단일 신호 오탐을 완화한다.
SAME_CATEGORY_REPEAT_THRESHOLD = 3

# 다지역 게재: 서로 다른 구(district_id) 게재 수 임계.
MULTI_DISTRICT_THRESHOLD = 2

# 업자 후보 판정: 3개 신호(등록속도·동일카테고리반복·다지역) 중 이 개수 이상 충족해야 후보로
# 표시한다. 단일 신호(예: 이사 후 지역이 바뀐 개인 판매자의 다지역 신호 1건)만으로는 오탐이라
# 다중신호를 요구한다.
CANDIDATE_MIN_SIGNALS = 2

# 사업자 어휘 — 정보용 신호(§4-5 예시: "bảo hành"(보증) · "trả góp"(할부)). 후보 판정에는
# 가담시키지 않는다 — 개인 판매자도 흔히 쓰는 표현이라 단독 신호로 쓰면 오탐이 크다(§4-4
# 오탐 주의와 동일 취지). 어드민 화면에 참고 정보로만 노출.
BIZ_VOCAB_PATTERNS = ["bảo hành", "trả góp"]
