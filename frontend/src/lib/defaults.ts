/**
 * 앱 UI 자체의 기본값(폴백) 리소스 상수.
 *
 * 실제 사용자 콘텐츠가 아니라 미설정 상태를 나타내는 자리표시자이므로,
 * 반드시 로컬 번들 자산을 가리켜야 한다 (원격 CDN 의존 금지 — 저속망 안정성).
 */

/** 아바타 미설정 유저의 기본 이미지 (frontend/public/avatar-default.png) */
export const DEFAULT_AVATAR_URL = '/avatar-default.png';
