# 현재 상황 (Session Carry-Over)

> 진행 상태의 SoT는 **Plane CE** (https://plane.doil.me)이다. Plane MCP 또는 `/admin/dev`로 확인.
> Context KV는 DB(`__DEV_context`)에 유지. Features/Todos는 Plane Issues 기반 (폴백: DB).
> 이 파일은 Plane에 담기 어려운 **맥락적 판단·결정사항·외부 의존**만 기록한다.
> 완료 이력은 [`history.md`](history.md). **마지막 갱신**: 2026-07-18 (**관리자 콘솔 리메이크 1차 완료(P0~P8)** — 신규 admin SPA(`/admin`, admin-frontend)+JSON API(`/admin/api`)+legacy 병행(`/admin-legacy`), T&S 코어(통합 신고·제재·매물 모더레이션·금칙어)+공지/FAQ CMS+앱 T&S UI, P8 통합 회귀 전건 PASS·마이너 수정 2건·검증 잔여물 정리. 상세 활성 태스크 최상단 항목) / 2026-07-12 (**동네지도 2차 배치 완료** — 대상 `App.tsx`·`NeighborhoodMap.tsx`·`SaigonMapV5.tsx`(+css)·`FeedDetail.tsx`(+css). 상세 3종(업체/매물/피드) 전체화면 오버레이 전환(`App.tsx` 신규 `BackgroundRoutes` — navigate state `backgroundLocation`으로 배경 라우트(지도) 유지 + `.detailOverlay` 레이어로 상세 렌더, 뒤로가기=오버레이만 닫힘·지도 상태 보존, `bizReturn` 스냅샷 저장/복원 로직 주석 비활성화·잔존 키 정리 이펙트만 유지) / 지도보기 필을 시트 바깥 하단 중앙 floating 버튼으로 재배치(시트 full 스냅 노출, `maxHeight` 65vh→72vh, 리스트 하단 패딩 보강) / 바텀시트 리스트 최상단 ward 지역 제목(`map.wardTitle.{listings,feed,biz}`, `{{area}}`=중심 ward 베트남어 원문, 헤더 'N건'과 병존, 검색·커버리지 밖 숨김) / 좌측 버튼 묶음(내 위치·♥·+)이 시트 상단(+14px) 밀착→포스트 패널 열림 시 카드 기준 추종→시트 풀업 시 숨김 / `SaigonMapV5` 렌더 완성도(도로 casing 2-pass·줌 연동 도로폭 `roadWidthK`·팔레트 4단 명도 위계·건물 음영 duplicate(딥줌 게이트)·round cap/join, 마커·LOD·콜백은 불변) / `FeedDetail` 전체 재작성(`MarketDetail` 구조 미러 — hero 캐러셀·작성자 행·하단 액션바, 헤더 타이틀 '게시글'→'피드'). 회귀 하네스 `regr-biz-return.mjs` 오버레이 의미로 재작성(S1/S2 오버레이 열림·닫힘+상태 유지, S4 잔존 키 정리 검증)+`regr-map.mjs` S7 floating 위치 어서션 추가, 전 시나리오 PASS. **워킹트리 미커밋**(1차와 동일 세션 누적, 커밋은 후속)) / 2026-07-12 (**동네지도 구조 리팩토링 완료** — 대상 `NeighborhoodMap.tsx`/`SaigonMapV5.tsx`/`PostPanel.tsx`. 바텀시트 탭 순서 `[업체,피드,매물]`+기본 탭 업체 / 핀 표출 줌 게이트 강화(`L2_VBW`→`L3_VBW`, 매물·피드·업체 공통·검색 forceMarkers 예외 유지) / 지도-리스트 분리(리스트는 지도 중심이 속한 ward 기준 — `findWardAt` 신규 export+`regionContains`, ward 불변이면 refetch 안 함, 핀은 기존 뷰포트 bbox 유지) / 지역선택(region 모드) 진입점 비활성화(`onRegionSelect` 배선·`selectArea` 가이드 주석 처리, 로직은 보존 — ward 자동 추적으로 대체) / PostPanel `PanelItem` union 일반화(매물·피드 핀 탭도 팝업 캐러셀, 플리킹 recenter는 후보 재구성에서 제외·업체의 "열림 중 후보 동결"은 유지) / 카드 캐러셀 스케일 확대(폭 ≈350px·높이 220px·썸네일 120px). 회귀 하네스 DEEP bbox 축소(L3 게이트 통과)+`regr-map` S5 매물 핀 팝업 검증 재작성+`regr-biz-return` S1 찜 토글 멱등화, 전 시나리오 PASS. **워킹트리 미커밋**(코드·문서만 완료, 커밋은 후속) / 2026-07-12 (**동네지도 업체 UX 4패키지 완료** — W1 업체 핀 당근식 teardrop 리디자인(`22688c5`)·W2 BizPublic 소식 섹션+`GET /biz/public/:id/news`(`b5c008b`)·W3 좌측 버튼 배선 ♥찜 토글 필터·+글쓰기 메뉴(`7eff692`)·W4 업체상세 뒤로가기 상태 복원(`18794b4`)+회귀 하네스 갱신. 통합 시각회귀 7종 PASS + 복원 회귀 S1~S4 4/4 PASS. **+W5 업체 후기 신규 도메인**(대표 결정 "후기=업체에 남기는 후기" 반영): `business_review` init/123(UNIQUE upsert)·`GET/POST(/mine) /biz/public/:id/reviews`·+메뉴 재배선(포커스 업체 직행/뷰포트 선택 스텝)·BizPublic 후기 섹션(★평균+목록). **운영 미배포**(대표 지시 — 배포 시 init/123+BFF 재시작+프론트 재빌드). ADR 신규 저장·moderate 재인덱싱 완료(W5 후 fast 인덱싱까지 수행, 이후 MCP 재단절)) / 2026-07-11 (**동네지도 3패키지 전건 완료** — 1차 업종아이콘·자동말풍선·소식실데이터 + 2차 당근UX(W1지도보기필·W2포스트패널·W3카테고리DB화·W4unread뱃지) + 3차 프로필 실배선(찜·장소제안·내후기), 커밋 13건. 통합 시각회귀 8종 PASS + Opus qm-reviewer 리뷰 CHANGES→수정 반영. **잔여 P3**: 패널 재점화·view-ping 자기포함(제품 결정 대기). 운영 배포 시 init/118~122+BFF 재시작+프론트 재빌드. **⚠️ ADR 저장 미완**(세션 MCP 재단절 — 다음 세션 `manage_adr store` 필수)) / 2026-07-10 (**동네지도 개편 P1 T1~T7 구현 완료** — SGR-321~327, 커밋 `13420c9`~docs. 업체 핀 제3 레이어·업체 탭·카테고리 칩·업체 검색. 상세 활성 태스크 항목) / 2026-07-10 (**SGR-312 비즈니스 파트너 BP-1~BP-7 전 패키지 구현 완료·독립리뷰 PASS** — 커밋 `89ae6a0`~`fc57eda`, 잔여는 시각검증·운영 배포 체크리스트뿐. 상세 활성 태스크 항목 참조) / 2026-07-10 (**Plane 티켓 정리·재분류** — 방치된 Todo18+InProgress22=40건을 서브에이전트 7개(haiku/sonnet/opus 라우팅)로 검증 → **Done 18·Cancelled 12·유지 10**. 피벗 기준(게임요소=폐기 / 마일리지→RP→기프티콘 코어=보존) 적용. 게임요소 12건(가챠·스킬트리·아이템착용·시즌패스·다크모드·퀘스트검증) Cancel, **쿠폰연동 SGR-116은 이미 완전구현이라 Done**(TODO "미진행"은 stale), 마켓 Phase 대부분 구현완료 Done. handoff SoT=Plane **SGR-311**. 유지 10건 후속: 238/236/270(WorldMap 유가위젯·info리팩터)·227(fresh DB init 실버그)·288(마켓 P2)·274/119/120/117/118(실기기·외부의존)) / 2026-07-07 (⓪+2 **동네지도 시나리오 회귀**: GPS 컨텍스트 4종×30케이스 명세([`TEST/map_test_scenarios.md`](../TEST/map_test_scenarios.md)) 작성 → 3트랙 병렬 회귀 → **FAIL 9건 전건 수정·재검증 7/7 PASS** (핀 탭 무반응=pointer capture 재타겟팅, ghost 카드=정렬 tie-breaker 부재, HCMC 밖 가드 V5 이식 누락, region 해제 불일치, 재진입 복원 경합 등). 명세 개정 4건 포함. (⓪+1 **UX 피드백 배치 5건**: ①동네지도 줌 게이트(줌아웃=배지+가이드, fetch 0건·뷰포트 localStorage 복원·GPS 자동측정 0 — 원칙 준수 절충, SoT [`260707_map_zoom_gate_task.md`](../task/active/260707_map_zoom_gate_task.md)) ②DM 거래카드 리디자인(컴팩트 중앙 카드+CTA 버튼 행, 당근 관례) ③**약속잡기 게이팅**: 판매자 항상/구매자는 가격제안 수락 or 판매자 선제안 후(서버 403 강제, 대표 피드백. 당근 실사실은 대화 게이트·양측 대칭 — 완화 여지 기록) ④약속 일시 기본값(다음 정시) ⑤홈 "내 주변"→마켓 위치 컨텍스트 연결(쿼리 소비 후 제거). 교차회귀 14체크: 13 PASS + 발견 4건 전건 수정(쿼리 잔존 스냅백·vi 버튼 클리핑·이미지 오토스크롤). **QM 점검 기준에 UX 렌즈(폼 초기값·빈 상태·마찰) 추가** — 약속 빈 일시 누락 지적 반영. (⓪-0 **디자인 리서치 제안 5건 전건 처리**: ①신뢰 티어 전환(5티어 칩 — 매물상세·프로필) ②거리→🛵 오토바이 N분 ③전화 CTA 보류(전화번호 미보유+동의 정책 선행 — 근거 기록) ④홈 등록 FAB(탭바 개편은 결정 대기) ⑤판매 카운터 "Đã bán N" 칩. SoT [`spec/design-uplift-260707.md`](../spec/design-uplift-260707.md) §후속. CLAUDE.md 에 **모델 라우팅 규칙(§5) 명문화**(작업별 모델 자체 결정+근거 제시). ⓪-1 **회귀 스윕(41체크 38PASS)+발견 6건 전건 수정** & **P1 안정성 패키지 완료**(E-1 워커 격리+DLQ·E-2 healthcheck·E-4 콜백 유실 차단·E-9 재시도·B-1 suppress 제거+표기 정직화, 적대 리뷰 반영·실Redis 테스트 PASS) — SoT [`TEST/qm_regression_260707.md`](../TEST/qm_regression_260707.md). **멱등성 2차 완료**: GPS msg_id 멱등키(sre055)·E-6 쿼터 락·E-8 키 필수화·E-12 user 스코프 — 잔여는 E-5 가챠/상점 멱등 레코드(현재 이중지급 경로 없음 판정·운영 트래픽 전 권장)·E-7(stub) ⓪-2 **디자인 업리프트 1차(마켓 표면)** — 플랫 리스트 전환(4화면)·가격 표기 현지화(`N đ` 후치, Chotot 실측 관례)·오렌지 절제·가짜 카운트 제거. SoT [`spec/design-uplift-260707.md`](../spec/design-uplift-260707.md) — **매너온도 은유 재검토 등 제품 차원 제안 5건 대기**) / 2026-07-06 (①**QM 웹 스윕 1차 완료** — 13유닛 브라우저+코드 점검, 결함 25건 중 P1/P2 18건 수정·리뷰 PASS·커밋(`4435edd`~`e413da5`), TS-1~15/F-* 전건 판정, **사용자 결정 8건 대기**. 보고서 [`TEST/qm_sweep_260706.md`](../TEST/qm_sweep_260706.md). QM 하네스 `tools/qm/qm-shot.mjs` 신설 ②**DM 가격제안 코드 DONE**(SoT [`260706_dm_price_offer_task.md`](../task/active/260706_dm_price_offer_task.md), 운영 배포 시 init/110) + DM 조회 3종 무인증 교정 ③점검 260703 후속 워킹트리 전부 커밋(보안 `46d13ee`/지도·마켓 `6e642be`/DM·키보드 `8c69a7c`) ④운영 방침 전환(대표): 작업 단위·웹앱 우선·QM 위임·모델 라우팅 AI 판단 ⑤**제품 결정(중고거래 피벗)**: FAB 비활성 유지·다크모드 안 만듦·전화번호 인증/변경 안 함(버튼 제거)·**Android에서 Apple 로그인 숨김(iOS 전용)**. **⚠️ codebase-memory 재인덱싱 여전히 미완**(MCP 끊김 — 다음 세션 `index_repository(moderate)` + ADR 확인 필수. 이번 세션 변경 대량: dm/market/auth/settings/shop/gacha/info 등). 직전 2026-07-04 전체 점검·P0 보안 S-1~S-7)

---

## 외부 의존 / 대기 중

| 항목 | 상태 | 다음 액션 |
|---|---|---|
| ~~다크모드 (SGR-192/197)~~ | **종결 (2026-07-06 결정: 안 만듦)** | 메뉴 숨김(DARK_MODE_MENU_ENABLED=false) 유지. 코드는 보존, 후속 작업 없음 |
| Capacitor 네이티브 빌드 검증 | Mac 측 대기 | `native/CAPACITOR_MIGRATION.md` §3 회귀 체크리스트 |
| SaigonDistrictMap 집계 배지 | 시각검증 대기 | 브라우저에서 줌/배지 탭 확인 |
| OpenWeather API 키 | `.env`에 설정 완료, 활성화 대기 | mock fallback 동작 중 |
| **TRANSLATE_API_KEY (Google Translate) 403** | 키 설정은 있으나 무효/만료 추정 — 매물 상세 번역이 전부 폴백(원문 표시) 중 | 키 재발급→`.env` 교체→bff 재시작. 코드 견고성은 확보됨(`de6cbd0` — 번역 실패 시 원문 폴백 200, 과거엔 500). 키 복구 후 정상 번역 경로 1회 검증 필요 |
| Google Maps Directions 키 (SGR-269) | 코드 완료·휴면, 키 발급 대기 | `GOOGLE_MAPS_API_KEY` 발급→`.env` 입력→`restart bff`. 가이드: [`google-maps-api-key-setup.md`](../google-maps-api-key-setup.md). 미설정 시 "준비 중" 폴백 |

## 미해결 결함 ([issues.md](../TEST/issues.md))

> QM 스윕 260706 판정: F-AUTH-LOGIN·F-02-7 은 **폐기**(OtpInput/PhoneInput 삭제, OAuth 전환으로 전제 소멸).

(없음 — F-03-2 닉네임 중복체크도 2026-07-06 구현 완료. 이력: qm_sweep_260706.md §5)

## 트러블슈팅 (QA 점검) — 2026-05-30 → ✅ QM 스윕 260706 전건 판정 완료

> 판정 근거·상세는 [`TEST/qm_sweep_260706.md`](../TEST/qm_sweep_260706.md) §4. 요약:
> - **해소/정상 확인**: TS-1(재현 안 됨 — 유사 증상 원인이던 dev-login 공유계정 레이스는 수정), TS-2, TS-3, TS-4, TS-6, TS-7, TS-8, TS-10, TS-11, TS-12, TS-13, TS-14, TS-15
> - ~~잔여~~ → **전건 종결(2026-07-06 결정 후속)**: TS-5 보류 결정(게이미피케이션 축소), TS-9 5토글 축소+연동 구현 완료

## 활성 태스크 (🔧)

- **서비스·지도 출시 차단 감사 후속 (2026-07-22, P0/High 코드 처리 완료·외부 게이트 대기)** — `origin/main` `3755179`의 감사 Batch A~F를 재-pull해 대조한 뒤 OAuth state를 Redis 단회용 저장으로 전환하고 callback 장기 토큰 노출을 제거했다. 양방향 피드 차단 경계, 알림 수신자별 재전달 멱등 키·기존 DB one-shot migration/readiness, 번역 rate-limit 장애 fail-closed·원자적 TTL을 보강했다. 실제 fulfillment가 없는 INTERNAL 쿠폰은 Engine catalog·BFF API·앱 진입점 모두 게이트 OFF했고, inventory 잠정보류와 불일치하던 차고 route·프로필·게임 허브 진입점도 닫았다. 운영 DB의 `[DEV]` seed는 compose profile+SQL guard+pre-commit 검사로 차단했다. 침수 실패를 안전/0건과 분리하고 제보 GPS를 명시적 버튼 뒤로 이동했으며 날씨 위치 provenance를 바로잡고, 이미지 저장 동기 I/O도 event loop 밖으로 이동했다. **출시 HOLD 잔여**: Android/iOS 실기기 권한·GPS·FCM·OAuth 딥링크, 운영 외부 키/실패 주입, Docker fresh-volume·compose build, 약관/개인정보 제품·법무 확정. **2026-07-23 해결**: 관리자 로그인 throttling(ADM-6 — Redis escalating lockout, username+IP 2축, fail-open), migration prefix lint(DB-6 — baseline+pre-commit), 번역 실패 UI(BIZ-9 — market/feed 상세 `translation_failed` 배지), 알림 producer transactional outbox(FD-6 — `notification_outbox`+enqueue+relay, event_id 안정 멱등). **잔여 후속**: FD-6 delivery-side outbox(worker crash 창 한정 push 재전달; `_try_push` 재시도+DLQ가 provider 실패는 이미 커버). SoT: [`260722_service_user_full_launch_audit_task.md`](../task/active/260722_service_user_full_launch_audit_task.md), 지도 상세: [`260722_map_service_launch_blockers_task.md`](../task/active/260722_map_service_launch_blockers_task.md).
- **관리자 콘솔 화면 리빌드 (2026-07-21, 완료·미커밋)** — 신규 `/admin` SPA의 기능·API·라우트는 유지하고 `DESIGN-admin.md` 기준으로 화면단을 재설계. 전역 AntD/CSS 토큰·라이트 운영 사이드바·64px 탑바·컨텐츠 페이지 헤더·틴트 상태 배지·공용 KPI·테이블/카드/폼 표면을 통일하고, 저장 가능한 라이트/다크 테마 전환과 제공 로고 기반 favicon·사이드바 브랜드 이미지를 적용. 공식 사이공 스카이라인 자산 기반 로그인 히어로와 `www.saigon-rider.com` 랜딩 링크도 배선. 레거시 콘솔 진입 링크는 제거. `admin_frontend` 컨테이너 재빌드 완료. SoT: [`260719_admin_console_visual_rebuild_task.md`](../task/active/260719_admin_console_visual_rebuild_task.md). Plane은 권한 403, Notion 미러는 외부 쓰기 정책으로 생성 거부.
- **관리자 콘솔 리메이크 1차 + T&S 코어 (2026-07-18, P0~P8 전건 완료·커밋됨)** — 커밋 `5aa6695`(T&S 스키마 init/126~131: reports 통합·user_sanctions·admin_audit_log·notices/faqs·banned_keywords) / `cea12af`+`ca37c23`(기존 어드민 `/admin-legacy` 이전) / `c3acfae`(admin_auth 공용화+`/admin/api` JSON API 골격) / `e411b90`(admin SPA 스캐폴드+compose/nginx 배선) / `a8b7c84`(T&S 코어 — 통합 신고 처리·유저 제재·매물 모더레이션 HIDDEN/REMOVED·정지 차단·DM 금칙어) / `b141a7f`(대시보드 지표) / `bd1edee`(신고센터·유저·매물 화면) / `37fc736`(고객센터 티켓) / `cdf5f6f`(공지·FAQ CMS+앱 공지/FAQ 화면) / `7e12794`(앱 T&S UI — 유저·DM 신고, `/suspended`, 금칙어 토스트, 숨김 표기).
  - **구조**: `/admin` = 신규 SPA(`admin-frontend/` React+antd+react-query, vite base `/admin/`, compose 서비스 `admin_frontend` — 포트 미노출, nginx 내부 프록시 전용) / `/admin/api` = BFF JSON API(`backend/app/routers/admin_api/` — auth·dashboard·reports·users·listings·support·cms) / `/admin-legacy` = 기존 Jinja 콘솔 병행 운영. 인증은 `backend/app/admin_auth.py` 공용(JWT+httpOnly 쿠키 `admin_session` — 신규↔legacy SSO). 계정: env `ADMIN_USER`(root) + `admin_accounts` 서브계정(role admin, 감사로그 메뉴는 root 전용).
  - **P8 통합 회귀 (2026-07-18, curl/psql/Playwright 실측)**: 앱 API 계약 24체크 전건 PASS(매물 신고 201/409/400/404, HIDDEN/REMOVED 목록 비노출 — ON_SALE 베이스라인 포함 실효 검증, HIDDEN 상세는 200(의도 — 판매자 상태표기)·REMOVED 상세 404, status 전이+moderated 가드, DM 전송/조회, ACTIVE 스모크 GET 전부 200) / 신규 콘솔 e2e 로그인+9메뉴 진입 렌더 콘솔에러 0 / legacy 사이드바 24메뉴 전건 200 / 세션 상호호환(신규 쿠키→legacy 200) / Engine 무변경(`git diff main~9..HEAD -- engine/` 0) / `.env`↔`.env.example` 키셋 diff 0 / 전 서비스 compose 재빌드 기동.
  - **P8 마이너 수정**: ① legacy `/admin-legacy/users` 500 — `h(u.phone)`이 phone NULL(OAuth 계정)에서 AttributeError → `u.phone or "—"` 가드 ② admin SPA favicon(data URI 🛡️) — 매 페이지 404 콘솔로그 제거 ③ dev DB P4 검증 잔여물 정리(WARN 제재 1건+`Playwright 자동검증` MODERATION 알림 3건+P8 회귀 임시 데이터, admin_audit_log는 보존).
  - **2차 이식 잔여**(legacy → 신규 SPA 미이식 메뉴): 퀘스트 / 피드 / 관리자계정(admins) / 설정 / SRE 6메뉴(items·gacha·shop·policies·ops·daily-featured) / 스트림 / 라이딩정책(config/ride) / 유가(fuel) / 제보 심사 3종(gas-submissions·repair-submissions·place-suggestions) / 비즈 심사 2종(biz-accounts·biz-ads) / 푸시 / POI / DEV Context. + **Engine admin API 4종 `_admin`→`_svc` 전환 필요**.
  - **미해결**: ① 신규 콘솔 감사로그 페이지는 Placeholder("준비 중" — 메뉴 노출·라우트만 존재, 목록 API 미구현. DB `admin_audit_log`는 정상 기록 중) ② dev DB에 P2 검증 제재 이력 5건(user_sanctions `검증 *`, `__dev_p2tgt` 계정) 잔존 — P8 정리 범위 밖이라 보존, 신고센터 데이터 아님 ③ 운영 배포 시 init/126~131 적용+`ADMIN_JWT_SECRET` env 필수(미설정 시 BFF fail-fast)+admin_frontend 서비스 추가 기동.
- **동네지도 2차 배치 — 오버레이 전환·floating UX·렌더 완성도·피드 상세 통일 (2026-07-12)** — 아래 "동네지도 구조 리팩토링" 바로 다음에 이어진 같은 날 2차 작업. 대상 `App.tsx`/`NeighborhoodMap.tsx`/`SaigonMapV5.tsx`(+`.module.css`)/`FeedDetail.tsx`(+`.module.css`). 코드·회귀 검증 DONE. **워킹트리 미커밋**(1차와 동일 — 커밋은 후속).
  - **지도보기 필 재배치**: 시트 안 `floatingTopCenter` 슬롯 사용을 그만두고 `NeighborhoodMap.tsx`에서 시트 바깥 형제 엘리먼트로 직접 렌더하는 하단 중앙 floating 버튼으로 전환(노출 조건 `sheetSnap==='full' && !isSearching && !postPanelOpen`). 시트 `maxHeight` 65vh→72vh로 확장(필이 빠지며 확보된 여백), 리스트 컨테이너에 하단 패딩 클래스(`listPillPad`) 추가로 마지막 리스트 항목이 필에 가리지 않게 함.
  - **바텀시트 리스트 상단 ward 지역 제목**: `t('map.wardTitle.{listings|feed|biz}', { area })` — `area`는 `centerWard.region.name`(ward 폴리곤의 베트남어 원문 지명), 탭마다 다른 문구(ko 예: `{{area}} 이웃들이 등록한 상품이에요` / `...의 소식이에요` / `...지역의 가게들을 만나보세요`, en/vi 3벌 대응). 헤더의 'N건' 카운트와 별개로 리스트 최상단에 병존. 검색 중이거나 `centerWard`가 없으면(region 모드·커버리지 밖) 숨김.
  - **좌측 지도 버튼 묶음(내 위치/♥/+) 위치**: 기본은 시트 상단에서 14px 띄운 위치(`sheetVisibleHeight + 14`, 드래그와 함께 연동), 포스트 패널(팝업 카드)이 열려 있으면 카드 높이 기준(`postPanelHeight + 14`)으로 전환해 카드를 따라가고, 시트가 `full`로 풀업되면 숨김(`display:none`) — 인라인 style 3분기.
  - **`SaigonMapV5` 렌더 완성도** (마커·LOD·콜백 시그니처는 불변): 도로 케이싱 2-pass(케이싱 폴리라인 전체→fill 폴리라인 전체 순서로 그려 교차점을 자연 병합, `ROAD_FILL`/`ROAD_CASING` 팔레트 매핑, 골목 `#f6f6f6`은 무케이싱 — 전체 도로의 ~55%를 차지해 노드 2배 비용을 피함) / 줌 연동 도로 폭(`roadWidthK(vbw)` — `(vbw/L3_VBW)^0.4` 지수 곡선, 진입 직후 ×1.5~딥줌 ×0.69 완만화, 0.6~2.0 클램프) / 팔레트 4단 명도 위계(수면 < 지면(ward) < 블록 < 건물 < 도로(흰 fill), 저채도 웜톤으로 `.sea`/`.ward`/`.blk`/`.bldg`/`.road`/`.water` 색상 전면 재조정) / 건물 음영 duplicate(`bldgShadow` — y+오프셋 어두운 폴리곤을 아래 깔아 입체감, 딥줌 게이트 `vb.w < L3_VBW*0.5`에서만 렌더해 노드 부담 제한) / 도로·하천 `stroke-linecap`/`stroke-linejoin: round`. depth3 fetch 시 도로를 폭 오름차순 1회 정렬(fill pass에서 상위 등급 도로가 위에 그려지도록).
  - **피드 상세(`FeedDetail.tsx`) 전체 재작성**: `MarketDetail` 레이아웃을 미러링 — 공용 `TopBar` 대신 `StatusBar`+커스텀 뒤로가기 헤더, hero 이미지 캐러셀, sellerRow를 미러한 `authorBlock`(작성자 아바타·닉네임·레벨뱃지·타임스탬프), 하단 `actionBar`(🔥응원 토글+💬댓글 수+댓글 입력+전송, 기존 별도 입력바를 대체). 헤더 타이틀 i18n 기본값 '게시글'→'피드'(ko/en/vi 3벌: 피드/Feed/Bảng tin).
  - **동네지도 → 상세 3종(업체/매물/피드) 전체화면 오버레이 전환**: `App.tsx`에 `BackgroundRoutes` 컴포넌트 신규 — `navigate(path, { state: { backgroundLocation } })`로 진입하면 배경 `<Routes>`는 `backgroundLocation`(지도)으로 그대로 렌더되고, 실제 목적지(`/biz/:id`/`/market/:id`/`/feed/post/:postId`)는 `App.module.css` `.detailOverlay`(전체화면 absolute, z-index 30, `AppShell` 스플래시보다 아래) 레이어로 얹힌다. `NeighborhoodMap.tsx`의 지도 내 상세 진입 지점 전부(업체 말풍선·업체 카드·매물 카드 2곳(검색 결과/리스트)·포스트 패널 카드 3분기(업체/매물/피드) — `navigate()` 호출 7곳)에 `backgroundLocation` state를 실어 오버레이 진입으로 전환. 하드웨어/브라우저 뒤로가기는 오버레이만 닫고 지도는 언마운트되지 않으므로 탭·칩·찜필터·포스트패널·선택핀 상태가 그대로 유지된다. **`sgr.map.bizReturn` 스냅샷 장치(저장 `saveBizReturnSnapshot`/복원 `pendingUiRestoreRef` 등)는 이제 불필요해져 전부 주석 처리로 비활성화**(과거 세션이 남긴 키를 지우는 `sessionStorage.removeItem` 정리 이펙트만 유지). 마켓 리스트·커뮤니티 등 지도 바깥에서의 기존 상세 진입은 `backgroundLocation`을 싣지 않아 그대로 페이지 이동.
  - **회귀 하네스**: `tools/qm/regr-biz-return.mjs`를 오버레이 검증 취지로 재작성 — S1/S2는 "오버레이 열림(URL=상세, 지도 DOM 잔존 확인용 `mapAlive`, 스냅샷 미저장) → back → 오버레이만 닫히고 탭/칩/찜/패널/선택핀 상태 유지"를 확인(S1은 찜 토글 멱등화도 유지), S3은 신규 진입 기본값(업체 탭+'전체' 칩), S4는 "과거 세션이 남긴 스냅샷 키가 있어도 정리 이펙트가 지우고 기본 상태로 시작"으로 재정의(예전 "PUSH 진입 시 스냅샷 폐기" 취지를 오버레이 체제에 맞게 대체). `tools/qm/regr-map.mjs` S7에는 지도보기 필의 화면 하단 근접 위치 어서션(`rect.top > innerHeight*0.7`)을 추가. 전 시나리오 PASS.
- **동네지도 구조 리팩토링 완료 (2026-07-12)** — 대상 `NeighborhoodMap.tsx`/`SaigonMapV5.tsx`/`PostPanel.tsx`, 코드·회귀 검증 DONE. **워킹트리 미커밋**(요청에 따라 이번 세션은 문서만 갱신 — 별도 커밋 필요).
  - **탭 순서/기본값**: 시트 탭 배열을 `['biz','feed','listings']`(= 업체·피드·매물)로 변경, 기본 탭 `biz`.
  - **핀 표출 줌 게이트 강화**: `SaigonMapV5.onViewportChange`가 부모에 알리는 `showDistrictBadges`(핀 숨김/뱃지 표시) 판정을 `!l2`→`!l3`로 변경 — 임계값이 `L2_VBW`(블록 단계)에서 `L3_VBW`(건물 단계)로 조여져, 더 깊이 줌인해야 핀이 뜬다. 매물·피드·업체 핀 레이어 공통 적용, 검색 결과(`forceMarkers`)는 게이트 예외로 유지. 게이트 미통과 시 `listings`/`posts`/`bizItems` 상태를 전부 비운다(리스트는 별도 — 아래 참조).
  - **지도-리스트 분리**: 바텀시트 리스트(`wardListings`/`wardPosts`/`wardBiz`)는 지도 중심 좌표가 속한 ward 기준으로 조회하도록 전환(`SaigonMapV5`에 신규 export된 `findWardAt` — `saigon-depth1.json` 폴리곤+`regionContains` 재사용, ward 미변경 시 참조 캐시로 재조회 생략). 핀(`listings`/`posts`/`bizItems`, `markers`)은 기존처럼 뷰포트 bbox 소스를 그대로 쓴다 — 즉 같은 화면 안에서 핀과 리스트가 서로 다른 데이터 소스를 갖는다. 지도 dot 탭 시 리스트 항목을 활성화 스크롤하던 동기화 코드는 주석 처리로 제거(로직은 남아 있지 않음, 완전 삭제 아님 — 필요 시 `git diff`로 확인).
  - **지역선택(region 모드) 진입점 비활성화**: `SaigonMapV5`의 `onRegionSelect={handleRegionSelect}` prop 배선과 리스트의 `selectArea` 안내 가이드를 주석 처리(코드는 보존 — 부활 시 주석 해제만 필요). ward 자동 추적 리스트가 사실상 그 역할을 대체.
  - **PostPanel 일반화**: `PanelItem = {kind:'biz', biz} | {kind:'listing', listing} | {kind:'feed', post}` union 도입. 매물/피드 핀 탭 시에도 업체와 동일한 팝업 카드 캐러셀이 열림(플리킹 → `focusPointRef`로 recenter, 사용자가 직접 지도를 팬/줌하면 새 뷰포트로 캐러셀 후보 재구성). 플리킹·오픈 recenter 가 커밋하는 bbox는 `suppressPanelRebuildRef`+`suppressedPanelBboxRef`(참조 기억)로 재구성 대상에서 제외해 무한 루프를 막는다. 줌 게이트 이탈(핀 소멸) 시 매물/피드 팝업은 자동으로 닫히되, 업체 팝업의 "열림 중 후보 동결"(2026-07-11 대표 결정)은 그대로 유지. 찜 토글/`viewerCount`/읽음 배지/`bizReturn` 스냅샷은 업체 카드 전용으로 남음.
  - **카드 캐러셀 스케일 확대**: 카드 폭 `calc(100% - 40px)`(390px 화면 기준 카드 ≈350px, 이웃 peek ≈12px), 높이 220px 고정, 썸네일 72px→120px, 하단 여백은 `--bottom-safe` 변수 반영.
  - **회귀 하네스**: `tools/qm/regr-map.mjs`/`regr-biz-return.mjs`의 `DEEP` bbox를 축소(L3 게이트를 확실히 통과하는 좌표로 조정). `regr-map` S5를 "매물 핀 탭 → 팝업 캐러셀" 검증으로 재작성(플리킹 recenter·닫기 후 시트 복귀까지 확인). `regr-biz-return` S1은 찜 토글을 멱등화(이미 찜된 상태면 재클릭하지 않음 — 반복 실행 시 토글이 누적되어 결과가 매번 뒤집히던 플레이크 수정), S3/S4는 기본 탭 '업체' 반영. 전 시나리오 PASS.
- **동네지도 업체 UX 4패키지 완료 (2026-07-12)** — 당근 레퍼런스 기반 대표 피드백 반영, 코드·시각검증 DONE·커밋 5건.
  - **W1 핀 리디자인**(`22688c5`): `MapMarkerV2.kind='biz'` teardrop 분기(매물/피드 원형 무변경), 꼭짓점 앵커, BIZ_COLOR `#16a34a`→`#ff5a1f`(탭 배타라 매물 핀과 동시 노출 없음), 선택=꼭짓점 기준 1.3x 확대(구 선택 링 제거 — 회귀 하네스 셀렉터도 갱신), 그림자는 defs radialGradient(200핀 filter 비용 회피).
  - **W2 소식 섹션**(`b5c008b`): BFF `GET /biz/public/:id/news`(무인증·APPROVED 404 가드·limit 1~30·DESC), BizPublic 연락처 아래 소식 피드(3줄 클램프+더보기·사진+N·AppImage), 로드 시 `markBizNewsRead`로 핀 unread 뱃지 정합, i18n 3종. 당근 상세의 탭/별점/단골은 데이터 부재로 미도입(과설계 배제).
  - **W3 좌측 버튼**(`7eff692`): ♥=찜 업체만 필터 토글(ON 시 biz 탭 자동 전환, 비로그인 토스트, 0건 emptyBiz 문구, 칩 필터와 AND) / +=팝오버(장소 제안=NeighborhoodProfile 시트 `?openPlaceForm=1` 재사용, 후기쓰기=/info/repair 목록 — **지도 컨텍스트 전용 후기 작성 플로우 부재, 제품 결정 대기**).
  - **W4 뒤로가기 복원**(`18794b4`): sessionStorage `sgr.map.bizReturn`(tab·칩·favOnly·postPanel/bubble UI), `useNavigationType()===POP`만 소비(PUSH는 폐기), 선택 UI는 첫 biz fetch 후 1회 가드 복원, 자동 말풍선 이펙트 뒤 선언으로 덮어쓰기 차단, SaigonMapV5(07-07 rAF 복원 지점) 무수정. 하네스 `tools/qm/regr-biz-return.mjs` 신설 4/4 PASS.
  - **검증**: 통합 시각회귀 `regr-map.mjs` 7종 전건 PASS(S2 FAIL은 구 선택 링 셀렉터 — 하네스 갱신으로 해소). tsc/eslint/ruff 0.
  - **W5 업체 후기 (2026-07-12 후속 확정·구현)**: 대표 결정 "후기=업체 후기, 별도 배선". init/123 `business_review`(UNIQUE(profile_id,user_id) upsert — RepairReview는 방문 건별 기록형이라 관례 비적용 근거 기록), BFF `GET /biz/public/:id/reviews`(무인증, wrapper `{reviews,total,avg_rating,has_more}` — info_repair 관례 미러)·`POST`(세션 인증, 무인증 419 관례)·`GET .../mine`(수정 프리필), 프론트 `BizReviewSheet`(별점+본문, 수정 모드)+`BizReviewPickerSheet`(포커스 업체 직행/뷰포트 선택/0건 토스트)+BizPublic 후기 섹션(소식 아래·광고 위). curl 전 케이스·regr-map 7/7 PASS·스크린샷 4종 확인. dev 시드 3건.
  - **잔여**: ①업체 후기 RP 보상 여부(제품 결정 — 훅 없음) ②후기 신고/모더레이션 컬럼 없음(악성 대응 필요 시 후속) ③**운영 미배포 상태**(대표 지시): 배포 시 init/118~123 적용+BFF 재시작+프론트 재빌드.
- **동네지도 3패키지 전건 완료 (2026-07-11)** — 1차/2차/3차 코드·시각검증·독립리뷰 DONE·커밋됨.
  - **1차** SoT [`260711_map_biz_icons_autobubble_news_task.md`](../task/active/260711_map_biz_icons_autobubble_news_task.md). init/118(소식 시드 4건), `/biz/public/map` latest_news, 핀 글리프 5종+칩·카드 아이콘, 뷰포트 idle 자동 말풍선(lat 스팬 ≤0.03·중앙 반경 0.25·시트 무이동), 말풍선 정직화(소식 없으면 소개 폴백).
  - **2차(당근 UX)** SoT [`260711_map_daangn_ux_task.md`](../task/active/260711_map_daangn_ux_task.md). W1 지도보기 필(`0801724`) / W2 핀 터치→포스트 패널(`d9f0dac`, 시트 대체·IntersectionObserver 캐러셀·N명 보는중 Redis 폴링) / W3 카테고리 DB화(`4ae528a`+`037aea3`, business_category init/119 15종·4그룹, BizApply taxonomy 이원화 선재버그 해소)+소식 사진 N장(business_news_photo init/120) / W4 핀 unread 뱃지(`8b2091a`, localStorage `sgr.biz.readNews`).
  - **3차(프로필 배선)** SoT [`260711_map_profile_wiring_task.md`](../task/active/260711_map_profile_wiring_task.md). P-BE(`b61a806`, user_favorite_business init/121+`/biz/favorites`·place_submission init/122+`/biz/place-suggestions`+admin 큐·내 후기 집계) / P-FE(`12789f1`, 퀵메뉴·나의 후기 실데이터·장소 제안·`/map/favorites` 통합 탭·찜 토글).
  - **검증**: 통합 시각 회귀 8시나리오 전건 PASS(하네스 `tools/qm/regr-map.mjs`, 커밋됨). **qm-reviewer(Opus) 독립 리뷰 CHANGES → 수정 반영**(`7df2082`: placeForm.error 키 신설·`useVisualViewport.ts` 고아 삭제·캐러셀 핸들러 useCallback).
  - **P3 제품 결정 2건 확정·반영(2026-07-11)**: view-ping 자기 포함 **현행 유지** / 패널 [X] 직후 자동 말풍선 **1회 억제 구현**(다음 bbox 커밋 시 해제, 헤드리스 검증 PASS).
  - **잔여**: 소식 사진 등록 API 구현 시 상한 10 적용, codebase-memory ADR 저장(세션 MCP 재단절 — 다음 세션 필수), 운영 배포 시 init/118~122 적용+BFF 재시작+프론트 재빌드, Plane 티켓 등록(세션 내 MCP 부재).
- **동네지도 개편 P1 — 업체 핀 레이어 구현 완료 (2026-07-10, SGR-315, T1~T7=SGR-321~327 전건 Done)** — SoT [`260710_map_biz_pin_layer_task.md`](../task/active/260710_map_biz_pin_layer_task.md) §검증 기록. 커밋: T1 `13420c9`(BFF `GET /biz/public/map` bbox·category·q, APPROVED+좌표만, 상한 200) / T2 `1286615`(init/117 dev 시드 5건 HCMC 좌표+카테고리 — `/biz/apply` 위치 입력은 기존재 확인, dev DB 적용됨) / T3 `12ef2a3`(핀 레이어 배열: listing/feed 탭 배타 유지+**biz 상시 제3 레이어**, SaigonMapV5 label·r 렌더, 줌 게이트 준수) / T4 `6af11d5`(업체 탭+카테고리 칩 5종+업체 카드, i18n ko/en/vi) / T5 `b299002`(업체 핀 탭→탭 전환+시트 mid+하이라이트) / T6 `6275643`(업체 검색 — searchScope 제출 시점 고정, 최근검색어 공유). **해석 결정(문서 모호점)**: "3종 핀 동시 렌더"를 당근 G-1/G-2(업체 핀=지도 상시 콘텐츠) 근거로 biz 상시 레이어로 구현 — 매물/피드 탭에서도 업체 핀 노출. 대표 확인 필요 시 탭 배타로 축소 가능(레이어 1줄 수정). **잔여**: ①실기기/브라우저 시각 스모크(GPS 4컨텍스트 회귀는 코드·API 레벨만 검증 — `TEST/map_test_scenarios.md` 수동 확인 필요) ②codebase-memory 재인덱싱+ADR(세션 MCP 끊김) ③운영 배포 시 init/117+BFF 재시작+프론트 재빌드. P2(후기+RP+찜)·P3(단골·쿠폰·소식)는 백로그 유지.
- **비즈니스 파트너 — 비즈니스 계정 신설 (2026-07-10, SGR-312)** — **BP-1~BP-7 전 패키지 구현 완료·독립리뷰 전건 PASS**. SoT [`spec/business-partner-260710.md`](../spec/business-partner-260710.md). 커밋: BP-1 스키마 `89ae6a0`(init/113, business_group·business_profile·marketplace_ads owner/review_status) / BP-2 신청 플로우 `1d5c97d`+fix `f63c295`(BFF `routers/biz.py`, 프론트 /biz/intro·apply·status·manage 4화면, LinkRouter `biz`) / BP-3 admin 심사 `59e3dda`+fix `a30ba59`(`/admin/biz-accounts` 큐, noti_worker `biz.profile_reviewed`, init/115) / BP-4 광고 `c689757`+fix `d6470a8`(init/116, 광고 CRUD+노출 게이트 `review_status=APPROVED` 필터, `/admin/biz-ads`, `biz.ad_reviewed`) / BP-5 이관 `a62797e`(init/114, dev 광고주 5명→business_profile 5건 APPROVED 시드, 광고 28건 연결·레거시 3건 NULL 의도 보존) / BP-6 노출 `c604557`(`GET /biz/public/:id` 무인증 공개조회, `/biz/:id` BizPublic, `adHref()` 헬퍼로 AdCard 3곳+AdDetail 연결) / BP-7 랜딩 `fc57eda`(`landing/` 정적 vi/ko/en 3벌, 프리런치 CTA, nginx `business-landing.conf`, dev 포트 **:18092**). **잔여**: ① `/biz` 화면·admin 큐 브라우저 시각검증(랜딩은 스크린샷 확인됨) ② 레거시 광고 3건 owner NULL 배정 여부 별도 결정 ③ business_profile/신규 광고 `district_id` 없음(지역 타게팅 미구현, 필요 시 후속) ④ L2 Zalo OA 미결(이메일 단독 시작) ⑤ **운영 배포 시**: init 113~116 적용 + BFF·noti_worker 재시작 + `BIZ_LANDING_PORT` env + 랜딩 DNS/TLS(saigon-rider.com 마이그레이션과 조율).
- **알림 에이전트 레이어 + 설정 정비 + 인앱 알림함 (2026-07-10)** — **P1~P3 코드 DONE·리뷰 전건 PASS**. SoT [`260710_notification_agent_task.md`](../task/active/260710_notification_agent_task.md) (**Plane 미등록 — md가 SoT**, 오염방지 결정). 커밋 `ec2f61a`/`c39cb52`/`92dc289`+`becaba6`+`a3e9fba`.
  - 아키텍처 결정: agent manager 별도 구현 안 함 — Redis Streams 컨슈머 그룹이 producer-consumer 오케스트레이션 제공. 단일 알림 바운디드 컨텍스트 `noti_worker`(BFF 스택, docker 서비스 신규, 스트림 `noti:events`/DLQ). DM·키워드 푸시 이벤트화, 죽어있던 notification_settings 토글 실효화(푸시만 게이트, 인앱 row 항상 기록).
  - 화면: 퀘스트 토글 4종 UI 제거(게이미피케이션 보류, 백엔드 보존)·keyword_alert 토글 신설 / 인앱 알림함 `/notifications` + 홈 벨 뱃지 신설(GET /notifications 최초 실사용 배선).
  - 보안 부수확보: notifications 라우터 무인증 구멍 3건 교정(GET 목록·GET/PUT settings — 46d13ee S-계열 동일 클래스, 세션검증+본인스코프 404).
  - **⚠️ 잔여**: ① codebase-memory 재인덱싱(이 세션 MCP 끊김 — 다음 세션 필수) + 알림함 화면 page-map/ADR 반영 ② 운영 배포 시 init/111·112 적용 + `noti_worker` 서비스 기동(자동 reload 없음, WSL /mnt 에선 bff --reload 도 신뢰 불가 — 재시작 필수) ③ FCM 실전달은 실기기 검증 의존(SGR-274 축) ④ dev DB에 P2/P3 QA 데이터 잔존(__dev_p2qa1/2 계정 등).
- **DM 가격제안하기 (2026-07-06)** — **코드 DONE·스모크 통과, 시각검증 잔여**. SoT [`260706_dm_price_offer_task.md`](../task/active/260706_dm_price_offer_task.md) (구현·리뷰 요약 상단 블록). 약속잡기 패턴 미러 + 리뷰 부수확보: **DM 조회 3종 무인증 구멍 교정**(260703 점검 누락분 — 세션+참여자 검증), send_message 예약타입 위조 차단. dev DB init/110 적용됨, **운영 배포 시 110 적용 필요**. **금주(~07/10) 대표 리뷰 대상.**
- **B-2 OAuth 로그인 전환 (SGR-B2, 2026-06-19)** — P1~P5 코드 DONE, dev 검증 완료. SoT [`ai-docs/task/active/260619_oauth_login_task.md`](../task/active/260619_oauth_login_task.md).
  - P1: DB 마이그 100(users.phone nullable + user_oauth_identities) + 101(oauth app_config seed) — dev 적용 완료.
  - P2: BFF `POST /auth/oauth/login`(Google tokeninfo + Facebook debug_token) + `POST /auth/session/verify` + `POST /auth/dev-login`(개발 전용). `backend/app/services/oauth.py` 신규.
  - P3: `session.ts` → `{userId, sessionToken}`, `App.tsx` bootstrap → session/verify, `api/auth.ts` apiOAuthLogin/apiSessionVerify/apiDevLogin.
  - P4: `native.ts` signInWith stub, `OAuthLogin.tsx` 신규(Google/Facebook 버튼 + dev 테스트 버튼), i18n ko/en/vi. PhoneInput/OtpInput 폐기 삭제.
  - P5: `101_oauth_config_seed.sql` placeholder-only.
  - **⚠️ 잔여**: ① 실제 Google/Facebook client_id → app_config UPDATE(키 발급 안내 문서 `260619_oauth_login_task.md` §7) ② native SDK 통합(Android Capacitor 플러그인 — Mac 빌드 의존) ③ `01085213251` 테스트 계정 OAuth 연동(OAuth 완성 후) ④ 운영배포 시 APP_ENV=production 설정 필요(dev-login 차단).
  - **tsc 0 / eslint warning 0(기존 코드 경고만) / vite build 성공 / dev-login 200 / session-verify valid→200 bad→401**

- **침수 데이터 전략 3층 모델 (결정 2026-06-04)** — ①상습 핫스팟 baseline + ③실시간 UGC 제보 **안정화 완료**, ②날씨 기반 일일 예측은 **OpenWeather 실연동 후로 보류**.
  - ① baseline: `flood_hotspot_stats` 30건(037 시드, dev 적용됨) → 플러드 페이지 "🌧 상습 침수 지역" 섹션으로 표출(좌표→구역 필터). 제보 0건이어도 화면 채워짐.
  - ③ UGC: 침수 신고 = 주유/정비식 **바텀시트**(FAB→하단 CTA 이동), 깊이 select, **사진 실첨부**(`/contents/upload`→imgproxy_url을 photo_url 저장, FloodDetailSheet 표시), 현재 GPS. 즉시 반영(주유/정비와 달리 admin 큐 없음 — 실시간 특성).
  - **② 구현 완료(SGR-268, 시각검증 대기)**: OpenWeather **이미 라이브**(키 유효·BFF 전달·프론트 `VITE_USE_MOCK=false`; 메모리 "mock fallback" stale). init `054_flood_risk_daily.sql`+`FloodRiskDaily` 모델(flood_report와 분리). 잡 `jobs/predict_flood_risk.py`(구역별 24h pop≥50%→핫스팟 예측위험 당일 적재, BFF APScheduler 05:30/15:00 ICT). map-data에 `risks`+수동 트리거 `POST /info/flood/admin/predict-risk`(service key). 프론트: 통합 리스트에 "⚠️ 오늘 침수 위험 N%"(예측·실신고 아님 명시)+중복 핫스팟 억제. 만료 24h. 가짜 실신고 seed 없음.
  - **흡수/정렬**: 핫스팟·예측위험·실시간 제보를 "최근 침수" 단일 리스트로 통합·최신순(제보 reported_at > 예측 today > 핫스팟 last_flood). 헤더 "최근 침수 · N건".


- **SGR-251 정보 서브페이지 지도/리스트 템플릿 통일** — 코드 DONE, **시각검증 대기**. 부모 SGR-242(메인화면/지도 개편). 기준 템플릿=침수(InfoFloodMap): 토글 없이 [지도(상단)→활성리스트(하단)] 동시 표시.
  - **주유/정비**: `view` 토글(`[지도|리스트]`) 제거, `InfoMap variant="fullscreen"`(침수와 동일, mapWrap height 280px) + 리스트 하단 동시 표시. 죽은 `viewToggle*` CSS 정리.
  - **날씨**: 날씨 카드 기준은 현 동작(선택 구역 따라감) 유지. 지도 컴포넌트를 메인과 동일한 `InfoMap variant="section"`(구역 지도, 현 위치 구역 highlight)으로 교체. **RainRadar(RainViewer 강수 레이더) 제거**(`RainRadar.tsx` 삭제, weather `mapTitle` i18n 3종 추가). 잔여: `api/info.ts getRainRadar`/`RainRadarData`는 미사용 export로 잔존(라이브러리 메서드), weather radarTitle/radarUpdated/myLocation i18n 키 dead.
  - 검증: `tsc -b` 0, eslint 에러 0. seed 데이터(gas/repair) 정확화는 본 작업 후 별도(SGR-236 P3).
- **주유소 CSV 시드 전면교체 + UGC 제보 대기큐 + admin 검증** — P1~P3 코드 DONE, **시각검증 대기**(앱 제보 제출·admin 승인/반려 클릭). SGR-236 info 후속.
  - **P1 스키마+시드**: init `050_gas_ugc.sql`(gas_station +phone/url, `gas_station_submission` 대기큐 PENDING/CONFIRMED/REJECTED), `051_gas_seed_csv.sql`(OSM 759건 클리어→Google CSV 58건, **가격 없음**·district NULL·source=GOOGLE·brand 파생). dev DB 적용 완료(58건).
  - **P2 제보+화면**: BFF `POST /info/gas/report`→submission 적재(브랜드 서버 파생), nearby/상세에 phone. 프론트 **가격 UI 전면 숨김**(공식가 배너·cheapest 정렬·가격행 제거)→전화번호 표시, 하단 "주유소 제보" CTA+시트(현재 GPS). i18n 12종×3.
  - **P3 admin**: `/admin/gas-submissions` 큐(PENDING 상단)+승인(→gas_station upsert USER_REPORTED·verified_at)/반려. nav "주유소 제보" 추가. QA용 테스트 제보 1건(submission_id=1) 삽입해둠.
  - **결정**: 가격은 화면만 숨김(백엔드 fuel_price 유지), 제보는 신규추가만. **잔여**: 시각 QA / 향후 AI 자동검증(submission.status·review_note 개방) / 정비소 동일 패턴은 미요청. dead: gas viewMap/viewList·price CSS·formatPriceFull.
- **SGR-236 info 화면 마무리** — 발행 2026-06-04. 4모듈 골격은 완성, 마무리만 남음. 하위 P1~P4(SGR-237~240): P1 비 레이더 RainViewer 실연동(현 CSS 목업), P2 유가 후속 #50(admin 가격입력·WorldMap 위젯), P3 **초기 seed 정확화**(gas_station/repair_shop/repair_review/fuel_price INSERT 0건 — 현재 프론트 MOCK 의존), P4 시각 QA·통일성. 순서: P3→P4 선행, P1/P2 병렬. SoT `task/active/260604_info_screens_finalize_task.md`, Notion `3753bd6b-405d-819e...`.
- **SGR-228 재화 개념 정의 + 밸런싱 (RP·골드·스킬포인트)** — In Progress. 환율 **골드 100 : 스킬 10 : 크리스탈(RP) 1**. 골드=퀘스트+레벨업→아이템/가차, 스킬=레벨업→스킬. 확정 사항:
  - **RP 가치 = 1 RP : 100 VND**. 커피 한 잔(50,000 VND) = **500 RP**. 쿠폰 카탈로그 face_value 항목 재가격(mig **sre040**): DATA_1GB 140 / GOTIT_50K 500(커피) / GOTIT_100K 1000.
  - **데일리 퀘스트 RP = 0** (`ride.py` payload.rp→0). RP 수급은 **이벤트 퀘스트 전용**(재고 예측). 현재는 일괄 0, 경제밸런스 확정 후 이벤트 한정 도입(분기 미구현). 엔진 `action_definition.rp_grant`는 sre039서 이미 0.
  - **코스메틱 재가격**(mig **sre041**): BADGE_FOUNDER 100 / FRAME_NEON 300 / BADGE_LEGEND 2000 (커피 500 기준).
  - **마일리지 = EXP 전용**(mig **sre042**): MILEAGE_XP(고아 XP) 비활성, MILEAGE_EXP(10/km) 유지.
  - **레벨업 보상 단일 발동점 + 정책 주도**: `gain_exp(db,user,amt)` 단일 함수(`utils.py`)로 exp+레벨업+보상 통합(`apply_level_up` 폐기). 보상값은 BFF `levelup_reward_policy`(init **049**, seed GOLD 200/SKILL_PT 1)에서 읽음. 엔진 `LEVELUP_REWARD` 비활성(**sre044**; sre043은 SGR-229 criteria 점유). 호출처 전부(ride/quests/internal) gain_exp 경유 — quests 퀘완료 레벨업 누락도 교정.
  - 가챠 통화 라벨 `XP→RP` 통일(BFF `_CURRENCY_MAP`/`_gacha_type` + 프론트 gacha.ts/GachaMain + i18n tab_gc).
  - **Gold 확정**: 퀘 50/건 유지 + 레벨업 정책 GOLD 200/레벨.
  - ✅ **보고서 SGR-235 Done** — `ai-docs/spec/economy-balance-report.md` + Notion "260604 재화 경제 체계 정립 보고서". sink-economy-design.md `reward_exp×0.3` OBSOLETE 배너 정정 완료.
  - **미결(설계상 후속)**: 이벤트 퀘 RP 도입 / monthly_quota 실값(비즈니스 입력) / 마이그·049 활성화(기존 DB 수동, 배포 시). Notion 260604.
  - **✅ 상한 체계 + 리밸런싱 (2026-06-07, dev 적용·E2E PASS)** — SoT [`economy-cap-rebalance-design.md`](../spec/economy-cap-rebalance-design.md). 앵커: 커피≈30일(액티브 3슬롯)·RP예산 월100만원.
    - 구현: RP단가 7→6(`ride.py`) / 가챠 GP인하 BASIC 150·PREMIUM 1050(mig **sre049**) / 쿠폰 `monthly_quota` 200·50·100(sre049) / RP 일일캡 60·초과폐기(`xp_ledger.credit_gc`+`XpBalance.daily_gc_*`, mig **sre050**) / Lv30·SP누적12 캡(`utils.gain_exp`) / 슬롯합산 9 명문화(`quests`).
    - **보류(합의)**: 골드·마일리지 일일캡 — 무비용이라 ROI 낮음(예산은 quota가 보호).
    - **⚠️ 선재 갭 수정**: dev DB에 `levelup_reward_policy` 미존재(init/049 미적용)→레벨업 `gain_exp` 크래시 상태였음. init/049 적용함. **운영 배포 시 sre049·sre050 + init/049 + 엔진·워커 재시작 필요**.
  - **✅ 스킬 단계당 비용 1→3 SP (SGR-280, 2026-06-07 dev 적용·E2E PASS)** — SoT [`260607_skill_cost_3x_subpoints_task.md`](../task/active/260607_skill_cost_3x_subpoints_task.md). 효과 단계는 0~3 유지, 내부 저장을 **0~9 서브포인트**로 전환(단계=`//3`). 한 단계 = 3 서브칸 = 3 SP, 클릭당 1 SP 점진 투자. SP 적립 1/레벨 유지·**누적 상한 12→36**(`MAX_SKILL_PT_TOTAL`). 프론트 SkillTree는 3단계×3서브칸 시각화. 기존 단계값 ×3 보존(`init/072`).
    - 소비처 전부 `//3` 환산: `utils.resolve_reward_pct`(EXP/Gold)·`skill_cost_discount_pct`·`quests._item_slot_bonus`(슬롯 단계3 = `>=9`)·프론트 `rewards.ts`. invest 캡 `>=9`.
    - **⚠️ 운영 배포 시**: `init/072` 적용(CHECK 0..9 재설정 + 기존값 ×3) + BFF 재시작. 엔진 무관.
- (없음) — **SGR-220 개발→운영 1차 배포 + SOP 완료** (운영 `https://letantonsheriff.com` 가동). 이력은 [`history.md`](history.md) 2026-06-04, runbook `task/active/260604_deploy_prod_task.md`.
  - **배포 후속(backlog)**: ① **SGR-227** init 스키마 베이스라인 결함(fresh DB 빌드 불가 — dump-restore 우회 중) ② FCM firebase json 마운트(푸시 활성화) ③ official/grand-opening.jpg 1건(saigon.doil.me 복구 시) ④ **전용 도메인 `saigon-rider.com` 확보됨(2026-07-10 대표 확인)** — letantonsheriff.com→saigon-rider.com 마이그레이션 실행 대기(규칙은 runbook §도메인 마이그레이션). Zalo 콘솔 prod 콜백(www.saigon-rider.com, SGR-313)·비즈니스 랜딩(business.saigon-rider.com, SGR-312 §8 L1)이 이 도메인 기준으로 선등록/기획됨 — 마이그레이션 시점과 조율

## 활성 — 검증 대기 (🔧)

- **info 탭 정비리뷰·주유대기 RP 전환 + 리뷰 조회 화면 (2026-06-08, 커밋 `94d71ec`)** — 코드 DONE, **시각검증 대기**.
  - **🔑 근본버그(전 영역 영향)**: info 모듈 `_earn_gp_safe`가 `engine_client.post_event`를 잘못된 인자명(`user_id=`/`isoformat()`/`idempotency_key=` ↔ 실제 `user_uuid=`/`datetime`/`idem_key=`)으로 호출 → 매 호출 TypeError가 `contextlib.suppress`에 삼켜져 **info 리워드 이벤트가 엔진에 한 번도 전송된 적 없었음**(repair/gas/flood 전부 GP·RP 0 적립). repair/gas/flood 교정(weather는 원래 정상). → 이제 침수/주유 제보·정비 리뷰 보상이 실제 발동.
  - **정비 리뷰 XP→RP**: 마이그 **sre051**(INFO_REPAIR_REVIEW/PHOTO/PRICE `base_xp→0`, `rp_grant→50/10/10`). RP=gc_balance, 일일캡 60(=리뷰50+사진10, 가격분 폐기). UI XP→RP(ko/en/vi), 응답키 `rp_earned`. 종단검증: 엔진 발행→`gc_balance 0→50` 확인.
  - **차종**: 고정 select→자유입력+localStorage(`sgr.repair.lastVehicle`) 최근값. **사진 +10 RP는 실제 미적립**(토글만 있고 실업로드 없어 `photo_url` 미전송 → INFO_REPAIR_PHOTO 미발동). 실첨부는 별도(침수처럼 `/contents/upload`).
  - **리뷰 등록 정직화**: 실패 시 가짜 성공(setDone) 제거→에러 토스트.
  - **주유 대기 제보 화면 신규**: `WaitReportSheet`(혼잡도 3단계 여유/보통/혼잡 → 기존 `wait_minutes` 0/5/15 매핑, 인프라 재사용). 보상 RP 전환(마이그 **sre052**, +5). **신선도 윈도우 90→30분**(nearby 집계+어뷰징가드+expires_at) + 카드 'N분 전' 표기.
  - **정비소 전체 리뷰 화면 신규**: `/info/repair/:id/reviews`(InfoRepairReviews) + BFF `GET /{shop_id}/reviews`(페이지네이션, offset clamp) + 상세 '전체 보기' 링크. 상세 `reviewer_name→reviewer_nickname` alias 정합(리뷰어명 익명표시 버그 교정).
  - **FAB**: 정보 탭 활성화(시즌패스만 비활성).
  - **⚠️ 잔여**: ① 시각검증(브라우저: 리뷰 등록→RP↑, 혼잡도 제보, 전체 리뷰) ② 마이그 sre051/sre052는 dev 적용·엔진 재시작 완료, **운영 배포 시 적용+엔진 재시작 필요** ③ 사진 실첨부 미구현 ④ push 미실행.

- **SGR-274 DM 푸시 알림 + 알림 클릭 → 해당 DM 딥링크** — 코드 DONE(P1~P5), **실기기 시각검증 + FCM 자격증명 대기**. 서브 SGR-275~279. SoT [`260607_dm_push_deeplink_task.md`](../task/active/260607_dm_push_deeplink_task.md), Notion `3783bd6b-405d-8163…`.
  - **연결고리 5곳**: DM 기능·FCM 인프라는 완비됐으나 미연결이었음 → ① BFF `dm.py send_message`가 수신자(`_other_user_id`)에 푸시 트리거(try/except로 흡수, DM 전송 우선) ② engine `POST /v1/push/notify`(service-key, external_uuid→`device_user_map.fcm_token` 해석, 토큰無=no-op 200, `send_push(log_history=False)`로 admin 이력 미오염) ③ `engine_client.notify_user_push` ④ 웹 `LinkRouter` `dm` 케이스 + `NotificationBridge`(BrowserRouter 내 상주, `onNotificationClick`→`/link?action=dm&id=<conv>` + 콜드스타트 `getPendingNotification` drain) ⑤ native: iOS `FcmPlugin.getPendingNotification`(콜드스타트 `pendingPushPayload` 소비) + Android `FcmPlugin.java` 신규(jsName "Fcm", emit/setPending) · `MyFirebaseMessagingService`가 data `navigateTo`를 Intent extra로 · `MainActivity` onCreate(콜드)/onNewIntent(웜) 분기.
  - **딥링크 규약**: `data.navigateTo="dm&id=<conv_id>"` → `/link?action=dm&id=<conv_id>` → `/dm/<conv_id>`. 기존 quest 컨벤션 동일.
  - **검증 완료**: ruff/eslint/tsc 0, frontend vite build·BFF·engine 기동 클린, `/v1/push/notify` 단위(토큰有=발송시도/無=no-op/키無=401), BFF→engine `notify_user_push` 종단 배선.
  - **⚠️ 잔여 의존**: ① **실기기 클릭→DM 이동**(iOS/Android, 빌드머신 Mac 대기. native는 각 origin main 직접 push) ② **실제 FCM 전달**: ✅정정(2026-06-17) — `engine/firebase-credentials.json` 유효한 서비스계정(project saigon-rider-af3c9) 마운트됨 + `device_user_map` 토큰 6개 존재 + `fcm_push.send_push`(HTTP v1 JWT→OAuth) 구현. **과거 "401 미인가"는 자격증명 없던 시절 메모로 stale**. 현재 푸시 경로 동작 가능(실기기 토큰 대상 전달은 미검증). ③ 웹 라우팅 브라우저 시각확인.
- **SGR-271 퀘스트 수행 인터페이스 RideNav 완전 이관** — 코드 DONE, **실기기 시각검증 대기**. 부모 SGR-242. 발견: 엔진 검증·보상은 **이미 완결**(네이티브 bg GPS→`/v1/sreMessage`→Redis→GpsAgent→`quest_tracker.update`→validators→`quest_completed`→BFF `/internal/quest-card-completed` 멱등 지급). 실작업=프론트 배선 교체.
  - RideNav `type=quest`을 `useRideStore`(서버 폴링값) 기반으로 재배선: 진행도(distance=서버 `current_distance_m`/target, checkpoint=`distance_to_target_m`)·완료(`reachedTarget`=서버 status COMPLETED, 두 타입 공통)·속도 전부 서버값. 클라 haversine 누적 제거.
  - GPS 소스 = **기존 네이티브 핑 재사용**(신규 BFF 채널 미신설). QuestDetail "수행 시작"→`/ride-nav?type=quest`. **RideActive.tsx+CSS 삭제**, `/ride/active` 라우트·AppShell hide 제거.
  - **보상 서버 권위화**: 클라 이중지급(`addExp/addGold`) 제거→완료 시 `refreshUser()`로 잔액 동기화. 결과화면 표시는 `calculateRewards`(display 전용) 유지.
  - 잔여: ① 실기기 시각검증 ② `RideResultFail` 진입점 고아(라우트만 잔존, 보존) ③ `refreshUser`가 워커 비동기 지급보다 빠를 수 있음(자가치유). `tsc -b` 0·eslint 에러 0.
  - **이동경로(trail) 시각화 추가(2026-06-05)**: BFF `GET /quests/ride-trail`(engine `/v1/admin/stream/messages?type=gps&uuid=` 재사용)로 서버 수신 GPS 궤적을 RideNav `MapCanvas`에 시안색 폴리라인 표시. FE 3초 폴링(`native.getDeviceUUID()` 기반).
  - **⚠️ DEBUG 임시조치 — 복구 필요**: RideNav quest origin 의 HCMC D1 폴백을 비활성화함(`resolveOrigin()`→`native.getLocation()` 직접, 실패 시 origin=null). 코드 `DEBUG(SGR-271)` 주석. 디버그 종료 시 `resolveOrigin()`으로 되돌릴 것.
- **SGR-229 퀘스트 검증 인터페이스 추상화 (전략패턴 + 신호 기반)** — 코드 DONE(P1~P4), **✅ P5 마이그 적용 완료(2026-06-05)**. 서브 P1~P5(SGR-230~234).
  - 완료: `quest_tracker`를 Signal(GpsSignal/EventSignal) 기반 `QuestValidator` 전략 + `ValidatorRegistry` 디스패치로 리팩토링. DISTANCE/CHECKPOINT → `quest_validators/{distance,checkpoint}.py`. 목표 파라미터 `criteria JSONB` 전면 이관(mig **sre043**, 타입별 컬럼·CHECK 제약 제거). BFF `start_ride`/`engine_client`/프론트 `ActiveCardState`까지 criteria 배선. `dispatch_event()` 이벤트 진입점 시드(구체 타입은 제품 요구 시). SoT `task/active/260604_quest_validation_strategy_task.md`, Notion `3753bd6b...`.
- **✅ RESOLVED(SGR-228/229 dev 마이그 미적용 — SGR-271 테스트 중 발견 2026-06-05)**: dev DB가 sre039 고정 + 엔진 코드는 신버전 → 코드↔스키마 불일치로 퀘스트 카드 생성 **502**(`criteria` 컬럼 없음) + 가챠/상점 깨짐(엔진이 4-arg `pull_gacha`/`purchase_shop_item` 호출하나 DB는 sre035 3-arg). **`alembic upgrade head` 로 sre039→sre046 전체 적용**(040·041 reprice / 042 mileage / 043 criteria / 044 levelup / 045 skill discount / 046 item rebalance). **마이그 결함 수정**: `045`/`046` 의 `op.execute`/`exec_driver_sql` 가 한 호출에 다중 SQL문(DROP;CREATE, 다중 UPDATE) → asyncpg `cannot insert multiple commands into a prepared statement`. 문장 단위로 분리 실행하도록 수정(SQL 본문 불변). 엔진 재시작 불필요(신규 enum 값 없음). → **SGR-229 P5 해소 + 502/TS-3/TS-5(가챠·상점) 동시 해소**.
    - **⚠️ 후속(2026-06-05)**: 마이그 후 `saigon_worker`(GPS 소비자)가 구 모델로 드롭된 `target_distance_m`를 조회 → quest_tracker가 모든 핑에서 죽어 **이동거리 측정이 멈춰 있었음**. 워커는 자동 reload 없음 → `docker restart saigon_worker`로 해소(거리 누적 재개 확인). 모델/스키마 마이그 시 워커 재시작 필수.
  - 이전 `040`/`041` `required_rp`→`required_xp` 교정 + levelup 마이그 ID 충돌(sre044 재번호)은 2026-06-04 처리분.

## 부분 점검 (🟡)

- **info 4페이지·LocationPickerSheet 의 locateOnMount(진입 시 GPS 자동 측정)** — 동네지도는 원칙 위반으로 제거(2026-07-07). info 페이지들은 위치 기반 정보 화면 특성상 의도적일 수 있으나 service-rules 원칙 1("화면 이동 시 GPS 자동 측정 금지") 문구와 긴장 — **의도인지 제품 결정 필요** (결정 시 service-rules 화면별 표에 info 추가 or 동네지도식 명시 버튼으로 전환)

- F-03-1 닉네임 1자 IME 이슈 — 재빌드 후 재점검 필요
- **✅ SGR-285 회원가입 Android 버그 (F-03) — 해결 (2026-06-08)**. 사용자 제약 준수: Android 조치가 iOS 무영향.
  - ① **F-03-IME 전역 한글 밀림(블로커, 진짜 원인)**: Capacitor **`captureInput=true`** → `CapacitorWebView.onCreateInputConnection` 이 조합 미지원 `BaseInputConnection(this,false)` 반환 → **모든 입력창** 한글 조합 깨짐. React/controlled 무관(그래서 프론트 우회 전부 무효였음). **모바일 Chrome 정상 = 앱 WebView 설정 문제로 격리**. 해결 = **`captureInput=false`**: MainActivity CapConfig **+ `android/.../assets/capacitor.config.json`(WebView 실제 로드 파일 — cap sync 미실행으로 true 잔존했던 게 막판 핵심)** + `capacitor.config.ts`. → [[project_capacitor_captureinput_breaks_ime]]
  - ② **F-03-KBD 입력창 압축**: 키보드 무관 CSS — `.nickField`/`.styleCard` flex 자식 기본 `flex-shrink:1` 눌림 → **`flex-shrink:0`**(web). 보조 native `windowSoftInputMode=adjustPan`.
  - ③ **F-03-NICK 공백 닉네임**: 가입 시점 랜덤 닉네임 기본 부여(`auth.register`)+login/재가입 self-heal(`utils.generate_random_nickname`, UNIQUE 회피). 건너뛰기는 랜덤 유지.
  - 헛다리(원복됨): ProfileSetup uncontrolled+리스너/state-free, interactive-widget 런타임/정적 — IME엔 무효(captureInput이 원인이라). **교훈: 모바일 Chrome vs 앱 WebView 비교로 React vs WebView-config 를 먼저 격리**할 것.
  - 푸시: android `1d5a01f`(captureInput·adjustPan), primary `dbe8b30`. CSS/닉네임/다크모드숨김은 web 반영.
- 퀘스트 `thumbnail_content_id` 미연결 — 어드민 퀘스트 편집 시 컨텐츠 연결 UI 필요

## 다음 우선순위

0. **관리자 콘솔 2차 이식 (2026-07-18 잔여)** — legacy 잔여 메뉴(퀘스트/피드/관리자계정/설정/SRE 6메뉴/스트림/라이딩정책/유가/제보심사 3종/비즈심사 2종/푸시/POI/DEV Context)를 신규 SPA로 이식 + Engine admin API 4종 `_admin`→`_svc` 전환 + 감사로그 목록 API·페이지 실구현(현 Placeholder). 상세는 활성 태스크 최상단 항목.
1. **L4 줌 단계 확장 패키지 (동네지도, 2026-07-12 합의 — 2차 배치 렌더 완성도 후속으로 ⑤-B 요구 통합)** — 별도 패키지로 분리. Overpass 재추출(골목 footway/path 클래스 추가 + **⑤-B: 도로명 `name` 태그·골목·녹지 수집**(도로 렌더 완성도 패키지 후속 — 현재는 도로 색/폭만 렌더되고 이름 라벨은 없음) → 렌더 도로명 라벨링, 데이터 수집 에이전트) → simplify 0.15~0.2로 하향 → ward별 `depth4.json` 생성 → `MIN_VBW` 완화.
2. **동네지도 리팩토링 비차단 NIT 2건 (리뷰 발견, 2026-07-12)** — ① biz 탭이 아닐 때도 ward 매물/피드 fetch 이펙트가 상시 실행됨(탭 무관 — 기능 영향은 없고 낭비 호출뿐, 필요 시 `tab==='biz'` 가드 추가) ② `PostPanel.module.css`의 `.feedName { composes: bizName; }` — 회귀 하네스가 `bizName` 클래스로 업체 카드를 특정하므로, 하네스 셀렉터를 넓힐 때 피드 카드와 혼동하지 않도록 주의.
3. **CHECKPOINT BFF 연동** — BFF Quest 모델에 target_lat/lng 추가 또는 quest_pins 연동
4. **유가 후속 (Feature #50)** — admin 페이지, WorldMap 위젯 적용, InfoGasList nearby-v2
5. **GAP-H1 (#32)** E2E 테스트 시나리오 5개

---

## TODO 리스트 (스프레드시트 기준)

### 그룹 A — 아이템·퀘스트·푸시

| # | 내용 | 상태 | 비고 |
|---|---|---|---|
| A-1 | 퀘스트 개수를 20개로 줄이기 | 미진행 | |
| A-2 | 아이템 효과정의 | ✅ 완료 (SGR-210 DONE) | 착용효과 4종(RP/Gold 배수·퀘스트 슬롯·비용 할인), rarity별 고정 테이블. 슬롯별 시드(sre036). **사용자 노출 UI 완료**: 개러지 합산 HUD(탭→상세 시트)+개러지/인벤토리 개별 배지, effect 필드 Engine→BFF→Front. 개별 조정은 어드민 effect_type |
| A-3 | 아이템 착용시 개러지에 캐릭터에 착용 효과 보여주기 | 미진행 | A-2 완료, 시각화는 별도 |
| A-4 | 아이템 개수도 부위별 5개로 줄이기 | 미진행 | |
| A-5 | 앱에서 접속시 FCM 토큰 서버전송 | 미진행 | |
| A-6 | 관리자페이지에서 PUSH 기능 | 미진행 | |

### 그룹 B — 연동·테스트·알림

| # | 내용 | 상태 | 비고 |
|---|---|---|---|
| B-1 | 쿠폰연동 | 미진행 | 기존 작업 완료 후 진행 |
| B-2 | 회원가입 OAuth 연동 | 미진행 | 기존 작업 완료 후 진행 |
| B-3 | 안드로이드 14일 테스트 | 미진행 | 크몽에서 5000~6000원정도에 가능, 14일 테스트가 필수이기 때문에 앱등록이 필요함 |
| B-4 | 아이폰 알림서비스 클릭시 페이지 이동 | 코드DONE·검증대기 | SGR-274. iOS FcmPlugin navigateTo 포워딩(실행중)+콜드스타트 getPendingNotification. 실기기 대기 |
| B-5 | 안드로이드 알림서비스 클릭시 페이지 이동 | 코드DONE·검증대기 | SGR-274. Android FcmPlugin.java 신규+Intent extra+onNewIntent. 실기기 대기 |
| B-6 | 앱푸시연동 | **진행중** | DM 푸시(SGR-274) 연동됨. 미열람개수는 기존 badge(Redis incr_badge, 999 cap)·DM unread_count 별개. 1000→999 캡 잔여 |
